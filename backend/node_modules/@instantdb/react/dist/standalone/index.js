var Mi = Object.defineProperty;
var Ri = (n, e, t) => e in n ? Mi(n, e, { enumerable: !0, configurable: !0, writable: !0, value: t }) : n[e] = t;
var p = (n, e, t) => Ri(n, typeof e != "symbol" ? e + "" : e, t);
import fs, { useRef as $e, useCallback as Me, useSyncExternalStore as qt, useEffect as We, useMemo as _n, useState as ds, createElement as ji } from "react";
function ee(n) {
  if (typeof n == "number")
    return (Math.abs(n * 2654435761) >>> 0).toString(16);
  if (typeof n == "boolean")
    return n ? "1" : "0";
  if (n === null)
    return "null";
  if (n === void 0)
    return "undefined";
  if (typeof n == "string") {
    let e = 2166136261;
    for (let t = 0; t < n.length; t++)
      e ^= n.charCodeAt(t), e += (e << 1) + (e << 4) + (e << 7) + (e << 8) + (e << 24), e = e >>> 0;
    return e.toString(16);
  }
  if (Array.isArray(n)) {
    let e = 2166136261;
    for (let t = 0; t < n.length; t++) {
      e ^= (t + 1) * 2654435761;
      const r = ee(n[t]);
      for (let s = 0; s < r.length; s++)
        e ^= r.charCodeAt(s), e *= 16777619, e = e >>> 0;
    }
    return e.toString(16);
  }
  if (typeof n == "object") {
    let e = 2166136261;
    const t = Object.keys(n).sort();
    for (let r = 0; r < t.length; r++) {
      const s = t[r];
      if (n[s] === void 0)
        continue;
      const i = ee(s);
      e ^= parseInt(i, 16), e *= 16777619, e = e >>> 0;
      const o = ee(n[s]);
      e ^= parseInt(o, 16), e *= 16777619, e = e >>> 0;
    }
    return e.toString(16);
  }
  return ee(String(n));
}
const Z = {
  Remove: "remove",
  Replace: "replace",
  Add: "add"
}, hs = Symbol.for("__MUTATIVE_PROXY_DRAFT__"), xi = Symbol("__MUTATIVE_RAW_RETURN_SYMBOL__"), Dt = Symbol.iterator, me = {
  mutable: "mutable",
  immutable: "immutable"
}, Vn = {};
function mt(n, e) {
  return n instanceof Map ? n.has(e) : Object.prototype.hasOwnProperty.call(n, e);
}
function Tr(n, e) {
  if (e in n) {
    let t = Reflect.getPrototypeOf(n);
    for (; t; ) {
      const r = Reflect.getOwnPropertyDescriptor(t, e);
      if (r)
        return r;
      t = Reflect.getPrototypeOf(t);
    }
  }
}
function Qn(n) {
  return Object.getPrototypeOf(n) === Set.prototype;
}
function Bn(n) {
  return Object.getPrototypeOf(n) === Map.prototype;
}
function _e(n) {
  var e;
  return (e = n.copy) !== null && e !== void 0 ? e : n.original;
}
function qe(n) {
  return !!D(n);
}
function D(n) {
  return typeof n != "object" ? null : n == null ? void 0 : n[hs];
}
function Gn(n) {
  var e;
  const t = D(n);
  return t ? (e = t.copy) !== null && e !== void 0 ? e : t.original : n;
}
function Oe(n, e) {
  if (!n || typeof n != "object")
    return !1;
  let t;
  return Object.getPrototypeOf(n) === Object.prototype || Array.isArray(n) || n instanceof Map || n instanceof Set || !!(e != null && e.mark) && ((t = e.mark(n, me)) === me.immutable || typeof t == "function");
}
function ps(n, e = []) {
  if (Object.hasOwnProperty.call(n, "key")) {
    const t = n.parent.copy, r = D(je(t, n.key));
    if (r !== null && (r == null ? void 0 : r.original) !== n.original)
      return null;
    const s = n.parent.type === 3, i = s ? Array.from(n.parent.setMap.keys()).indexOf(n.key) : n.key;
    if (!(s && t.size > i || mt(t, i)))
      return null;
    e.push(i);
  }
  if (n.parent)
    return ps(n.parent, e);
  e.reverse();
  try {
    $i(n.copy, e);
  } catch {
    return null;
  }
  return e;
}
function ze(n) {
  return Array.isArray(n) ? 1 : n instanceof Map ? 2 : n instanceof Set ? 3 : 0;
}
function je(n, e) {
  return ze(n) === 2 ? n.get(e) : n[e];
}
function Et(n, e, t) {
  ze(n) === 2 ? n.set(e, t) : n[e] = t;
}
function un(n, e) {
  const t = D(n);
  return (t ? _e(t) : n)[e];
}
function Re(n, e) {
  return n === e ? n !== 0 || 1 / n === 1 / e : n !== n && e !== e;
}
function mn(n) {
  if (n)
    for (; n.finalities.revoke.length > 0; )
      n.finalities.revoke.pop()();
}
function Ne(n, e) {
  return e ? n : [""].concat(n).map((t) => {
    const r = `${t}`;
    return r.indexOf("/") === -1 && r.indexOf("~") === -1 ? r : r.replace(/~/g, "~0").replace(/\//g, "~1");
  }).join("/");
}
function $i(n, e) {
  for (let t = 0; t < e.length - 1; t += 1) {
    const r = e[t];
    if (n = je(ze(n) === 3 ? Array.from(n) : n, r), typeof n != "object")
      throw new Error(`Cannot resolve patch at '${e.join("/")}'.`);
  }
  return n;
}
function Di(n) {
  const e = Object.create(Object.getPrototypeOf(n));
  return Reflect.ownKeys(n).forEach((t) => {
    let r = Reflect.getOwnPropertyDescriptor(n, t);
    if (r.enumerable && r.configurable && r.writable) {
      e[t] = n[t];
      return;
    }
    r.writable || (r.writable = !0, r.configurable = !0), (r.get || r.set) && (r = {
      configurable: !0,
      writable: !0,
      enumerable: r.enumerable,
      value: n[t]
    }), Reflect.defineProperty(e, t, r);
  }), e;
}
const Ui = Object.prototype.propertyIsEnumerable;
function ys(n, e) {
  let t;
  if (Array.isArray(n))
    return Array.prototype.concat.call(n);
  if (n instanceof Set) {
    if (!Qn(n)) {
      const r = Object.getPrototypeOf(n).constructor;
      return new r(n.values());
    }
    return Set.prototype.difference ? Set.prototype.difference.call(n, /* @__PURE__ */ new Set()) : new Set(n.values());
  } else if (n instanceof Map) {
    if (!Bn(n)) {
      const r = Object.getPrototypeOf(n).constructor;
      return new r(n);
    }
    return new Map(n);
  } else if (e != null && e.mark && (t = e.mark(n, me), t !== void 0) && t !== me.mutable) {
    if (t === me.immutable)
      return Di(n);
    if (typeof t == "function") {
      if (e.enablePatches || e.enableAutoFreeze)
        throw new Error("You can't use mark and patches or auto freeze together.");
      return t();
    }
    throw new Error(`Unsupported mark result: ${t}`);
  } else if (typeof n == "object" && Object.getPrototypeOf(n) === Object.prototype) {
    const r = {};
    return Object.keys(n).forEach((s) => {
      r[s] = n[s];
    }), Object.getOwnPropertySymbols(n).forEach((s) => {
      Ui.call(n, s) && (r[s] = n[s]);
    }), r;
  } else
    throw new Error("Please check mark() to ensure that it is a stable marker draftable function.");
}
function ue(n) {
  n.copy || (n.copy = ys(n.original, n.options));
}
function dt(n) {
  if (!Oe(n))
    return Gn(n);
  if (Array.isArray(n))
    return n.map(dt);
  if (n instanceof Map) {
    const t = Array.from(n.entries()).map(([r, s]) => [
      r,
      dt(s)
    ]);
    if (!Bn(n)) {
      const r = Object.getPrototypeOf(n).constructor;
      return new r(t);
    }
    return new Map(t);
  }
  if (n instanceof Set) {
    const t = Array.from(n).map(dt);
    if (!Qn(n)) {
      const r = Object.getPrototypeOf(n).constructor;
      return new r(t);
    }
    return new Set(t);
  }
  const e = Object.create(Object.getPrototypeOf(n));
  for (const t in n)
    e[t] = dt(n[t]);
  return e;
}
function Ut(n) {
  return qe(n) ? dt(n) : n;
}
function Ie(n) {
  var e;
  n.assignedMap = (e = n.assignedMap) !== null && e !== void 0 ? e : /* @__PURE__ */ new Map(), n.operated || (n.operated = !0, n.parent && Ie(n.parent));
}
function Sr() {
  throw new Error("Cannot modify frozen object");
}
function Je(n, e, t, r, s) {
  {
    t = t ?? /* @__PURE__ */ new WeakMap(), r = r ?? [], s = s ?? [];
    const o = t.has(n) ? t.get(n) : n;
    if (r.length > 0) {
      const a = r.indexOf(o);
      if (o && typeof o == "object" && a !== -1)
        throw r[0] === o ? new Error("Forbids circular reference") : new Error(`Forbids circular reference: ~/${s.slice(0, a).map((c, u) => {
          if (typeof c == "symbol")
            return `[${c.toString()}]`;
          const l = r[u];
          return typeof c == "object" && (l instanceof Map || l instanceof Set) ? Array.from(l.keys()).indexOf(c) : c;
        }).join("/")}`);
      r.push(o), s.push(e);
    } else
      r.push(o);
  }
  if (Object.isFrozen(n) || qe(n)) {
    r.pop(), s.pop();
    return;
  }
  switch (ze(n)) {
    case 2:
      for (const [a, c] of n)
        Je(a, a, t, r, s), Je(c, a, t, r, s);
      n.set = n.clear = n.delete = Sr;
      break;
    case 3:
      for (const a of n)
        Je(a, a, t, r, s);
      n.add = n.clear = n.delete = Sr;
      break;
    case 1:
      Object.freeze(n);
      let o = 0;
      for (const a of n)
        Je(a, o, t, r, s), o += 1;
      break;
    default:
      Object.freeze(n), Object.keys(n).forEach((a) => {
        const c = n[a];
        Je(c, a, t, r, s);
      });
  }
  r.pop(), s.pop();
}
function Hn(n, e) {
  const t = ze(n);
  if (t === 0)
    Reflect.ownKeys(n).forEach((r) => {
      e(r, n[r], n);
    });
  else if (t === 1) {
    let r = 0;
    for (const s of n)
      e(r, s, n), r += 1;
  } else
    n.forEach((r, s) => e(s, r, n));
}
function bs(n, e, t) {
  if (qe(n) || !Oe(n, t) || e.has(n) || Object.isFrozen(n))
    return;
  const r = n instanceof Set, s = r ? /* @__PURE__ */ new Map() : void 0;
  if (e.add(n), Hn(n, (i, o) => {
    var a;
    if (qe(o)) {
      const c = D(o);
      ue(c);
      const u = !((a = c.assignedMap) === null || a === void 0) && a.size || c.operated ? c.copy : c.original;
      Et(r ? s : n, i, u);
    } else
      bs(o, e, t);
  }), s) {
    const i = n, o = Array.from(i);
    i.clear(), o.forEach((a) => {
      i.add(s.has(a) ? s.get(a) : a);
    });
  }
}
function Li(n, e) {
  const t = n.type === 3 ? n.setMap : n.copy;
  n.finalities.revoke.length > 1 && n.assignedMap.get(e) && t && bs(je(t, e), n.finalities.handledSet, n.options);
}
function wn(n) {
  n.type === 3 && n.copy && (n.copy.clear(), n.setMap.forEach((e) => {
    n.copy.add(Gn(e));
  }));
}
function Tn(n, e, t, r) {
  if (n.operated && n.assignedMap && n.assignedMap.size > 0 && !n.finalized) {
    if (t && r) {
      const i = ps(n);
      i && e(n, i, t, r);
    }
    n.finalized = !0;
  }
}
function Jn(n, e, t, r) {
  const s = D(t);
  s && (s.callbacks || (s.callbacks = []), s.callbacks.push((i, o) => {
    var a;
    const c = n.type === 3 ? n.setMap : n.copy;
    if (Re(je(c, e), t)) {
      let u = s.original;
      s.copy && (u = s.copy), wn(n), Tn(n, r, i, o), n.options.enableAutoFreeze && (n.options.updatedValues = (a = n.options.updatedValues) !== null && a !== void 0 ? a : /* @__PURE__ */ new WeakMap(), n.options.updatedValues.set(u, s.original)), Et(c, e, u);
    }
  }), n.options.enableAutoFreeze && s.finalities !== n.finalities && (n.options.enableAutoFreeze = !1)), Oe(t, n.options) && n.finalities.draft.push(() => {
    const i = n.type === 3 ? n.setMap : n.copy;
    Re(je(i, e), t) && Li(n, e);
  });
}
function Fi(n, e, t, r, s) {
  let { original: i, assignedMap: o, options: a } = n, c = n.copy;
  c.length < i.length && ([i, c] = [c, i], [t, r] = [r, t]);
  for (let u = 0; u < i.length; u += 1)
    if (o.get(u.toString()) && c[u] !== i[u]) {
      const l = e.concat([u]), f = Ne(l, s);
      t.push({
        op: Z.Replace,
        path: f,
        // If it is a draft, it needs to be deep cloned, and it may also be non-draft.
        value: Ut(c[u])
      }), r.push({
        op: Z.Replace,
        path: f,
        // If it is a draft, it needs to be deep cloned, and it may also be non-draft.
        value: Ut(i[u])
      });
    }
  for (let u = i.length; u < c.length; u += 1) {
    const l = e.concat([u]), f = Ne(l, s);
    t.push({
      op: Z.Add,
      path: f,
      // If it is a draft, it needs to be deep cloned, and it may also be non-draft.
      value: Ut(c[u])
    });
  }
  if (i.length < c.length) {
    const { arrayLengthAssignment: u = !0 } = a.enablePatches;
    if (u) {
      const l = e.concat(["length"]), f = Ne(l, s);
      r.push({
        op: Z.Replace,
        path: f,
        value: i.length
      });
    } else
      for (let l = c.length; i.length < l; l -= 1) {
        const f = e.concat([l - 1]), d = Ne(f, s);
        r.push({
          op: Z.Remove,
          path: d
        });
      }
  }
}
function Ni({ original: n, copy: e, assignedMap: t }, r, s, i, o) {
  t.forEach((a, c) => {
    const u = je(n, c), l = Ut(je(e, c)), f = a ? mt(n, c) ? Z.Replace : Z.Add : Z.Remove;
    if (Re(u, l) && f === Z.Replace)
      return;
    const d = r.concat(c), y = Ne(d, o);
    s.push(f === Z.Remove ? { op: f, path: y } : { op: f, path: y, value: l }), i.push(f === Z.Add ? { op: Z.Remove, path: y } : f === Z.Remove ? { op: Z.Add, path: y, value: u } : { op: Z.Replace, path: y, value: u });
  });
}
function Ki({ original: n, copy: e }, t, r, s, i) {
  let o = 0;
  n.forEach((a) => {
    if (!e.has(a)) {
      const c = t.concat([o]), u = Ne(c, i);
      r.push({
        op: Z.Remove,
        path: u,
        value: a
      }), s.unshift({
        op: Z.Add,
        path: u,
        value: a
      });
    }
    o += 1;
  }), o = 0, e.forEach((a) => {
    if (!n.has(a)) {
      const c = t.concat([o]), u = Ne(c, i);
      r.push({
        op: Z.Add,
        path: u,
        value: a
      }), s.unshift({
        op: Z.Remove,
        path: u,
        value: a
      });
    }
    o += 1;
  });
}
function vt(n, e, t, r) {
  const { pathAsArray: s = !0 } = n.options.enablePatches;
  switch (n.type) {
    case 0:
    case 2:
      return Ni(n, e, t, r, s);
    case 1:
      return Fi(n, e, t, r, s);
    case 3:
      return Ki(n, e, t, r, s);
  }
}
const zt = (n, e, t = !1) => {
  if (typeof n == "object" && n !== null && (!Oe(n, e) || t))
    throw new Error("Strict mode: Mutable data cannot be accessed directly, please use 'unsafe(callback)' wrap.");
}, Sn = {
  get size() {
    return _e(D(this)).size;
  },
  has(n) {
    return _e(D(this)).has(n);
  },
  set(n, e) {
    const t = D(this), r = _e(t);
    return (!r.has(n) || !Re(r.get(n), e)) && (ue(t), Ie(t), t.assignedMap.set(n, !0), t.copy.set(n, e), Jn(t, n, e, vt)), this;
  },
  delete(n) {
    if (!this.has(n))
      return !1;
    const e = D(this);
    return ue(e), Ie(e), e.original.has(n) ? e.assignedMap.set(n, !1) : e.assignedMap.delete(n), e.copy.delete(n), !0;
  },
  clear() {
    const n = D(this);
    if (this.size) {
      ue(n), Ie(n), n.assignedMap = /* @__PURE__ */ new Map();
      for (const [e] of n.original)
        n.assignedMap.set(e, !1);
      n.copy.clear();
    }
  },
  forEach(n, e) {
    const t = D(this);
    _e(t).forEach((r, s) => {
      n.call(e, this.get(s), s, this);
    });
  },
  get(n) {
    var e, t;
    const r = D(this), s = _e(r).get(n), i = ((t = (e = r.options).mark) === null || t === void 0 ? void 0 : t.call(e, s, me)) === me.mutable;
    if (r.options.strict && zt(s, r.options, i), i || r.finalized || !Oe(s, r.options) || s !== r.original.get(n))
      return s;
    const o = Vn.createDraft({
      original: s,
      parentDraft: r,
      key: n,
      finalities: r.finalities,
      options: r.options
    });
    return ue(r), r.copy.set(n, o), o;
  },
  keys() {
    return _e(D(this)).keys();
  },
  values() {
    const n = this.keys();
    return {
      [Dt]: () => this.values(),
      next: () => {
        const e = n.next();
        return e.done ? e : {
          done: !1,
          value: this.get(e.value)
        };
      }
    };
  },
  entries() {
    const n = this.keys();
    return {
      [Dt]: () => this.entries(),
      next: () => {
        const e = n.next();
        if (e.done)
          return e;
        const t = this.get(e.value);
        return {
          done: !1,
          value: [e.value, t]
        };
      }
    };
  },
  [Dt]() {
    return this.entries();
  }
}, Wi = Reflect.ownKeys(Sn), vr = (n, e, { isValuesIterator: t }) => () => {
  var r, s;
  const i = e.next();
  if (i.done)
    return i;
  const o = i.value;
  let a = n.setMap.get(o);
  const c = D(a), u = ((s = (r = n.options).mark) === null || s === void 0 ? void 0 : s.call(r, a, me)) === me.mutable;
  if (n.options.strict && zt(o, n.options, u), !u && !c && Oe(o, n.options) && !n.finalized && n.original.has(o)) {
    const l = Vn.createDraft({
      original: o,
      parentDraft: n,
      key: o,
      finalities: n.finalities,
      options: n.options
    });
    n.setMap.set(o, l), a = l;
  } else c && (a = c.proxy);
  return {
    done: !1,
    value: t ? a : [a, a]
  };
}, Vt = {
  get size() {
    return D(this).setMap.size;
  },
  has(n) {
    const e = D(this);
    if (e.setMap.has(n))
      return !0;
    ue(e);
    const t = D(n);
    return !!(t && e.setMap.has(t.original));
  },
  add(n) {
    const e = D(this);
    return this.has(n) || (ue(e), Ie(e), e.assignedMap.set(n, !0), e.setMap.set(n, n), Jn(e, n, n, vt)), this;
  },
  delete(n) {
    if (!this.has(n))
      return !1;
    const e = D(this);
    ue(e), Ie(e);
    const t = D(n);
    return t && e.setMap.has(t.original) ? (e.assignedMap.set(t.original, !1), e.setMap.delete(t.original)) : (!t && e.setMap.has(n) ? e.assignedMap.set(n, !1) : e.assignedMap.delete(n), e.setMap.delete(n));
  },
  clear() {
    if (!this.size)
      return;
    const n = D(this);
    ue(n), Ie(n);
    for (const e of n.original)
      n.assignedMap.set(e, !1);
    n.setMap.clear();
  },
  values() {
    const n = D(this);
    ue(n);
    const e = n.setMap.keys();
    return {
      [Symbol.iterator]: () => this.values(),
      next: vr(n, e, { isValuesIterator: !0 })
    };
  },
  entries() {
    const n = D(this);
    ue(n);
    const e = n.setMap.keys();
    return {
      [Symbol.iterator]: () => this.entries(),
      next: vr(n, e, {
        isValuesIterator: !1
      })
    };
  },
  keys() {
    return this.values();
  },
  [Dt]() {
    return this.values();
  },
  forEach(n, e) {
    const t = this.values();
    let r = t.next();
    for (; !r.done; )
      n.call(e, r.value, r.value, this), r = t.next();
  }
};
Set.prototype.difference && Object.assign(Vt, {
  intersection(n) {
    return Set.prototype.intersection.call(new Set(this.values()), n);
  },
  union(n) {
    return Set.prototype.union.call(new Set(this.values()), n);
  },
  difference(n) {
    return Set.prototype.difference.call(new Set(this.values()), n);
  },
  symmetricDifference(n) {
    return Set.prototype.symmetricDifference.call(new Set(this.values()), n);
  },
  isSubsetOf(n) {
    return Set.prototype.isSubsetOf.call(new Set(this.values()), n);
  },
  isSupersetOf(n) {
    return Set.prototype.isSupersetOf.call(new Set(this.values()), n);
  },
  isDisjointFrom(n) {
    return Set.prototype.isDisjointFrom.call(new Set(this.values()), n);
  }
});
const qi = Reflect.ownKeys(Vt), gs = /* @__PURE__ */ new WeakSet(), _s = {
  get(n, e, t) {
    var r, s;
    const i = (r = n.copy) === null || r === void 0 ? void 0 : r[e];
    if (i && gs.has(i))
      return i;
    if (e === hs)
      return n;
    let o;
    if (n.options.mark) {
      const u = e === "size" && (n.original instanceof Map || n.original instanceof Set) ? Reflect.get(n.original, e) : Reflect.get(n.original, e, t);
      if (o = n.options.mark(u, me), o === me.mutable)
        return n.options.strict && zt(u, n.options, !0), u;
    }
    const a = _e(n);
    if (a instanceof Map && Wi.includes(e)) {
      if (e === "size")
        return Object.getOwnPropertyDescriptor(Sn, "size").get.call(n.proxy);
      const u = Sn[e];
      if (u)
        return u.bind(n.proxy);
    }
    if (a instanceof Set && qi.includes(e)) {
      if (e === "size")
        return Object.getOwnPropertyDescriptor(Vt, "size").get.call(n.proxy);
      const u = Vt[e];
      if (u)
        return u.bind(n.proxy);
    }
    if (!mt(a, e)) {
      const u = Tr(a, e);
      return u ? "value" in u ? u.value : (
        // !case: support for getter
        (s = u.get) === null || s === void 0 ? void 0 : s.call(n.proxy)
      ) : void 0;
    }
    const c = a[e];
    if (n.options.strict && zt(c, n.options), n.finalized || !Oe(c, n.options))
      return c;
    if (c === un(n.original, e)) {
      if (ue(n), n.copy[e] = Yn({
        original: n.original[e],
        parentDraft: n,
        key: n.type === 1 ? Number(e) : e,
        finalities: n.finalities,
        options: n.options
      }), typeof o == "function") {
        const u = D(n.copy[e]);
        return ue(u), Ie(u), u.copy;
      }
      return n.copy[e];
    }
    return c;
  },
  set(n, e, t) {
    var r;
    if (n.type === 3 || n.type === 2)
      throw new Error("Map/Set draft does not support any property assignment.");
    let s;
    if (n.type === 1 && e !== "length" && !(Number.isInteger(s = Number(e)) && s >= 0 && (e === 0 || s === 0 || String(s) === String(e))))
      throw new Error("Only supports setting array indices and the 'length' property.");
    const i = Tr(_e(n), e);
    if (i != null && i.set)
      return i.set.call(n.proxy, t), !0;
    const o = un(_e(n), e), a = D(o);
    return a && Re(a.original, t) ? (n.copy[e] = t, n.assignedMap = (r = n.assignedMap) !== null && r !== void 0 ? r : /* @__PURE__ */ new Map(), n.assignedMap.set(e, !1), !0) : (Re(t, o) && (t !== void 0 || mt(n.original, e)) || (ue(n), Ie(n), mt(n.original, e) && Re(t, n.original[e]) ? n.assignedMap.delete(e) : n.assignedMap.set(e, !0), n.copy[e] = t, Jn(n, e, t, vt)), !0);
  },
  has(n, e) {
    return e in _e(n);
  },
  ownKeys(n) {
    return Reflect.ownKeys(_e(n));
  },
  getOwnPropertyDescriptor(n, e) {
    const t = _e(n), r = Reflect.getOwnPropertyDescriptor(t, e);
    return r && {
      writable: !0,
      configurable: n.type !== 1 || e !== "length",
      enumerable: r.enumerable,
      value: t[e]
    };
  },
  getPrototypeOf(n) {
    return Reflect.getPrototypeOf(n.original);
  },
  setPrototypeOf() {
    throw new Error("Cannot call 'setPrototypeOf()' on drafts");
  },
  defineProperty() {
    throw new Error("Cannot call 'defineProperty()' on drafts");
  },
  deleteProperty(n, e) {
    var t;
    return n.type === 1 ? _s.set.call(this, n, e, void 0, n.proxy) : (un(n.original, e) !== void 0 || e in n.original ? (ue(n), Ie(n), n.assignedMap.set(e, !1)) : (n.assignedMap = (t = n.assignedMap) !== null && t !== void 0 ? t : /* @__PURE__ */ new Map(), n.assignedMap.delete(e)), n.copy && delete n.copy[e], !0);
  }
};
function Yn(n) {
  const { original: e, parentDraft: t, key: r, finalities: s, options: i } = n, o = ze(e), a = {
    type: o,
    finalized: !1,
    parent: t,
    original: e,
    copy: null,
    proxy: null,
    finalities: s,
    options: i,
    // Mapping of draft Set items to their corresponding draft values.
    setMap: o === 3 ? new Map(e.entries()) : void 0
  };
  (r || "key" in n) && (a.key = r);
  const { proxy: c, revoke: u } = Proxy.revocable(o === 1 ? Object.assign([], a) : a, _s);
  if (s.revoke.push(u), gs.add(c), a.proxy = c, t) {
    const l = t;
    l.finalities.draft.push((f, d) => {
      var y, b;
      const g = D(c);
      let w = l.type === 3 ? l.setMap : l.copy;
      const S = je(w, r), _ = D(S);
      if (_) {
        let m = _.original;
        _.operated && (m = Gn(S)), wn(_), Tn(_, vt, f, d), l.options.enableAutoFreeze && (l.options.updatedValues = (y = l.options.updatedValues) !== null && y !== void 0 ? y : /* @__PURE__ */ new WeakMap(), l.options.updatedValues.set(m, _.original)), Et(w, r, m);
      }
      (b = g.callbacks) === null || b === void 0 || b.forEach((m) => {
        m(f, d);
      });
    });
  } else {
    const l = D(c);
    l.finalities.draft.push((f, d) => {
      wn(l), Tn(l, vt, f, d);
    });
  }
  return c;
}
Vn.createDraft = Yn;
function zi(n, e, t, r, s) {
  var i;
  const o = D(n), a = (i = o == null ? void 0 : o.original) !== null && i !== void 0 ? i : n, c = !!e.length;
  if (o != null && o.operated)
    for (; o.finalities.draft.length > 0; )
      o.finalities.draft.pop()(t, r);
  const u = c ? e[0] : o ? o.operated ? o.copy : o.original : n;
  return o && mn(o), s && Je(u, u, o == null ? void 0 : o.options.updatedValues), [
    u,
    t && c ? [{ op: Z.Replace, path: [], value: e[0] }] : t,
    r && c ? [{ op: Z.Replace, path: [], value: a }] : r
  ];
}
function Vi(n, e) {
  var t;
  const r = {
    draft: [],
    revoke: [],
    handledSet: /* @__PURE__ */ new WeakSet()
  };
  let s, i;
  e.enablePatches && (s = [], i = []);
  const a = ((t = e.mark) === null || t === void 0 ? void 0 : t.call(e, n, me)) === me.mutable || !Oe(n, e) ? n : Yn({
    original: n,
    parentDraft: null,
    finalities: r,
    options: e
  });
  return [
    a,
    (c = []) => {
      const [u, l, f] = zi(a, c, s, i, e.enableAutoFreeze);
      return e.enablePatches ? [u, l, f] : u;
    }
  ];
}
function vn(n) {
  const { rootDraft: e, value: t, useRawReturn: r = !1, isRoot: s = !0 } = n;
  Hn(t, (i, o, a) => {
    const c = D(o);
    if (c && e && c.finalities === e.finalities) {
      n.isContainDraft = !0;
      const u = c.original;
      if (a instanceof Set) {
        const l = Array.from(a);
        a.clear(), l.forEach((f) => a.add(i === f ? u : f));
      } else
        Et(a, i, u);
    } else typeof o == "object" && o !== null && (n.value = o, n.isRoot = !1, vn(n));
  }), s && (n.isContainDraft || console.warn("The return value does not contain any draft, please use 'rawReturn()' to wrap the return value to improve performance."), r && console.warn("The return value contains drafts, please don't use 'rawReturn()' to wrap the return value."));
}
function ms(n) {
  var e;
  const t = D(n);
  if (!Oe(n, t == null ? void 0 : t.options))
    return n;
  const r = ze(n);
  if (t && !t.operated)
    return t.original;
  let s;
  function i() {
    s = r === 2 ? Bn(n) ? new Map(n) : new (Object.getPrototypeOf(n)).constructor(n) : r === 3 ? Array.from(t.setMap.values()) : ys(n, t == null ? void 0 : t.options);
  }
  if (t) {
    t.finalized = !0;
    try {
      i();
    } finally {
      t.finalized = !1;
    }
  } else
    s = n;
  if (Hn(s, (o, a) => {
    if (t && Re(je(t.original, o), a))
      return;
    const c = ms(a);
    c !== a && (s === n && i(), Et(s, o, c));
  }), r === 3) {
    const o = (e = t == null ? void 0 : t.original) !== null && e !== void 0 ? e : s;
    return Qn(o) ? new Set(s) : new (Object.getPrototypeOf(o)).constructor(s);
  }
  return s;
}
function Er(n) {
  if (!qe(n))
    throw new Error(`current() is only used for Draft, parameter: ${n}`);
  return ms(n);
}
const Qi = (n) => function e(t, r, s) {
  var i, o, a;
  if (typeof t == "function" && typeof r != "function")
    return function(v, ...I) {
      return e(v, (j) => t.call(this, j, ...I), r);
    };
  const c = t, u = r;
  let l = s;
  if (typeof r != "function" && (l = r), l !== void 0 && Object.prototype.toString.call(l) !== "[object Object]")
    throw new Error(`Invalid options: ${l}, 'options' should be an object.`);
  l = Object.assign(Object.assign({}, n), l);
  const f = qe(c) ? Er(c) : c, d = Array.isArray(l.mark) ? (v, I) => {
    for (const j of l.mark) {
      if (typeof j != "function")
        throw new Error(`Invalid mark: ${j}, 'mark' should be a function.`);
      const N = j(v, I);
      if (N)
        return N;
    }
  } : l.mark, y = (i = l.enablePatches) !== null && i !== void 0 ? i : !1, b = (o = l.strict) !== null && o !== void 0 ? o : !1, w = {
    enableAutoFreeze: (a = l.enableAutoFreeze) !== null && a !== void 0 ? a : !1,
    mark: d,
    strict: b,
    enablePatches: y
  };
  if (!Oe(f, w) && typeof f == "object" && f !== null)
    throw new Error("Invalid base state: create() only supports plain objects, arrays, Set, Map or using mark() to mark the state as immutable.");
  const [S, _] = Vi(f, w);
  if (typeof r != "function") {
    if (!Oe(f, w))
      throw new Error("Invalid base state: create() only supports plain objects, arrays, Set, Map or using mark() to mark the state as immutable.");
    return [S, _];
  }
  let m;
  try {
    m = u(S);
  } catch (v) {
    throw mn(D(S)), v;
  }
  const A = (v) => {
    const I = D(S);
    if (!qe(v)) {
      if (v !== void 0 && !Re(v, S) && (I != null && I.operated))
        throw new Error("Either the value is returned as a new non-draft value, or only the draft is modified without returning any value.");
      const N = v == null ? void 0 : v[xi];
      if (N) {
        const Q = N[0];
        return w.strict && typeof v == "object" && v !== null && vn({
          rootDraft: I,
          value: v,
          useRawReturn: !0
        }), _([Q]);
      }
      if (v !== void 0)
        return typeof v == "object" && v !== null && vn({ rootDraft: I, value: v }), _([v]);
    }
    if (v === S || v === void 0)
      return _([]);
    const j = D(v);
    if (w === j.options) {
      if (j.operated)
        throw new Error("Cannot return a modified child draft.");
      return _([Er(v)]);
    }
    return _([v]);
  };
  return m instanceof Promise ? m.then(A, (v) => {
    throw mn(D(S)), v;
  }) : A(m);
}, Qt = Qi();
Object.prototype.constructor.toString();
function ws(n, e) {
  const t = Object.keys(n), r = Object.keys(e);
  return t.length === r.length && Object.keys(n).every((s) => e.hasOwnProperty(s));
}
function Or(n, e) {
  return Object.keys(n).length === Object.keys(e).length && Object.keys(n).every((t) => e.hasOwnProperty(t) && n[t] === e[t]);
}
function Lt(n, e) {
  return typeof n != "object" || typeof e != "object" || n === null || e === null ? n === e : ws(n, e) ? Object.keys(n).every((t) => Lt(n[t], e[t])) : !1;
}
function Zn(n) {
  if (!ht(n))
    return n;
  const e = {};
  for (const [t, r] of Object.entries(n))
    r !== void 0 && (e[t] = r);
  return e;
}
function Ts(n, e) {
  if (!ht(n) || !ht(e))
    return e;
  const t = { ...n };
  for (const r of Object.keys(e)) {
    if (e[r] === void 0)
      continue;
    if (e[r] === null) {
      delete t[r];
      continue;
    }
    const s = ht(n[r]) && ht(e[r]);
    t[r] = s ? Ts(n[r], e[r]) : e[r];
  }
  return t;
}
function ht(n) {
  return typeof n == "object" && n !== null && !Array.isArray(n);
}
function Bi(n, e, t) {
  if (!n || e.length === 0)
    return;
  let r = n || {};
  for (let i = 0; i < e.length - 1; i++) {
    const o = e[i];
    (!(o in r) || typeof r[o] != "object") && (r[o] = typeof e[i + 1] == "number" ? [] : {}), r = r[o];
  }
  const s = e[e.length - 1];
  Array.isArray(r) && typeof s == "number" ? r.splice(s, 0, t) : r[s] = t;
}
function kr(n, e, t) {
  if (!n || e.length === 0)
    return;
  let r = n || {};
  for (let s = 0; s < e.length - 1; s++) {
    const i = e[s];
    (!(i in r) || typeof r[i] != "object") && (r[i] = typeof e[s + 1] == "number" ? [] : {}), r = r[i];
  }
  r[e[e.length - 1]] = t;
}
function Ss(n, e) {
  if (!n || e.length === 0)
    return;
  const [t, ...r] = e;
  if (t in n) {
    if (r.length === 0) {
      Array.isArray(n) ? n.splice(t, 1) : delete n[t];
      return;
    }
    Ss(n[t], r), Gi(n[t]) && delete n[t];
  }
}
function Gi(n) {
  return n && Object.keys(n).length === 0;
}
const Ar = /ZULU|YEKT|YEKST|YAPT|YAKT|YAKST|XJT|WGT|WGST|WFT|WETDST|WET|WDT|WAT|WAST|WAKT|WADT|VUT|VOLT|VLAT|VLAST|VET|UZT|UZST|UYT|UYST|UTC|UT|ULAT|ULAST|UCT|TVT|TRUT|TOT|TMT|TKT|TJT|TFT|TAHT|SGT|SCT|SAST|SADT|RET|PYT|PYST|PWT|PST|PONT|PMST|PMDT|PKT|PKST|PHT|PGT|PETT|PETST|PET|PDT|OMST|OMSST|NZT|NZST|NZDT|NUT|NST|NPT|NOVT|NOVST|NFT|NDT|MYT|MVT|MUT|MUST|MST|MSK|MSD|MPT|MMT|MHT|MEZ|METDST|MET|MESZ|MEST|MDT|MAWT|MART|MAGT|MAGST|LKT|LINT|LIGT|LHST|LHDT|KST|KRAT|KRAST|KOST|KGT|KGST|KDT|JST|JAYT|IST|IRT|IRKT|IRKST|IOT|IDT|ICT|HST|HKT|GYT|GMT|GILT|GFT|GET|GEST|GAMT|GALT|FNT|FNST|FKT|FKST|FJT|FJST|FET|EST|EGT|EGST|EETDST|EET|EEST|EDT|EAT|EAST|EASST|DDUT|DAVT|CXT|CST|COT|CLT|CLST|CKT|CHUT|CHAST|CHADT|CETDST|CET|CEST|CDT|CCT|CAST|CADT|BTT|BST|BRT|BRST|BRA|BOT|BORT|BNT|BDT|BDST|AZT|AZST|AZOT|AZOST|AWST|AWSST|AST|ART|ARST|ANAT|ANAST|AMT|AMST|ALMT|ALMST|AKST|AKDT|AFT|AEST|AESST|AEDT|ADT|ACWST|ACT|ACST|ACSST|ACDT$/, Hi = {
  ZULU: 0,
  YEKT: 18e3,
  YEKST: 21600,
  YAPT: 36e3,
  YAKT: 32400,
  YAKST: 32400,
  XJT: 21600,
  WGT: -10800,
  WGST: -7200,
  WFT: 43200,
  WETDST: 3600,
  WET: 0,
  WDT: 32400,
  WAT: 3600,
  WAST: 25200,
  WAKT: 43200,
  WADT: 28800,
  VUT: 39600,
  VOLT: 10800,
  VLAT: 36e3,
  VLAST: 36e3,
  VET: -14400,
  UZT: 18e3,
  UZST: 21600,
  UYT: -10800,
  UYST: -7200,
  UTC: 0,
  UT: 0,
  ULAT: 28800,
  ULAST: 32400,
  UCT: 0,
  TVT: 43200,
  TRUT: 36e3,
  TOT: 46800,
  TMT: 18e3,
  TKT: 46800,
  TJT: 18e3,
  TFT: 18e3,
  TAHT: -36e3,
  SGT: 28800,
  SCT: 14400,
  SAST: 7200,
  SADT: 37800,
  RET: 14400,
  PYT: -14400,
  PYST: -10800,
  PWT: 32400,
  PST: -28800,
  PONT: 39600,
  PMST: -10800,
  PMDT: -7200,
  PKT: 18e3,
  PKST: 21600,
  PHT: 28800,
  PGT: 36e3,
  PETT: 43200,
  PETST: 43200,
  PET: -18e3,
  PDT: -25200,
  OMST: 21600,
  OMSST: 21600,
  NZT: 43200,
  NZST: 43200,
  NZDT: 46800,
  NUT: -39600,
  NST: -12600,
  NPT: 20700,
  NOVT: 25200,
  NOVST: 25200,
  NFT: -12600,
  NDT: -9e3,
  MYT: 28800,
  MVT: 18e3,
  MUT: 14400,
  MUST: 18e3,
  MST: -25200,
  MSK: 10800,
  MSD: 14400,
  MPT: 36e3,
  MMT: 23400,
  MHT: 43200,
  MEZ: 3600,
  METDST: 7200,
  MET: 3600,
  MESZ: 7200,
  MEST: 7200,
  MDT: -21600,
  MAWT: 18e3,
  MART: -34200,
  MAGT: 39600,
  MAGST: 39600,
  LKT: 19800,
  LINT: 50400,
  LIGT: 36e3,
  LHST: 37800,
  LHDT: 37800,
  KST: 32400,
  KRAT: 25200,
  KRAST: 25200,
  KOST: 39600,
  KGT: 21600,
  KGST: 21600,
  KDT: 36e3,
  JST: 32400,
  JAYT: 32400,
  IST: 7200,
  IRT: 12600,
  IRKT: 28800,
  IRKST: 28800,
  IOT: 21600,
  IDT: 10800,
  ICT: 25200,
  HST: -36e3,
  HKT: 28800,
  GYT: -14400,
  GMT: 0,
  GILT: 43200,
  GFT: -10800,
  GET: 14400,
  GEST: 14400,
  GAMT: -32400,
  GALT: -21600,
  FNT: -7200,
  FNST: -3600,
  FKT: -10800,
  FKST: -10800,
  FJT: 43200,
  FJST: 46800,
  FET: 10800,
  EST: -18e3,
  EGT: -3600,
  EGST: 0,
  EETDST: 10800,
  EET: 7200,
  EEST: 10800,
  EDT: -14400,
  EAT: 10800,
  EAST: -21600,
  EASST: -21600,
  DDUT: 36e3,
  DAVT: 25200,
  CXT: 25200,
  CST: -21600,
  COT: -18e3,
  CLT: -14400,
  CLST: -10800,
  CKT: -36e3,
  CHUT: 36e3,
  CHAST: 45900,
  CHADT: 49500,
  CETDST: 7200,
  CET: 3600,
  CEST: 7200,
  CDT: -18e3,
  CCT: 28800,
  CAST: 34200,
  CADT: 37800,
  BTT: 21600,
  BST: 3600,
  BRT: -10800,
  BRST: -7200,
  BRA: -10800,
  BOT: -14400,
  BORT: 28800,
  BNT: 28800,
  BDT: 21600,
  BDST: 7200,
  AZT: 14400,
  AZST: 14400,
  AZOT: -3600,
  AZOST: 0,
  AWST: 28800,
  AWSST: 32400,
  AST: -14400,
  ART: -10800,
  ARST: -10800,
  ANAT: 43200,
  ANAST: 43200,
  AMT: -14400,
  AMST: 14400,
  ALMT: 21600,
  ALMST: 25200,
  AKST: -32400,
  AKDT: -28800,
  AFT: 16200,
  AEST: 36e3,
  AESST: 39600,
  AEDT: 39600,
  ADT: -10800,
  ACWST: 31500,
  ACT: -18e3,
  ACST: 34200,
  ACSST: 37800,
  ACDT: 37800
};
function Ji(n) {
  return new Date(n);
}
function Yi(n) {
  return /* @__PURE__ */ new Date(n + "Z");
}
const Zi = /^(\d+)[\./-](\d+)[\./-](\d+)$/;
function Xi(n) {
  const e = n.match(Zi);
  if (!e)
    return null;
  const [t, r, s, i] = e;
  return r <= 0 || s <= 0 || i <= 0 ? null : r > 999 ? new Date(Date.UTC(r, s - 1, i, 0, 0, 0, 0)) : new Date(Date.UTC(i, r - 1, s, 0, 0, 0, 0));
}
function eo(n) {
  const [e, t] = n.split(" ");
  return /* @__PURE__ */ new Date(e + "T" + t + "Z");
}
function to(n) {
  const [e, t] = n.split(" ");
  return /* @__PURE__ */ new Date(e + "T" + t + "Z");
}
function no(n) {
  return new Date(n);
}
function ro(n) {
  const e = /^(\w{3}) (\w{3}) (\d{2}) (\d{4})$/;
  if (!n.match(e))
    throw new Error(`Unable to parse \`${n}\` as a date.`);
  const r = /* @__PURE__ */ new Date(n + " UTC");
  return new Date(Date.UTC(r.getUTCFullYear(), r.getUTCMonth(), r.getUTCDate(), 0, 0, 0, 0));
}
function so(n) {
  const e = /^(.+T.+)([+-])(\d{2})$/, t = n.match(e);
  if (t) {
    const [, r, s, i] = t, o = `${r}${s}${i}:00`;
    return new Date(o);
  }
  return null;
}
function io(n) {
  const e = /^(\d+)-(\d{1,2})-(\d{1,2})([ T])(.+)$/, t = n.match(e);
  if (t) {
    const [, r, s, i, o, a] = t, c = s.padStart(2, "0"), u = i.padStart(2, "0"), l = `${r}-${c}-${u}T${a}`;
    return new Date(l);
  }
  return null;
}
function oo(n) {
  const [e, t] = n.split(", "), [r, s, i] = e.split("/").map(Number), o = t.match(/(\d{1,2}):(\d{2}):(\d{2}) (AM|PM)/);
  if (!o)
    throw new Error(`Unable to parse time from: ${n}`);
  let [, a, c, u, l] = o;
  return a = Number(a), c = Number(c), u = Number(u), l === "PM" && a !== 12 ? a += 12 : l === "AM" && a === 12 && (a = 0), new Date(Date.UTC(i, r - 1, s, a, c, u));
}
function ao(n) {
  switch (n) {
    case "epoch":
      return /* @__PURE__ */ new Date(0);
    case "infinity":
    case "-infinity":
    case "today":
    case "tomorrow":
    case "yesterday":
      return null;
  }
}
function co(n) {
  const e = n.match(Ar);
  if (!e)
    return null;
  const [t] = e, r = Hi[t], s = new Date(n.replace(Ar, "Z"));
  return new Date(s.getTime() - r * 1e3);
}
const uo = [
  Xi,
  to,
  ro,
  oo,
  no,
  Yi,
  so,
  eo,
  Ji,
  ao,
  co,
  io
];
function lo(n, e) {
  try {
    const t = n(e);
    return t instanceof Date && !isNaN(t.getTime()) ? t : null;
  } catch {
    return null;
  }
}
function En(n) {
  for (const e of uo) {
    const t = lo(e, n);
    if (t)
      return t;
  }
  return null;
}
function fo(n) {
  try {
    const e = JSON.parse(n);
    return typeof e == "string" ? En(e) : null;
  } catch {
    return null;
  }
}
function Jt(n) {
  if (n !== void 0) {
    if (n === null)
      return null;
    if (n instanceof Date)
      return n;
    if (typeof n == "string") {
      const e = En(n) || fo(n) || En(n.trim());
      if (!e)
        throw new Error(`Unable to parse \`${n}\` as a date.`);
      return e;
    } else if (typeof n == "number")
      return new Date(n);
    throw new Error(`Invalid date value \`${n}\`. Expected a date, number, or string, got type ${typeof n}.`);
  }
}
class Ke {
  constructor(e, t) {
    p(this, "attrs");
    p(this, "linkIndex");
    p(this, "_blobAttrs", null);
    p(this, "_primaryKeys", null);
    p(this, "_forwardIdents", null);
    p(this, "_revIdents", null);
    this.attrs = e, this.linkIndex = t;
  }
  resetAttrIndexes() {
    this._blobAttrs = null, this._primaryKeys = null, this._forwardIdents = null, this._revIdents = null;
  }
  addAttr(e) {
    this.attrs[e.id] = e, this.resetAttrIndexes();
  }
  deleteAttr(e) {
    delete this.attrs[e], this.resetAttrIndexes();
  }
  updateAttr(e) {
    const t = this.attrs[e.id];
    t && (this.attrs[e.id] = { ...t, ...e }, this.resetAttrIndexes());
  }
  getAttr(e) {
    return this.attrs[e];
  }
  get blobAttrs() {
    if (this._blobAttrs)
      return this._blobAttrs;
    this._blobAttrs = /* @__PURE__ */ new Map();
    for (const e of Object.values(this.attrs))
      if (er(e)) {
        const [t, r, s] = e["forward-identity"];
        pe(this.blobAttrs, [r, s], e);
      }
    return this._blobAttrs;
  }
  get primaryKeys() {
    if (this._primaryKeys)
      return this._primaryKeys;
    this._primaryKeys = /* @__PURE__ */ new Map();
    for (const e of Object.values(this.attrs))
      if (e["primary?"]) {
        const [t, r] = e["forward-identity"];
        pe(this._primaryKeys, [r], e);
      }
    return this._primaryKeys;
  }
  get forwardIdents() {
    if (this._forwardIdents)
      return this._forwardIdents;
    this._forwardIdents = /* @__PURE__ */ new Map();
    for (const e of Object.values(this.attrs)) {
      const t = e["forward-identity"], [r, s, i] = t;
      pe(this._forwardIdents, [s, i], e);
    }
    return this._forwardIdents;
  }
  get revIdents() {
    if (this._revIdents)
      return this._revIdents;
    this._revIdents = /* @__PURE__ */ new Map();
    for (const e of Object.values(this.attrs)) {
      const t = e["reverse-identity"];
      if (t) {
        const [r, s, i] = t;
        pe(this._revIdents, [s, i], e);
      }
    }
    return this._revIdents;
  }
  toJSON() {
    return { attrs: this.attrs, linkIndex: this.linkIndex };
  }
}
function ho(n) {
  return n.cardinality === "one";
}
function Xn(n) {
  return n["value-type"] === "ref";
}
function er(n) {
  return n["value-type"] === "blob";
}
function ot(n, e) {
  return e.reduce((t, r) => t && t.get(r), n);
}
function Ee(n, e) {
  if (e.length === 0)
    throw new Error("path must have at least one element");
  if (e.length === 1) {
    n.delete(e[0]);
    return;
  }
  const [t, ...r] = e;
  n.has(t) && Ee(n.get(t), r);
}
function pe(n, e, t) {
  let r = n;
  const s = e.length - 1;
  for (let i = 0; i < s; i++) {
    const o = e[i];
    let a = r.get(o);
    a === void 0 && (a = /* @__PURE__ */ new Map(), r.set(o, a)), r = a;
  }
  s > -1 && r.set(e[s], t);
}
function vs(n, e, t) {
  const r = /* @__PURE__ */ new Map(), s = /* @__PURE__ */ new Map(), i = /* @__PURE__ */ new Map();
  for (const o of e) {
    let [a, c, u] = o;
    const l = n.getAttr(c);
    if (!l) {
      console.warn("no such attr", c, a);
      continue;
    }
    l["checked-data-type"] === "date" && t && (u = Jt(u), o[2] = u), Xn(l) && pe(i, [u, c, a], o), pe(r, [a, c, u], o), pe(s, [c, a, u], o);
  }
  return { eav: r, aev: s, vae: i };
}
function Es(n) {
  return {
    triples: ye(n.eav, 3),
    cardinalityInference: n.cardinalityInference,
    useDateObjects: n.useDateObjects,
    version: 1
  };
}
function Os(n, e) {
  return pt(n, e.triples, e.cardinalityInference, e.useDateObjects);
}
function ks(n, e) {
  if (n)
    return new Ke(n.attrs, n.linkIndex);
  if (e && "__type" in e)
    return new Ke(e.attrs, e.linkIndex);
}
function po(n, e) {
  return ot(n.eav, [e]) !== void 0;
}
function pt(n, e, t, r) {
  const s = vs(n, e, r);
  return s.cardinalityInference = t, s.useDateObjects = r, s;
}
function Ot(n, e) {
  var s, i;
  let t;
  if (Array.isArray(e[0])) {
    const [o, a] = e[0], c = n.aev.get(o);
    if (!c)
      return null;
    t = (s = ye(c, 2).find((l) => l[2] === a)) == null ? void 0 : s[0];
  } else
    t = e[0];
  if (!t)
    return null;
  const r = e[2];
  if (Array.isArray(r) && r.length === 2 && n.aev.get(r[0])) {
    const [o, a] = r, c = n.aev.get(o);
    if (!c)
      return null;
    const l = (i = ye(c, 2).find((g) => g[2] === a)) == null ? void 0 : i[0];
    if (!l)
      return null;
    const [f, d, y, ...b] = e;
    return [t, d, l, ...b];
  } else {
    const [o, ...a] = e;
    return [t, ...a];
  }
}
function As(n, e, t) {
  const r = Ot(n, t);
  if (!r)
    return;
  const [s, i, o] = r, a = e.getAttr(i);
  a && (Ee(n.eav, [s, i, o]), Ee(n.aev, [i, s, o]), Xn(a) && Ee(n.vae, [o, i, s]));
}
let yo = 0;
function Cs(n, e, t) {
  const [r, s, i] = t;
  let o;
  const a = ot(n.eav, [r, s, i]);
  return a && (o = a[3]), o || Date.now() * 10 + yo++;
}
function Is(n, e, t) {
  const r = Ot(n, t);
  if (!r)
    return;
  let [s, i, o] = r;
  const a = e.getAttr(i);
  if (!a)
    return;
  a["checked-data-type"] === "date" && n.useDateObjects && (o = Jt(o));
  const c = ot(n.eav, [s, i, o]), u = (c == null ? void 0 : c[3]) ?? Cs(n, a, r), l = [s, i, o, u];
  ho(a) ? (pe(n.eav, [s, i], /* @__PURE__ */ new Map([[o, l]])), pe(n.aev, [i, s], /* @__PURE__ */ new Map([[o, l]]))) : (pe(n.eav, [s, i, o], l), pe(n.aev, [i, s, o], l)), Xn(a) && pe(n.vae, [o, i, s], l);
}
function bo(n, e, t) {
  var y;
  const r = Ot(n, t);
  if (!r)
    return;
  const [s, i, o] = r, a = e.getAttr(i);
  if (!a)
    return;
  if (!er(a))
    throw new Error("merge operation is not supported for links");
  const c = ot(n.eav, [s, i]);
  if (!c)
    return;
  const u = (y = c.values().next()) == null ? void 0 : y.value;
  if (!u)
    return;
  const l = u[2], f = Ts(l, o), d = [
    s,
    i,
    f,
    Cs(n, a, u)
  ];
  pe(n.eav, [s, i], /* @__PURE__ */ new Map([[f, d]])), pe(n.aev, [i, s], /* @__PURE__ */ new Map([[f, d]]));
}
function On(n, e, t) {
  var u, l;
  const [r, s] = t, i = Ot(n, [r]);
  if (!i)
    return;
  const [o] = i, a = n.eav.get(o);
  if (a) {
    for (const f of a.keys()) {
      const d = e.getAttr(f);
      d && d["on-delete-reverse"] === "cascade" && ye(a.get(f), 1).forEach(([y, b, g]) => {
        var w;
        return On(n, e, [g, (w = d["reverse-identity"]) == null ? void 0 : w[1]]);
      }), // Fall back to deleting everything if we've rehydrated tx-steps from
      // the store that didn't set `etype` in deleteEntity
      (!s || // If we don't know about the attr, let's just get rid of it
      !d || // Make sure it matches the etype
      ((u = d["forward-identity"]) == null ? void 0 : u[1]) === s) && (Ee(n.aev, [f, o]), Ee(n.eav, [o, f]));
    }
    a.size === 0 && Ee(n.eav, [o]);
  }
  const c = n.vae.get(o) && ye(n.vae.get(o), 2);
  c && c.forEach((f) => {
    var w, S, _;
    const [d, y, b] = f, g = e.getAttr(y);
    (!s || !g || ((w = g["reverse-identity"]) == null ? void 0 : w[1]) === s) && (Ee(n.eav, [d, y, b]), Ee(n.aev, [y, d, b]), Ee(n.vae, [b, y, d])), g && g["on-delete"] === "cascade" && ((S = g["reverse-identity"]) == null ? void 0 : S[1]) === s && On(n, e, [d, (_ = g["forward-identity"]) == null ? void 0 : _[1]]);
  }), ((l = n.vae.get(o)) == null ? void 0 : l.size) === 0 && Ee(n.vae, [o]);
}
function Ps(n, e, t) {
  const r = vs(e, t, n.useDateObjects);
  Object.keys(r).forEach((s) => {
    n[s] = r[s];
  });
}
function go(n, [e]) {
  n.addAttr(e);
}
function Ms(n) {
  return ye(n.eav, 3);
}
function _o(n, e, [t]) {
  if (!e.getAttr(t))
    return;
  const r = Ms(n).filter(([s, i]) => i !== t);
  e.deleteAttr(t), Ps(n, e, r);
}
function mo(n, e, [t]) {
  e.getAttr(t.id) && (e.updateAttr(t), Ps(n, e, Ms(n)));
}
function wo(n, e, t) {
  const [r, ...s] = t;
  switch (r) {
    case "add-triple":
      Is(n, e, s);
      break;
    case "deep-merge-triple":
      bo(n, e, s);
      break;
    case "retract-triple":
      As(n, e, s);
      break;
    case "delete-entity":
      On(n, e, s);
      break;
    case "add-attr":
      go(e, s);
      break;
    case "delete-attr":
      _o(n, e, s);
      break;
    case "update-attr":
      mo(n, e, s);
      break;
    case "restore-attr":
      break;
    case "rule-params":
      break;
    default:
      throw new Error(`unhandled transaction action: ${r}`);
  }
}
function ye(n, e, t = []) {
  if (!n || e === 0)
    return t;
  if (e === 1) {
    for (const r of n.values())
      t.push(r);
    return t;
  }
  for (const r of n.values())
    ye(r, e - 1, t);
  return t;
}
function Pt(n, e, t) {
  var i;
  const r = [];
  if (t != null && t.hasOwnProperty("$not")) {
    for (const o of e.keys())
      t.$not !== o && r.push(e.get(o));
    return r;
  }
  if (t != null && t.hasOwnProperty("$isNull")) {
    const { attrId: o, isNull: a, reverse: c } = t.$isNull;
    if (c)
      for (const u of e.keys()) {
        const l = n.vae.get(u), f = !l || !l.get(o);
        (a ? f : !f) && r.push(e.get(u));
      }
    else {
      const u = n.aev.get(o);
      for (const l of e.keys()) {
        const f = !u || ((i = u.get(l)) == null ? void 0 : i.get(null)) || !u.get(l);
        (a ? f : !f) && r.push(e.get(l));
      }
    }
    return r;
  }
  if (t != null && t.$comparator)
    return ye(e, 1).filter(t.$op);
  const s = t.in || t.$in || [t];
  for (const o of s) {
    const a = e.get(o);
    a && r.push(a);
  }
  return r;
}
function To(n, e, t) {
  let r = "";
  return n !== void 0 && (r += "e"), e !== void 0 && (r += "a"), t !== void 0 && (r += "v"), r;
}
function So(n, [e, t, r]) {
  var i, o;
  switch (To(e, t, r)) {
    case "e": {
      const a = n.eav.get(e);
      return ye(a, 2);
    }
    case "ea": {
      const a = (i = n.eav.get(e)) == null ? void 0 : i.get(t);
      return ye(a, 1);
    }
    case "eav": {
      const a = (o = n.eav.get(e)) == null ? void 0 : o.get(t);
      return a ? Pt(n, a, r) : [];
    }
    case "ev": {
      const a = n.eav.get(e);
      if (!a)
        return [];
      const c = [];
      for (const u of a.values())
        c.push(...Pt(n, u, r));
      return c;
    }
    case "a": {
      const a = n.aev.get(t);
      return ye(a, 2);
    }
    case "av": {
      const a = n.aev.get(t);
      if (!a)
        return [];
      const c = [];
      for (const u of a.values())
        c.push(...Pt(n, u, r));
      return c;
    }
    case "v": {
      const a = [];
      for (const c of n.eav.values())
        for (const u of c.values())
          a.push(...Pt(n, u, r));
      return a;
    }
    default:
      return ye(n.eav, 3);
  }
}
function vo(n, e, t) {
  var s;
  const r = {};
  if (!e)
    return r;
  for (const [i, o] of e.entries()) {
    const a = (s = n.eav.get(t)) == null ? void 0 : s.get(o.id), c = ye(a, 1);
    for (const u of c)
      r[i] = u[2];
  }
  return r;
}
function te(n, e, t) {
  var r;
  return (r = n.forwardIdents.get(e)) == null ? void 0 : r.get(t);
}
function Ve(n, e, t) {
  var r;
  return (r = n.revIdents.get(e)) == null ? void 0 : r.get(t);
}
function Eo(n, e) {
  return n.blobAttrs.get(e);
}
function Rs(n, e) {
  var r;
  const t = n.primaryKeys.get(e);
  return t || ((r = n.forwardIdents.get(e)) == null ? void 0 : r.get("id"));
}
function Oo(n, e, t) {
  const r = Ot(n, t);
  if (!r)
    return;
  const [s, i, o] = r;
  if (e.getAttr(i))
    return ot(n.eav, [s, i]);
}
function ko(n, e, t) {
  const r = t.filter(([s, i, o, a, c]) => {
    if (s !== "add-triple" && s !== "deep-merge-triple")
      return !0;
    const u = c == null ? void 0 : c.mode;
    if (u !== "create" && u !== "update")
      return !0;
    let l = !1;
    const f = e.getAttr(o);
    if (f) {
      const d = Rs(e, f["forward-identity"][1]);
      l = !!Oo(n, e, [
        i,
        d == null ? void 0 : d.id,
        i
      ]);
    }
    return !(u === "create" && l || u === "update" && !l);
  });
  return Qt({ store: n, attrsStore: e }, (s) => {
    r.forEach((i) => {
      wo(s.store, s.attrsStore, i);
    });
  }, {
    mark: (s) => {
      if (s instanceof Ke)
        return "immutable";
    }
  });
}
function Ao(n) {
  return typeof n == "string" && n.startsWith("?");
}
function Co(n, e, t) {
  if (t.hasOwnProperty(n)) {
    const r = t[n];
    return js(r, e, t);
  }
  return { ...t, [n]: e };
}
function Cr(n, e, t) {
  return n === e ? t : null;
}
function Io(n) {
  switch (typeof n) {
    case "string":
      return n.startsWith("?") ? Co : Cr;
    default:
      return Cr;
  }
}
const Po = [
  "in",
  "$in",
  "$not",
  "$isNull",
  "$comparator"
  // covers all of $gt, $lt, etc.
];
function Mo(n) {
  for (const e of Po)
    if (n.hasOwnProperty(e))
      return !0;
  return !1;
}
function js(n, e, t) {
  return t ? typeof n == "object" ? Mo(n) ? t : null : Io(n)(n, e, t) : null;
}
function Ro(n, e, t) {
  return n.reduce((r, s, i) => {
    const o = e[i];
    return js(s, o, r);
  }, t);
}
function jo(n, e, t) {
  return Do(n, e, t).map((r) => Ro(e, r, t)).filter((r) => r);
}
function xo(n, e, t) {
  return e.or ? e.or.patterns.flatMap((r) => kn(n, r, t)) : e.and ? e.and.patterns.reduce((r, s) => kn(n, s, r), t) : t.flatMap((r) => jo(n, e, r));
}
function kn(n, e, t = [{}]) {
  return e.reduce((r, s) => xo(n, s, r), t);
}
function tr(n, e) {
  return Array.isArray(e) ? e.map((t) => tr(n, t)) : Ao(e) ? n[e] : e;
}
function $o(n, { find: e, where: t }) {
  return kn(n, t).map((s) => tr(s, e));
}
function Do(n, e, t) {
  return So(n, tr(t, e));
}
const Uo = /^(?:[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$/i;
function st(n) {
  return typeof n == "string" && Uo.test(n);
}
const ie = [];
for (let n = 0; n < 256; ++n)
  ie.push((n + 256).toString(16).slice(1));
function Lo(n, e = 0) {
  return (ie[n[e + 0]] + ie[n[e + 1]] + ie[n[e + 2]] + ie[n[e + 3]] + "-" + ie[n[e + 4]] + ie[n[e + 5]] + "-" + ie[n[e + 6]] + ie[n[e + 7]] + "-" + ie[n[e + 8]] + ie[n[e + 9]] + "-" + ie[n[e + 10]] + ie[n[e + 11]] + ie[n[e + 12]] + ie[n[e + 13]] + ie[n[e + 14]] + ie[n[e + 15]]).toLowerCase();
}
let ln;
const Fo = new Uint8Array(16);
function No() {
  if (!ln) {
    if (typeof crypto > "u" || !crypto.getRandomValues)
      throw new Error("crypto.getRandomValues() not supported. See https://github.com/uuidjs/uuid#getrandomvalues-not-supported");
    ln = crypto.getRandomValues.bind(crypto);
  }
  return ln(Fo);
}
const Ko = typeof crypto < "u" && crypto.randomUUID && crypto.randomUUID.bind(crypto), Ir = { randomUUID: Ko };
function Wo(n, e, t) {
  var s;
  if (Ir.randomUUID && !n)
    return Ir.randomUUID();
  n = n || {};
  const r = n.random ?? ((s = n.rng) == null ? void 0 : s.call(n)) ?? No();
  if (r.length < 16)
    throw new Error("Random bytes length must be >= 16");
  return r[6] = r[6] & 15 | 64, r[8] = r[8] & 63 | 128, Lo(r);
}
function Pr(n) {
  const e = n.replace(/-/g, ""), t = [];
  for (let r = 0; r < e.length; r += 2)
    t.push(parseInt(e.substring(r, r + 2), 16));
  return t;
}
function qo(n, e) {
  for (let t = 0; t < n.length; t++) {
    if (n[t] < e[t])
      return -1;
    if (n[t] > e[t])
      return 1;
  }
  return 0;
}
function zo(n, e) {
  return qo(Pr(n), Pr(e));
}
function H() {
  return Wo();
}
function Vo(n, e) {
  return n.localeCompare(e);
}
function Qo() {
  let n = Vo;
  if (typeof Intl == "object" && Intl.hasOwnProperty("Collator"))
    try {
      n = Intl.Collator("en-US").compare;
    } catch {
    }
  return n;
}
const Bo = Qo();
let xs = !0;
const il = (n) => {
  xs = n;
};
let Go = 0;
function wt(n) {
  return Yt(`_${n}`, Go++);
}
function Yt(n, e) {
  return `?${n}-${e}`;
}
class it extends Error {
  constructor(e) {
    super(e), this.name = "AttrNotFoundError";
  }
}
function Ho(n, e) {
  const t = Rs(n, e);
  if (!t)
    throw new it(`Could not find id attr for ${e}`);
  return t;
}
function Mr(n, e, t, r) {
  return [Jo(n, e, t, r)];
}
function Jo(n, e, t, r) {
  return [
    n(t, r),
    Ho(e, t).id,
    n(t, r),
    n("time", r)
  ];
}
function Yo(n, e, t) {
  return n.map((r) => r === e ? t : r);
}
function $s(n, e, t, r, s) {
  const i = te(e, t, s), o = Ve(e, t, s), a = i || o;
  if (!a)
    throw new it(`Could not find attr for ${[t, s]}`);
  if (a["value-type"] !== "ref")
    throw new Error(`Attr ${a.id} is not a ref`);
  const [c, u] = a["forward-identity"], [l, f] = a["reverse-identity"], d = r + 1, y = i ? [
    n(u, r),
    a.id,
    n(f, d),
    wt("time")
  ] : [
    n(u, d),
    a.id,
    n(f, r),
    wt("time")
  ];
  return [i ? f : u, d, y, a, !!i];
}
function Rr(n, e) {
  if (typeof e != "string")
    return function(o) {
      return !1;
    };
  const r = e.replace(/[.*+?^${}()|[\]\\]/g, "\\$&").replace(/%/g, ".*").replace(/_/g, "."), s = new RegExp(`^${r}$`, n ? void 0 : "i");
  return function(o) {
    return typeof o != "string" ? !1 : s.test(o);
  };
}
function Zo(n, e) {
  if (typeof e != "object" || e.hasOwnProperty("$in") || e.hasOwnProperty("in"))
    return e;
  const t = n["checked-data-type"] === "date";
  if (e.hasOwnProperty("$gt"))
    return {
      $comparator: !0,
      $op: t ? function(s) {
        return new Date(s[2]) > new Date(e.$gt);
      } : function(s) {
        return s[2] > e.$gt;
      }
    };
  if (e.hasOwnProperty("$gte"))
    return {
      $comparator: !0,
      $op: t ? function(s) {
        return new Date(s[2]) >= new Date(e.$gte);
      } : function(s) {
        return s[2] >= e.$gte;
      }
    };
  if (e.hasOwnProperty("$lt"))
    return {
      $comparator: !0,
      $op: t ? function(s) {
        return new Date(s[2]) < new Date(e.$lt);
      } : function(s) {
        return s[2] < e.$lt;
      }
    };
  if (e.hasOwnProperty("$lte"))
    return {
      $comparator: !0,
      $op: t ? function(s) {
        return new Date(s[2]) <= new Date(e.$lte);
      } : function(s) {
        return s[2] <= e.$lte;
      }
    };
  if (e.hasOwnProperty("$like")) {
    const r = Rr(!0, e.$like);
    return {
      $comparator: !0,
      $op: function(i) {
        return r(i[2]);
      }
    };
  }
  if (e.hasOwnProperty("$ilike")) {
    const r = Rr(!1, e.$ilike);
    return {
      $comparator: !0,
      $op: function(i) {
        return r(i[2]);
      }
    };
  }
  return e;
}
function Xo(n, e, t, r, s, i) {
  const o = te(e, t, s), a = Ve(e, t, s), c = o || a;
  if (!c)
    throw new it(`No attr for etype = ${t} label = ${s}`);
  if (i != null && i.hasOwnProperty("$isNull")) {
    const u = te(e, t, "id");
    if (!u)
      throw new it(`No attr for etype = ${t} label = id`);
    return [
      n(t, r),
      u.id,
      { $isNull: { attrId: c.id, isNull: i.$isNull, reverse: !o } },
      wt("time")
    ];
  }
  return o ? [
    n(t, r),
    c.id,
    Zo(c, i),
    wt("time")
  ] : [i, c.id, n(t, r), wt("time")];
}
function ea(n, e, t, r, s) {
  const [i, o, a] = s.reduce((c, u) => {
    const [l, f, d] = c, [y, b, g] = $s(n, e, l, f, u);
    return [y, b, [...d, g]];
  }, [t, r, []]);
  return [i, o, a];
}
function An(n, e, t, r, s, i) {
  const o = s.slice(0, s.length - 1), a = s[s.length - 1], [c, u, l] = ea(n, e, t, r, o), f = Xo(n, e, c, u, a, i);
  return l.concat([f]);
}
function ta(n, e) {
  return e ? [e].concat(n) : n;
}
function na([n, e]) {
  return n === "or" && Array.isArray(e);
}
function ra([n, e]) {
  return n === "and" && Array.isArray(e);
}
function sa(n, e, t) {
  return (r, s) => {
    const i = n(r, s);
    return e == i ? i : `${i}-${t}`;
  };
}
function jr(n, e, t, r, s, i) {
  const o = n(r, s), a = i.map((c, u) => {
    const l = sa(n, o, u);
    return Ds(l, t, r, s, c);
  });
  return { [e]: { patterns: a, joinSym: o } };
}
function ia(n) {
  const e = [];
  for (let t = 1; t <= n.length; t++)
    e.push(n.slice(0, t));
  return e;
}
function xr(n, e, t, r, s) {
  return ia(s).map((i) => An(n, e, t, r, i, {
    $isNull: !0
  }));
}
function Ds(n, e, t, r, s) {
  return Object.entries(s).flatMap(([i, o]) => {
    if (na([i, o]))
      return jr(n, "or", e, t, r, o);
    if (ra([i, o]))
      return jr(n, "and", e, t, r, o);
    if (i === "$entityIdStartsWith")
      return [];
    const a = i.split(".");
    if (o != null && o.hasOwnProperty("$ne") && (o = { ...o, $not: o.$ne }, delete o.$ne), o != null && o.hasOwnProperty("$not")) {
      const c = An(n, e, t, r, a, o), u = xr(n, e, t, r, a);
      return [
        {
          or: {
            patterns: [c, ...u],
            joinSym: n(t, r)
          }
        }
      ];
    }
    return o != null && o.hasOwnProperty("$isNull") && o.$isNull === !0 && a.length > 1 ? [
      {
        or: {
          patterns: xr(n, e, t, r, a),
          joinSym: n(t, r)
        }
      }
    ] : An(n, e, t, r, a, o);
  });
}
function oa(n, e, t, r) {
  const s = Yt;
  return r ? Ds(s, n, e, t, r).concat(Mr(s, n, e, t)) : Mr(s, n, e, t);
}
function aa(n, e, t) {
  return [n(e, t), n("time", t)];
}
function ca(n, e, t, r, s, i) {
  const [o, a, c, u, l] = $s(n, e, t, r, s), f = Yo(c, n(t, r), i);
  return [o, a, f, u, l];
}
function ua(n, e, t, { etype: r, level: s, form: i }, o) {
  const a = Object.keys(i).filter((c) => c !== "$");
  return a.length ? Object.entries(o).map(function([u, l]) {
    return a.map(function(y) {
      var g, w, S;
      const b = !!(e.cardinalityInference && ((S = (w = (g = t.linkIndex) == null ? void 0 : g[r]) == null ? void 0 : w[y]) != null && S.isSingular));
      try {
        const [_, m, A] = ca(n, t, r, s, y, u), v = Fs(e, t, {
          etype: _,
          level: m,
          form: i[y],
          join: A
        }), I = b ? v[0] : v;
        return { [y]: I };
      } catch (_) {
        if (_ instanceof it)
          return { [y]: b ? void 0 : [] };
        throw _;
      }
    }).reduce(function(y, b) {
      return { ...y, ...b };
    }, l);
  }) : Object.values(o);
}
function la(n, e, t) {
  return t === "string" ? Bo(n, e) : n > e ? 1 : -1;
}
function yt(n, e, t, r, s) {
  return e === r || e == null && r == null ? zo(n, t) : r == null ? 1 : e == null ? -1 : la(e, r, s);
}
function Bt([n, e], [t, r], s) {
  return yt(n, e, t, r, s);
}
function Cn(n) {
  return n == null ? n : new Date(n).getTime();
}
function Us(n, e, t) {
  var d;
  const [r, s, i, o] = n;
  if (((d = e["forward-identity"]) == null ? void 0 : d[2]) === "id")
    return Bt(t, [r, o], null);
  const [a, c] = t, u = e["checked-data-type"], l = u === "date" ? Cn(c) : c, f = u === "date" ? Cn(i) : i;
  return Bt([a, l], [r, f], u);
}
function fa(n, e, t, r) {
  const s = Us(n, e, r);
  return t === "desc" ? s > 0 : s < 0;
}
function da(n, e, t, r) {
  const s = Us(n, e, r);
  return t === "desc" ? s < 0 : s > 0;
}
function ha(n, e) {
  const t = e[1];
  return n.getAttr(t);
}
function pa(n, e, t) {
  const r = Object.keys(t)[0];
  return te(n, e, r);
}
function ya(n, e, t, r) {
  if (t)
    return ha(n, t);
  if (r)
    return pa(n, e, r);
}
function ba(n, e, t) {
  var s, i;
  if (!Array.isArray(t.fields))
    return Eo(n, e);
  const r = /* @__PURE__ */ new Map();
  for (const o of t.fields) {
    const a = te(n, e, o), c = (s = a == null ? void 0 : a["forward-identity"]) == null ? void 0 : s[2];
    c && er(a) && r.set(c, a);
  }
  if (!r.has("id")) {
    const o = te(n, e, "id"), a = (i = o == null ? void 0 : o["forward-identity"]) == null ? void 0 : i[2];
    a && r.set(a, o);
  }
  return r;
}
function ga(n, e, { etype: t, pageInfo: r, dq: s, form: i }) {
  var g, w;
  const o = (g = i == null ? void 0 : i.$) == null ? void 0 : g.order, a = Ls(i), c = _a(i);
  let u = $o(n, s);
  const l = r == null ? void 0 : r["start-cursor"], f = r == null ? void 0 : r["end-cursor"], d = ya(e, t, l, o);
  if (d && ((w = d == null ? void 0 : d["forward-identity"]) == null ? void 0 : w[2]) !== "id") {
    const S = d["checked-data-type"] === "date", _ = d.id;
    u = u.map(([m]) => {
      var v, I, j, N, Q;
      let A = (Q = (N = (j = (I = (v = n.eav.get(m)) == null ? void 0 : v.get(_)) == null ? void 0 : I.values()) == null ? void 0 : j.next()) == null ? void 0 : N.value) == null ? void 0 : Q[2];
      return S && (A = Cn(A)), [m, A];
    });
  }
  u.sort(c === "asc" ? function(_, m) {
    return Bt(_, m, d == null ? void 0 : d["checked-data-type"]);
  } : function(_, m) {
    return Bt(m, _, d == null ? void 0 : d["checked-data-type"]);
  });
  let y = {};
  const b = ba(e, t, s);
  for (const S of u) {
    const [_] = S;
    if (y[_] || !a && l && d && fa(l, d, c, S) || f && d && da(f, d, c, S))
      continue;
    const m = vo(n, b, _);
    m && (y[_] = m);
  }
  return y;
}
function _a(n) {
  var t;
  const e = (t = n.$) == null ? void 0 : t.order;
  return e && e[Object.keys(e)[0]] || "asc";
}
function Ls(n) {
  var s, i, o;
  const e = (s = n.$) == null ? void 0 : s.offset, t = (i = n.$) == null ? void 0 : i.before, r = (o = n.$) == null ? void 0 : o.after;
  return !e && !t && !r;
}
function ma(n, e, { etype: t, level: r, form: s, join: i, pageInfo: o }) {
  var d, y, b, g, w;
  if (!Ls(s) && (!o || !o["start-cursor"]))
    return [];
  const a = ta(oa(e, t, r, (d = s.$) == null ? void 0 : d.where), i), c = aa(Yt, t, r), u = (y = s.$) == null ? void 0 : y.fields, l = ga(n, e, {
    etype: t,
    pageInfo: o,
    form: s,
    dq: { where: a, find: c, fields: u }
  }), f = ((b = s.$) == null ? void 0 : b.limit) || ((g = s.$) == null ? void 0 : g.first) || ((w = s.$) == null ? void 0 : w.last);
  if (f != null) {
    r > 0 && xs && console.warn("WARNING: Limits in child queries are only run client-side. Data returned from the server will not have a limit.");
    const S = Object.entries(l);
    return S.length <= f ? l : Object.fromEntries(S.slice(0, f));
  }
  return l;
}
function wa(n, e, t) {
  try {
    return ma(n, e, t);
  } catch (r) {
    if (r instanceof it)
      return {};
    throw r;
  }
}
function Fs(n, e, t) {
  const r = wa(n, e, t);
  return ua(Yt, n, e, t, r);
}
function Ta(n) {
  const e = {};
  for (const [t, r] of Object.entries(n))
    e[t] = {
      startCursor: r["start-cursor"],
      endCursor: r["end-cursor"],
      hasNextPage: r["has-next-page?"],
      hasPreviousPage: r["has-previous-page?"]
    };
  return e;
}
function Ns({ store: n, attrsStore: e, pageInfo: t, aggregate: r }, s) {
  const o = { data: Object.keys(s).reduce(function(c, u) {
    return r != null && r[u] || u === "$$ruleParams" || (c[u] = Fs(n, e, {
      etype: u,
      form: s[u],
      level: 0,
      pageInfo: t == null ? void 0 : t[u]
    })), c;
  }, {}) };
  return t && (o.pageInfo = Ta(t)), r && (o.aggregate = r), o;
}
function Sa() {
  const e = {
    __etype: 1,
    __ops: 1,
    create: 1,
    update: 1,
    link: 1,
    unlink: 1,
    delete: 1,
    merge: 1,
    ruleParams: 1
  };
  return new Set(Object.keys(e));
}
const va = Sa();
function Ft(n, e, t) {
  const r = {
    __etype: n,
    __ops: t
  };
  return new Proxy(r, {
    get: (s, i) => {
      if (i === "__ops")
        return t;
      if (i === "__etype")
        return n;
      if (va.has(i))
        return (o, a) => Ft(n, e, [
          ...t,
          a ? [i, n, e, o, a] : [i, n, e, o]
        ]);
    }
  });
}
function Ea(n, e) {
  return `lookup__${n}__${JSON.stringify(e)}`;
}
function Gt(n) {
  return n.startsWith("lookup__");
}
function In(n) {
  const [e, t, ...r] = n.split("__");
  return [t, JSON.parse(r.join("__"))];
}
function Oa(n) {
  return new Proxy({
    __etype: n
  }, {
    get(e, t) {
      if (t === "lookup")
        return (s, i) => Ft(n, In(Ea(s, i)), []);
      if (t === "__etype")
        return n;
      const r = t;
      return Gt(r) ? Ft(n, In(r), []) : Ft(n, r, []);
    }
  });
}
function nr() {
  return new Proxy({}, {
    get(n, e) {
      return Oa(e);
    }
  });
}
const ol = nr();
function ka(n) {
  return n.__ops;
}
function Aa(n, e) {
  const { attrIdMap: t, refSwapAttrIds: r } = n, s = [];
  for (const o of e) {
    const a = t[o];
    if (a)
      s.push(a);
    else if (Array.isArray(o) && o.length == 2 && t[o[0]]) {
      const [c, u] = o;
      s.push([t[c], u]);
    } else
      s.push(o);
  }
  const [i] = e;
  if ((i === "add-triple" || i === "retract-triple") && r.has(e[2])) {
    const o = s[1];
    s[1] = s[3], s[3] = o;
  }
  return s;
}
function Ca(n) {
  if (Array.isArray(n))
    return n;
  const e = Object.entries(n);
  if (e.length !== 1)
    throw new Error("lookup must be an object with a single unique attr and value.");
  return e[0];
}
function Ia(n, e, t) {
  return t.indexOf(".") !== -1 && // attr names can have `.` in them, so use the attr we find with a `.`
  // before assuming it's a ref lookup.
  !te(n, e, t);
}
function Pn(n) {
  const [e, t, ...r] = n.split(".");
  if (r.length > 0 || t !== "id")
    throw new Error(`${n} is not a valid lookup attribute.`);
  return e;
}
function Pa(n, e, t) {
  if (!Ia(n, e, t))
    return te(n, e, t);
  const r = Pn(t), s = te(n, e, r) || Ve(n, e, r);
  if (s && s["value-type"] !== "ref")
    throw new Error(`${t} does not reference a valid link attribute.`);
  return s;
}
function Mn(n) {
  return typeof n == "string" && !Gt(n) ? null : typeof n == "string" && Gt(n) ? In(n) : Ca(n);
}
function be(n, e, t) {
  const r = Mn(t);
  if (r === null)
    return t;
  const [s, i] = r, o = Pa(n, e, s);
  if (!o || !o["unique?"])
    throw new Error(`${s} is not a unique attribute.`);
  return [o.id, i];
}
function Ks(n, e, t, r) {
  var o;
  const s = be(n, e, t);
  return Array.isArray(s) ? [[
    "add-triple",
    s,
    (o = te(n, e, "id")) == null ? void 0 : o.id,
    s
  ]].concat(r) : r;
}
function Ma({ attrsStore: n }, [e, t, r]) {
  const s = Object.entries(r).flatMap(([i, o]) => {
    const a = Array.isArray(o) ? o : [o], c = te(n, e, i), u = Ve(n, e, i);
    return a.map((l) => c ? [
      "add-triple",
      be(n, e, t),
      c.id,
      // Uses `!` because if we get here, we should have created the attr if it doesn't
      // already exist
      be(n, c["reverse-identity"][1], l)
    ] : [
      "add-triple",
      // Uses `!` because if we get here, we should have created the attr if it doesn't
      // already exist
      be(n, u["forward-identity"][1], l),
      u == null ? void 0 : u.id,
      be(n, e, t)
    ]);
  });
  return Ks(n, e, t, s);
}
function Ra({ attrsStore: n }, [e, t, r]) {
  const s = Object.entries(r).flatMap(([i, o]) => {
    const a = Array.isArray(o) ? o : [o], c = te(n, e, i), u = Ve(n, e, i);
    return a.map((l) => c ? [
      "retract-triple",
      be(n, e, t),
      c.id,
      // Uses `!` because if we get here, we should have created the attr if it doesn't
      // already exist
      be(n, c["reverse-identity"][1], l)
    ] : [
      "retract-triple",
      // Uses `!` because if we get here, we should have created the attr if it doesn't
      // already exist
      be(n, u["forward-identity"][1], l),
      u.id,
      be(n, e, t)
    ]);
  });
  return Ks(n, e, t, s);
}
function ja(n, e, t, r) {
  var s;
  if (Array.isArray(r)) {
    const [i, o] = r;
    for (const a of n || []) {
      const c = a == null ? void 0 : a.aev.get(i);
      if (c) {
        for (const [u, l, f] of ye(c, 2))
          if (f === o)
            return !0;
      }
    }
  } else
    for (const i of n || []) {
      const o = i == null ? void 0 : i.eav.get(r);
      if (o) {
        for (const a of o.keys())
          if (((s = e.getAttr(a)) == null ? void 0 : s["forward-identity"][1]) == t)
            return !0;
      }
    }
  return !1;
}
function Ws({ stores: n, attrsStore: e }, [t, r, s, i]) {
  return (i == null ? void 0 : i.upsert) === !1 ? { mode: "update" } : (i == null ? void 0 : i.upsert) === !0 ? null : ja(n, e, t, r) ? { mode: "update" } : null;
}
function xa(n, e) {
  const { attrsStore: t } = n, [r, s, i, o] = e, a = Zn(i), c = be(t, r, s);
  return [["id", c]].concat(Object.entries(a)).map(([l, f]) => {
    const d = te(t, r, l);
    return d["checked-data-type"] === "date" && n.useDateObjects && (f = Jt(f)), ["add-triple", c, d.id, f, { mode: "create" }];
  });
}
function $a(n, e) {
  const { attrsStore: t } = n, [r, s, i, o] = e, a = Zn(i), c = be(t, r, s), u = Ws(n, [r, c, i, o]);
  return [["id", c]].concat(Object.entries(a)).map(([f, d]) => {
    const y = te(t, r, f);
    return y["checked-data-type"] === "date" && n.useDateObjects && (d = Jt(d)), [
      "add-triple",
      c,
      y.id,
      d,
      ...u ? [u] : []
    ];
  });
}
function Da({ attrsStore: n }, [e, t]) {
  return [["delete-entity", be(n, e, t), e]];
}
function Ua(n, e) {
  const { attrsStore: t } = n, [r, s, i, o] = e, a = Zn(i), c = be(t, r, s), u = Ws(n, [r, c, i, o]), l = Object.entries(a).map(([d, y]) => {
    const b = te(t, r, d);
    return [
      "deep-merge-triple",
      c,
      b.id,
      y,
      ...u ? [u] : []
    ];
  });
  return [[
    "add-triple",
    c,
    te(t, r, "id").id,
    c,
    ...u ? [u] : []
  ]].concat(l);
}
function La({ attrsStore: n }, [e, t, r]) {
  return [["rule-params", be(n, e, t), e, r]];
}
function Fa(n) {
  const [e, t, r, s, i] = n;
  if (!s)
    return n;
  const o = { ...s };
  return delete o.id, [e, t, r, o, ...i ? [i] : []];
}
function Na(n, e) {
  const [t, ...r] = Fa(e);
  switch (t) {
    case "merge":
      return Ua(n, r);
    case "create":
      return xa(n, r);
    case "update":
      return $a(n, r);
    case "link":
      return Ma(n, r);
    case "unlink":
      return Ra(n, r);
    case "delete":
      return Da(n, r);
    case "ruleParams":
      return La(n, r);
    default:
      throw new Error(`unsupported action ${t}`);
  }
}
function Ka(n) {
  switch (n) {
    case "string":
    case "date":
    case "boolean":
    case "number":
      return n;
    default:
      return;
  }
}
function Wa(n, e, t) {
  var a, c;
  const r = (c = (a = n.entities[e]) == null ? void 0 : a.attrs) == null ? void 0 : c[t];
  if (t === "id")
    return null;
  if (!r)
    throw new Error(`${e}.${t} does not exist in your schema`);
  const { unique: s, indexed: i } = r == null ? void 0 : r.config, o = Ka(r == null ? void 0 : r.valueType);
  return {
    "index?": i,
    "unique?": s,
    "checked-data-type": o
  };
}
function Mt(n, e, t, r) {
  const s = n ? Wa(n, e, t) : null, i = H(), a = [H(), e, t];
  return {
    id: i,
    "forward-identity": a,
    "value-type": "blob",
    cardinality: "one",
    "unique?": !1,
    "index?": !1,
    isUnsynced: !0,
    ...s || {},
    ...r || {}
  };
}
function qa(n, e, t) {
  return Object.values(n.links).find((i) => i.forward.on === e && i.forward.label === t || i.reverse.on === e && i.reverse.label === t);
}
function za(n, e, t) {
  const r = qa(n, e, t);
  if (!r)
    throw new Error(`Couldn't find the link ${e}.${t} in your schema`);
  const { forward: s, reverse: i } = r;
  return {
    "forward-identity": [H(), s.on, s.label],
    "reverse-identity": [H(), i.on, i.label],
    cardinality: s.has === "one" ? "one" : "many",
    "unique?": i.has === "one",
    "on-delete": s.onDelete,
    "on-delete-reverse": i.onDelete
  };
}
function $r(n, e, t, r) {
  const s = n ? za(n, e, t) : null, i = H(), o = [H(), e, t], a = [H(), t, e];
  return {
    id: i,
    // @ts-ignore: ts thinks it's any[]
    "forward-identity": o,
    // @ts-ignore: ts thinks it's any[]
    "reverse-identity": a,
    "value-type": "ref",
    // @ts-ignore: ts thinks it's type string
    cardinality: "many",
    "unique?": !1,
    "index?": !1,
    isUnsynced: !0,
    ...s || {},
    ...r || {}
  };
}
const Va = /* @__PURE__ */ new Set(["create", "update", "merge", "link", "unlink"]), Qa = /* @__PURE__ */ new Set(["link", "unlink"]), Ba = /* @__PURE__ */ new Set(["create", "update", "merge"]), Ga = /* @__PURE__ */ new Set([
  "link",
  "unlink",
  "create",
  "update",
  "merge",
  "delete",
  "ruleParams"
]), Rn = { "unique?": !0, "index?": !0 }, Ha = {
  ...Rn,
  cardinality: "one"
};
function Ja(n) {
  const e = [], [t, r, s, i] = n;
  if (!Ga.has(t))
    return e;
  const o = Mn(s);
  if (o && e.push({ etype: r, lookupPair: o }), t === "link")
    for (const [a, c] of Object.entries(i)) {
      const u = Array.isArray(c) ? c : [c];
      for (const l of u) {
        const f = Mn(l);
        f && e.push({
          etype: r,
          lookupPair: f,
          linkLabel: a
        });
      }
    }
  return e;
}
function Ya({ attrsStore: n, schema: e }, t) {
  var d, y;
  const r = /* @__PURE__ */ new Set(), s = [], i = [];
  function o(b, g) {
    return te(n, b, g) || s.find((w) => w["forward-identity"][1] === b && w["forward-identity"][2] === g);
  }
  function a(b, g) {
    return Ve(n, b, g) || s.find((w) => {
      var S, _;
      return ((S = w["reverse-identity"]) == null ? void 0 : S[1]) === b && ((_ = w["reverse-identity"]) == null ? void 0 : _[2]) === g;
    });
  }
  function c(b) {
    s.push(b), i.push(["add-attr", b]), r.add(b.id);
  }
  function u(b) {
    b && "isUnsynced" in b && b.isUnsynced && !r.has(b.id) && (s.push(b), i.push(["add-attr", b]), r.add(b.id));
  }
  function l(b, g) {
    return g.indexOf(".") !== -1 && // attr names can have `.` in them, so use the attr we find with a `.`
    // before assuming it's a ref lookup.
    !o(b, g);
  }
  function f(b, g) {
    const w = o(b, g), S = a(b, g);
    u(w), u(S), !w && !S && c($r(e, b, g, Ha));
  }
  for (const b of t)
    for (const { etype: g, lookupPair: w, linkLabel: S } of Ja(b)) {
      const _ = w[0];
      if (S) {
        f(g, S);
        const m = o(g, S), A = a(g, S);
        u(m), u(A);
        const v = ((d = m == null ? void 0 : m["reverse-identity"]) == null ? void 0 : d[1]) || ((y = A == null ? void 0 : A["forward-identity"]) == null ? void 0 : y[1]) || S;
        if (l(v, _))
          f(v, Pn(_));
        else {
          const I = o(v, _);
          I || c(Mt(e, v, _, Rn)), u(I);
        }
      } else if (l(g, _))
        f(g, Pn(_));
      else {
        const m = o(g, _);
        m || c(Mt(e, g, _, Rn)), u(m);
      }
    }
  for (const b of t) {
    const [g, w, S, _] = b;
    if (Va.has(g)) {
      const m = o(w, "id");
      u(m), m || c(Mt(e, w, "id", { "unique?": !0 }));
      for (const A of Object.keys(_)) {
        const v = o(w, A);
        if (u(v), Ba.has(g) && (v || c(Mt(e, w, A, A === "id" ? { "unique?": !0 } : null))), Qa.has(g)) {
          const I = a(w, A);
          !v && !I && c($r(e, w, A)), u(I);
        }
      }
    }
  }
  if (s.length) {
    const b = { ...n.attrs };
    for (const g of s)
      b[g.id] = g;
    return [new Ke(b, n.linkIndex), i];
  }
  return [n, i];
}
function Za(n, e) {
  const r = (Array.isArray(e) ? e : [e]).flatMap((c) => ka(c)), [s, i] = Ya(n, r), o = { ...n, attrsStore: s }, a = r.flatMap((c) => Na(o, c));
  return [...i, ...a];
}
function Dr(n, e) {
  typeof requestIdleCallback > "u" ? n() : requestIdleCallback(n, { timeout: e });
}
const et = "__meta";
class Xa {
  constructor(e, t) {
  }
}
class jn {
  constructor(e) {
    p(this, "currentValue");
    p(this, "_subs", []);
    p(this, "_persister");
    p(this, "_merge");
    p(this, "serialize");
    p(this, "parse");
    p(this, "_saveThrottleMs");
    p(this, "_idleCallbackMaxWaitMs");
    p(this, "_nextSave", null);
    p(this, "_nextGc", null);
    p(this, "_pendingSaveKeys", /* @__PURE__ */ new Set());
    p(this, "_loadedKeys", /* @__PURE__ */ new Set());
    p(this, "_loadingKeys");
    p(this, "_objectSize");
    p(this, "_log");
    p(this, "onKeyLoaded");
    p(this, "_version", 0);
    p(this, "_meta", {
      isLoading: !0,
      onLoadCbs: [],
      value: null,
      error: null,
      attempts: 0
    });
    p(this, "_gcOpts");
    this._persister = e.persister, this._merge = e.merge, this.serialize = e.serialize, this.parse = e.parse, this._objectSize = e.objectSize, this._log = e.logger, this._saveThrottleMs = e.saveThrottleMs ?? 100, this._idleCallbackMaxWaitMs = e.idleCallbackMaxWaitMs ?? 1e3, this._gcOpts = e.gc, this.currentValue = {}, this._loadedKeys = /* @__PURE__ */ new Set(), this._loadingKeys = {}, this._initMeta(), e.preloadEntryCount && this._preloadEntries(e.preloadEntryCount);
  }
  async _initMeta() {
    var e;
    this._meta.loadingPromise && await this._meta.loadingPromise;
    try {
      const t = this._persister.getItem(et);
      this._meta.loadingPromise = t;
      const r = await t;
      this._meta.isLoading = !1, this._meta.error = null, this._meta.loadingPromise = null, this._meta.attempts = 0;
      const s = ((e = this._meta.value) == null ? void 0 : e.objects) ?? {}, i = r ?? {}, o = i.objects ?? {};
      this._meta.value = {
        ...i,
        objects: { ...s, ...o }
      };
    } catch (t) {
      this._meta.error = t, this._meta.attempts++, this._meta.loadingPromise = null;
    }
  }
  async _getMeta() {
    return this._meta.value ? this._meta.value : this._meta.loadingPromise ? (await this._meta.loadingPromise, this._meta.value) : (this._initMeta(), await this._meta.loadingPromise, this._meta.value);
  }
  async _refreshMeta() {
    return await this._initMeta(), this._meta.value;
  }
  async _preloadEntries(e) {
    const t = await this.waitForMetaToLoad();
    if (!t)
      return;
    const r = Object.entries(t.objects);
    r.sort(([s, i], [o, a]) => a.updatedAt - i.updatedAt);
    for (const [s] of r.slice(0, e))
      this._loadKey(s);
  }
  async _getFromStorage(e) {
    try {
      const t = await this._persister.getItem(e);
      return t && this.parse(e, t);
    } catch (t) {
      return console.error(`Unable to read from storage for key=${e}`, t), null;
    }
  }
  async waitForKeyToLoad(e) {
    return this._loadedKeys.has(e) ? this.currentValue[e] : (await (this._loadingKeys[e] || this._loadKey(e)), this.currentValue[e]);
  }
  // Used for tests
  async waitForMetaToLoad() {
    return this._getMeta();
  }
  // Unloads the key so that it can be garbage collected, but does not
  // delete it. Removes the key from currentValue.
  unloadKey(e) {
    this._loadedKeys.delete(e), delete this._loadingKeys[e], delete this.currentValue[e];
  }
  async _loadKey(e) {
    if (this._loadedKeys.has(e) || e in this._loadingKeys)
      return;
    const t = this._getFromStorage(e);
    this._loadingKeys[e] = t;
    const r = await t;
    if (delete this._loadingKeys[e], this._loadedKeys.add(e), r) {
      const s = this._merge(e, r, this.currentValue[e]);
      s && (this.currentValue[e] = s);
    }
    this.onKeyLoaded && this.onKeyLoaded(e);
  }
  // Returns a promise with a number so that we can wait for flush
  // to finish in the tests. The number is the number of operations
  // it performed, but it's mostly there so that typescript will warn
  // us if we forget to retun the promise from the function.
  _writeToStorage(e) {
    const t = [], r = e == null ? void 0 : e.skipGc;
    if (this._meta.isLoading) {
      const f = new Promise((d, y) => {
        setTimeout(() => this._enqueuePersist(e ? { ...e, attempts: (e.attempts || 0) + 1 } : { attempts: 1 }).then(d).catch(y), 10 + ((e == null ? void 0 : e.attempts) ?? 0) * 1e3);
      });
      return t.push(f), Promise.all(t).then((d) => d.reduce((y, b) => y + b, 0));
    }
    const s = this._meta.value;
    if (!s)
      return Promise.resolve(0);
    const i = [], o = [];
    for (const f of this._pendingSaveKeys)
      f in this.currentValue ? o.push(f) : (i.push(f), delete s.objects[f]);
    for (const f of i) {
      const d = this._persister.removeItem(f);
      t.push(d.then(() => 1)), this._loadedKeys.delete(f), this._pendingSaveKeys.delete(f);
    }
    const a = [], c = [[et, s]], u = s.objects ?? {};
    s.objects = u;
    for (const f of o)
      if (this._loadedKeys.has(f)) {
        const d = this.serialize(f, this.currentValue[f]);
        c.push([f, d]);
        const y = this._objectSize(d), b = u[f] ?? {
          createdAt: Date.now(),
          updatedAt: Date.now(),
          size: y
        };
        b.updatedAt = Date.now(), b.size = y, u[f] = b, this._pendingSaveKeys.delete(f);
      } else
        a.push(f);
    const l = this._persister.multiSet(c);
    t.push(l.then(() => 1));
    for (const f of a) {
      const d = this._loadKey(f).then(() => this._enqueuePersist(e));
      t.push(d);
    }
    return r || this.gc(), Promise.all(t).then((f) => f.reduce((d, y) => d + y, 0));
  }
  async flush() {
    return this._nextSave ? (clearTimeout(this._nextSave), this._nextSave = null, this._writeToStorage()) : void 0;
  }
  async _gc() {
    if (!this._gcOpts)
      return;
    const e = new Set(await this._persister.getAllKeys());
    e.delete(et);
    const t = new Set(Object.keys(this.currentValue));
    for (const d of Object.keys(this._loadingKeys))
      t.add(d);
    for (const d of this._loadedKeys)
      t.add(d);
    const r = await this._refreshMeta();
    if (!r) {
      this._log.info("Could not gc because we were not able to load meta");
      return;
    }
    const s = [], i = {
      gcOpts: this._gcOpts,
      keys: e,
      sacredKeys: t,
      removed: [],
      metaRemoved: [],
      removedMissingCount: 0,
      removedOldCount: 0,
      removedThresholdCount: 0,
      removedSizeCount: 0
    };
    for (const d of e)
      t.has(d) || d in r.objects || (this._log.info("Lost track of key in meta", d), s.push(this._persister.removeItem(d)), i.removed.push(d), i.removedMissingCount++);
    const o = Date.now();
    for (const [d, y] of Object.entries(r.objects))
      !t.has(d) && y.updatedAt < o - this._gcOpts.maxAgeMs && (s.push(this._persister.removeItem(d)), delete r.objects[d], i.removed.push(d), i.removedOldCount++);
    const a = Object.entries(r.objects);
    a.sort(([d, y], [b, g]) => y.updatedAt - g.updatedAt);
    const c = a.filter(([d]) => !t.has(d));
    if (a.length > this._gcOpts.maxEntries)
      for (const [d] of c.slice(0, a.length - this._gcOpts.maxEntries))
        s.push(this._persister.removeItem(d)), delete r.objects[d], i.removed.push(d), i.removedThresholdCount++;
    const u = Object.entries(r.objects);
    u.sort(([d, y], [b, g]) => y.updatedAt - g.updatedAt);
    const l = u.filter(([d]) => !t.has(d));
    let f = u.reduce((d, [y, b]) => d + b.size, 0);
    for (; f > 0 && f > this._gcOpts.maxSize && l.length; ) {
      const [[d, y]] = l.splice(0, 1);
      f -= y.size, s.push(this._persister.removeItem(d)), delete r.objects[d], i.removed.push(d), i.removedSizeCount++;
    }
    for (const d of Object.keys(r.objects))
      !e.has(d) && !t.has(d) && delete r.objects[d];
    return (i.removed.length || i.metaRemoved.length) && s.push(this._enqueuePersist({ skipGc: !0 })), this._log.info("Completed GC", i), await Promise.all(s), i;
  }
  // Schedules a GC to run in one minute (unless it is already scheduled)
  gc() {
    this._nextGc || (this._nextGc = setTimeout(
      () => {
        Dr(() => {
          this._nextGc = null, this._gc();
        }, 30 * 1e3);
      },
      // 1 minute + some jitter to keep multiple tabs from running at same time
      1e3 * 60 + Math.random() * 500
    ));
  }
  _enqueuePersist(e) {
    return new Promise((t, r) => {
      if (this._nextSave) {
        t(0);
        return;
      }
      this._nextSave = setTimeout(() => {
        Dr(() => {
          this._nextSave = null, this._writeToStorage(e).then(t).catch(r);
        }, this._idleCallbackMaxWaitMs);
      }, this._saveThrottleMs);
    });
  }
  version() {
    return this._version;
  }
  // Takes a function that updates the store in place.
  // Uses `mutative` to get a list of keys that were changed
  // so that we know which entries we need to persist to the store.
  updateInPlace(e) {
    this._version++;
    const [t, r] = Qt(this.currentValue, e, {
      enablePatches: !0
    });
    for (const s of r) {
      const i = s.path[0];
      i && typeof i == "string" && (this._pendingSaveKeys.add(i), this._loadedKeys.has(i) || this._loadKey(i));
    }
    this.currentValue = t, this._enqueuePersist();
    for (const s of this._subs)
      s(this.currentValue);
    return t;
  }
  subscribe(e) {
    return this._subs.push(e), e(this.currentValue), () => {
      this._subs = this._subs.filter((t) => t !== e);
    };
  }
  // Removes any keys that we haven't loaded--used when
  // changing users to make sure we clean out all user data
  async clearUnloadedKeys() {
    let e = !1;
    for (const t of await this._persister.getAllKeys())
      t === et || t in this.currentValue || (this._pendingSaveKeys.add(t), e = !0);
    e && await this._enqueuePersist();
  }
}
const ec = 6, tc = ["kv", "querySubs", "syncSubs"];
function nc(n) {
  return function(t) {
    console.error("Error in IndexedDB event", { source: n, event: t });
  };
}
async function rc(n) {
  return new Promise((e) => {
    const t = indexedDB.open(n);
    t.onerror = (r) => {
      e(null);
    }, t.onsuccess = (r) => {
      const i = r.target.result;
      e(i);
    }, t.onupgradeneeded = (r) => {
      var i;
      (i = r.target.transaction) == null || i.abort(), e(null);
    };
  });
}
async function sc(n, e, t) {
  const r = (
    // Backwards compatibility for older versions where we JSON.stringified before storing
    typeof e == "string" ? JSON.parse(e) : e
  );
  if (!r)
    return;
  const s = /* @__PURE__ */ new Set();
  return new Promise((i, o) => {
    var l, f, d;
    const a = {};
    for (const [y, b] of Object.entries(r)) {
      const g = typeof b == "string" ? JSON.parse(b) : b;
      if (g.lastAccessed) {
        const S = {
          createdAt: g.lastAccessed,
          updatedAt: g.lastAccessed,
          size: ((d = (f = (l = g.result) == null ? void 0 : l.store) == null ? void 0 : f.triples) == null ? void 0 : d.length) ?? 0
        };
        a[y] = S;
      }
      const w = t.put(g, y);
      s.add(w);
    }
    const c = { objects: a }, u = t.put(c, et);
    s.add(u);
    for (const y of s)
      y.onsuccess = () => {
        s.delete(y), s.size === 0 && i();
      }, y.onerror = (b) => {
        o(b);
      };
  });
}
async function Ur(n, e, t) {
  const r = t.put(e, n);
  return new Promise((s, i) => {
    r.onsuccess = () => s(), r.onerror = (o) => i(o);
  });
}
async function ic(n, e) {
  const t = await rc(`instant_${n}_5`);
  if (!t)
    return;
  const r = await new Promise((l, f) => {
    const b = t.transaction(["kv"], "readonly").objectStore("kv").openCursor();
    b.onerror = (w) => {
      f(w);
    };
    const g = [];
    b.onsuccess = () => {
      const w = b.result;
      if (w) {
        const S = w.key, _ = w.value;
        g.push([S, _]), w.continue();
      } else
        l(g);
    }, b.onerror = (w) => {
      f(w);
    };
  }), s = e.transaction(["kv", "querySubs"], "readwrite"), i = s.objectStore("kv"), o = s.objectStore("querySubs"), a = [], c = { objects: {} };
  for (const [l, f] of r)
    switch (l) {
      case "querySubs": {
        const d = sc(l, f, o);
        a.push(d);
        break;
      }
      default: {
        const d = Ur(l, f, i);
        a.push(d);
        const y = {
          createdAt: Date.now(),
          updatedAt: Date.now(),
          size: 0
        };
        c.objects[l] = y;
        break;
      }
    }
  const u = Ur(et, c, i);
  a.push(u), await Promise.all(a), await new Promise((l, f) => {
    s.oncomplete = (d) => l(d), s.onerror = (d) => f(d), s.onabort = (d) => f(d);
  });
}
const Lr = /* @__PURE__ */ new Map();
class qs extends Xa {
  constructor(t, r) {
    super(t, r);
    p(this, "dbName");
    p(this, "_storeName");
    p(this, "_appId");
    p(this, "_prefix");
    p(this, "_dbPromise");
    this.dbName = `instant_${t}_${ec}`, this._storeName = r, this._appId = t, this._dbPromise = this._init();
  }
  _init() {
    return new Promise((t, r) => {
      let s = !1;
      const i = indexedDB.open(this.dbName, 1);
      i.onerror = (o) => {
        r(o);
      }, i.onsuccess = (o) => {
        const c = o.target.result;
        if (s) {
          const u = ic(this._appId, c).catch((l) => {
            nc("Error upgrading store from version 5 to 6.")(l);
          });
          Lr.set(this.dbName, u), u.then(() => t(c)).catch(() => t(c));
        } else {
          const u = Lr.get(this.dbName);
          u ? u.then(() => t(c)).catch(() => t(c)) : t(c);
        }
      }, i.onupgradeneeded = (o) => {
        s = !0, this._upgradeStore(o);
      };
    });
  }
  _upgradeStore(t) {
    const s = t.target.result;
    for (const i of tc)
      s.objectStoreNames.contains(i) || s.createObjectStore(i);
  }
  async getItem(t) {
    const r = await this._dbPromise;
    return new Promise((s, i) => {
      const c = r.transaction([this._storeName], "readonly").objectStore(this._storeName).get(t);
      c.onerror = (u) => {
        i(u);
      }, c.onsuccess = (u) => {
        c.result ? s(c.result) : s(null);
      };
    });
  }
  async setItem(t, r) {
    const s = await this._dbPromise;
    return new Promise((i, o) => {
      const u = s.transaction([this._storeName], "readwrite").objectStore(this._storeName).put(r, t);
      u.onerror = (l) => {
        o(l);
      }, u.onsuccess = (l) => {
        i();
      };
    });
  }
  // Performs all writes in a transaction so that all succeed or none succeed.
  async multiSet(t) {
    const r = await this._dbPromise;
    return new Promise((s, i) => {
      const o = r.transaction([this._storeName], "readwrite"), a = o.objectStore(this._storeName), c = /* @__PURE__ */ new Set();
      for (const [u, l] of t) {
        const f = a.put(l, u);
        c.add(f);
      }
      for (const u of c)
        u.onerror = (l) => {
          o.abort(), i(l);
        }, u.onsuccess = (l) => {
          c.delete(u), c.size === 0 && s();
        };
    });
  }
  async removeItem(t) {
    const r = await this._dbPromise;
    return new Promise((s, i) => {
      const c = r.transaction([this._storeName], "readwrite").objectStore(this._storeName).delete(t);
      c.onerror = (u) => {
        i(u);
      }, c.onsuccess = (u) => {
        s();
      };
    });
  }
  async getAllKeys() {
    const t = await this._dbPromise;
    return new Promise((r, s) => {
      const a = t.transaction([this._storeName], "readonly").objectStore(this._storeName).getAllKeys();
      a.onerror = (c) => {
        s(c);
      }, a.onsuccess = (c) => {
        r(a.result.filter((u) => typeof u == "string"));
      };
    });
  }
}
class zs {
  static async getIsOnline() {
    return navigator.onLine;
  }
  static listen(e) {
    const t = () => {
      e(!0);
    }, r = () => {
      e(!1);
    };
    return addEventListener("online", t), addEventListener("offline", r), () => {
      removeEventListener("online", t), removeEventListener("offline", r);
    };
  }
}
class le extends Error {
  constructor(t, r, s) {
    super(t);
    p(this, "hint");
    p(this, "traceId");
    this.hint = r, s && (this.traceId = s);
    const i = new.target.prototype;
    Object.setPrototypeOf && Object.setPrototypeOf(this, i), Error.captureStackTrace && Error.captureStackTrace(this, le), this.name = "InstantError";
  }
  get [Symbol.toStringTag]() {
    return "InstantError";
  }
}
class Zt extends le {
  constructor(t) {
    var i;
    const r = ((i = t.body) == null ? void 0 : i.message) || `API Error (${t.status})`;
    super(r, t.body.hint, t.body["trace-id"]);
    p(this, "body");
    p(this, "status");
    const s = new.target.prototype;
    Object.setPrototypeOf && Object.setPrototypeOf(this, s), Error.captureStackTrace && Error.captureStackTrace(this, Zt), this.name = "InstantAPIError", this.status = t.status, this.body = t.body;
  }
  get [Symbol.toStringTag]() {
    return "InstantAPIError";
  }
}
async function ke(n, e) {
  const t = await fetch(n, e), r = await t.json();
  return t.status === 200 ? Promise.resolve(r) : Promise.reject(new Zt({ status: t.status, body: r }));
}
function oc({ apiURI: n, appId: e, email: t }) {
  return ke(`${n}/runtime/auth/send_magic_code`, {
    method: "POST",
    headers: { "content-type": "application/json" },
    body: JSON.stringify({ "app-id": e, email: t })
  });
}
async function ac({ apiURI: n, appId: e, email: t, code: r, refreshToken: s, extraFields: i }) {
  return await ke(`${n}/runtime/auth/verify_magic_code`, {
    method: "POST",
    headers: { "content-type": "application/json" },
    body: JSON.stringify({
      "app-id": e,
      email: t,
      code: r,
      ...s ? { "refresh-token": s } : {},
      ...i ? { "extra-fields": i } : {}
    })
  });
}
async function cc({ apiURI: n, appId: e, refreshToken: t }) {
  return await ke(`${n}/runtime/auth/verify_refresh_token`, {
    method: "POST",
    headers: { "content-type": "application/json" },
    body: JSON.stringify({
      "app-id": e,
      "refresh-token": t
    })
  });
}
async function uc({ apiURI: n, appId: e }) {
  return await ke(`${n}/runtime/auth/sign_in_guest`, {
    method: "POST",
    headers: { "content-type": "application/json" },
    body: JSON.stringify({
      "app-id": e
    })
  });
}
async function Fr({ apiURI: n, appId: e, code: t, codeVerifier: r, refreshToken: s, extraFields: i }) {
  return await ke(`${n}/runtime/oauth/token`, {
    method: "POST",
    headers: { "content-type": "application/json" },
    body: JSON.stringify({
      app_id: e,
      code: t,
      code_verifier: r,
      refresh_token: s,
      ...i ? { extra_fields: i } : {}
    })
  });
}
async function lc({ apiURI: n, appId: e, nonce: t, idToken: r, clientName: s, refreshToken: i, extraFields: o }) {
  return await ke(`${n}/runtime/oauth/id_token`, {
    method: "POST",
    headers: { "content-type": "application/json" },
    body: JSON.stringify({
      app_id: e,
      nonce: t,
      id_token: r,
      client_name: s,
      refresh_token: i,
      ...o ? { extra_fields: o } : {}
    })
  });
}
async function fc({ apiURI: n, appId: e, refreshToken: t }) {
  return await ke(`${n}/runtime/signout`, {
    method: "POST",
    headers: {
      "content-type": "application/json"
    },
    body: JSON.stringify({
      app_id: e,
      refresh_token: t
    })
  });
}
async function dc({ apiURI: n, appId: e, path: t, file: r, refreshToken: s, contentType: i, contentDisposition: o }) {
  const a = {
    app_id: e,
    path: t,
    authorization: `Bearer ${s}`,
    "content-type": i || r.type
  };
  return o && (a["content-disposition"] = o), await ke(`${n}/storage/upload`, {
    method: "PUT",
    headers: a,
    body: r
  });
}
async function hc({ apiURI: n, appId: e, path: t, refreshToken: r }) {
  const { data: s } = await ke(`${n}/storage/files?app_id=${e}&filename=${encodeURIComponent(t)}`, {
    method: "DELETE",
    headers: {
      "content-type": "application/json",
      authorization: `Bearer ${r}`
    }
  });
  return s;
}
async function pc({ apiURI: n, appId: e, fileName: t, refreshToken: r, metadata: s = {} }) {
  const { data: i } = await ke(`${n}/storage/signed-upload-url`, {
    method: "POST",
    headers: {
      "content-type": "application/json",
      authorization: `Bearer ${r}`
    },
    body: JSON.stringify({
      app_id: e,
      filename: t
    })
  });
  return i;
}
async function yc(n, e) {
  return (await fetch(n, {
    method: "PUT",
    body: e,
    headers: {
      "Content-Type": e.type
    }
  })).ok;
}
async function bc({ apiURI: n, appId: e, path: t, refreshToken: r }) {
  const { data: s } = await ke(`${n}/storage/signed-download-url?app_id=${e}&filename=${encodeURIComponent(t)}`, {
    method: "GET",
    headers: {
      "content-type": "application/json",
      authorization: `Bearer ${r}`
    }
  });
  return s;
}
let rr = !1, Vs = !1, Qs = !1;
typeof window < "u" && typeof window.localStorage < "u" && (rr = !!window.localStorage.getItem("devBackend"), Vs = !!window.localStorage.getItem("__instantLogging"), Qs = !!window.localStorage.getItem("__devtoolLocalDash"));
function Nr(n, e) {
  if (!e)
    return n;
  const t = {};
  return e.forEach((r) => {
    t[r] = n[r];
  }), t;
}
function gc(n, e, t) {
  const r = {
    peers: {}
  };
  if (e && "user" in e ? e.user : !0) {
    const i = Nr(n.user ?? {}, e == null ? void 0 : e.keys);
    r.user = { ...i, peerId: t };
  }
  for (const i of Object.keys(n.peers ?? {})) {
    const o = (e == null ? void 0 : e.peers) === void 0, a = Array.isArray(e == null ? void 0 : e.peers) && (e == null ? void 0 : e.peers.includes(i));
    if (o || a) {
      const c = Nr(n.peers[i], e == null ? void 0 : e.keys);
      r.peers[i] = { ...c, peerId: i };
    }
  }
  return r;
}
function _c(n, e) {
  if (n.isLoading !== e.isLoading || n.error !== e.error || (n.user || e.user) && (!n.user || !e.user || !Or(n.user, e.user)) || !ws(n.peers, e.peers))
    return !0;
  for (const r of Object.keys(n.peers))
    if (!Or(n.peers[r], e.peers[r]))
      return !0;
  return !1;
}
class Kr {
  constructor() {
    p(this, "promise");
    p(this, "_resolve");
    p(this, "_reject");
    this.promise = new Promise((e, t) => {
      this._resolve = e, this._reject = t;
    });
  }
  resolve(e) {
    this._resolve(e);
  }
  reject(e) {
    this._reject(e);
  }
}
function Bs(n, e = []) {
  n.forEach((t) => {
    const { data: r } = t, { "datalog-result": s } = r, { "join-rows": i } = s;
    for (const o of i)
      for (const a of o)
        e.push(a);
    Bs(t["child-nodes"], e);
  });
}
function Wr(n) {
  const e = [];
  return Bs(n, e), e;
}
function qr(n) {
  return Object.values(n.links).reduce((e, t) => {
    var r, s;
    return e[r = t.forward.on] ?? (e[r] = {}), e[t.forward.on][t.forward.label] = {
      isForward: !0,
      isSingular: t.forward.has === "one",
      link: t
    }, e[s = t.reverse.on] ?? (e[s] = {}), e[t.reverse.on][t.reverse.label] = {
      isForward: !1,
      isSingular: t.reverse.has === "one",
      link: t
    }, e;
  }, {});
}
const sr = "v0.22.171";
function mc(n, e) {
  return {
    info: n ? (...t) => console.info(...t, e()) : () => {
    },
    debug: n ? (...t) => console.debug(...t, e()) : () => {
    },
    error: n ? (...t) => console.error(...t, e()) : () => {
    }
  };
}
class fe {
  constructor(e, t, r, s = { indexed: !1, unique: !1 }) {
    p(this, "valueType");
    p(this, "required");
    p(this, "isIndexed");
    p(this, "config");
    p(this, "metadata", {});
    this.valueType = e, this.required = t, this.isIndexed = r, this.config = s;
  }
  /**
   * @deprecated Only use this temporarily for attributes that you want
   * to treat as required in frontend code but can’t yet mark as required
   * and enforced for backend
   */
  clientRequired() {
    return new fe(this.valueType, !1, this.isIndexed, this.config);
  }
  optional() {
    return new fe(this.valueType, !1, this.isIndexed, this.config);
  }
  unique() {
    return new fe(this.valueType, this.required, this.isIndexed, {
      ...this.config,
      unique: !0
    });
  }
  indexed() {
    return new fe(this.valueType, this.required, !0, {
      ...this.config,
      indexed: !0
    });
  }
}
class Xt {
  constructor(e, t) {
    p(this, "attrs");
    p(this, "links");
    this.attrs = e, this.links = t;
  }
  asType() {
    return new Xt(this.attrs, this.links);
  }
}
class en {
  constructor(e, t, r) {
    p(this, "entities");
    p(this, "links");
    p(this, "rooms");
    this.entities = e, this.links = t, this.rooms = r;
  }
  /**
   * @deprecated
   * `withRoomSchema` is deprecated. Define your schema in `rooms` directly:
   *
   * @example
   * // Before:
   * const schema = i.schema({
   *   // ...
   * }).withRoomSchema<RoomSchema>()
   *
   * // After
   * const schema = i.schema({
   *  rooms: {
   *    // ...
   *  }
   * })
   *
   * @see https://instantdb.com/docs/presence-and-topics#typesafety
   */
  withRoomSchema() {
    return new en(this.entities, this.links, {});
  }
}
class W extends Error {
  constructor(e, t) {
    const r = t ? `At path '${t}': ${e}` : e;
    super(r), this.name = "QueryValidationError";
  }
}
const zr = [
  "where",
  "order",
  "limit",
  "last",
  "first",
  "offset",
  "after",
  "afterInclusive",
  "before",
  "beforeInclusive",
  "fields",
  "aggregate"
], wc = (n) => n.valueType || "unknown", Gs = (n, e, t = !1) => {
  if (t || n == null)
    return !0;
  switch (e) {
    case "string":
      return typeof n == "string";
    case "number":
      return typeof n == "number" && !isNaN(n);
    case "boolean":
      return typeof n == "boolean";
    case "date":
      return n instanceof Date || typeof n == "string" || typeof n == "number";
    default:
      return !0;
  }
}, Tc = (n, e, t, r, s, i, o) => {
  const a = i.valueType === "json", c = (u, l, f) => {
    if (!Gs(f, l, a))
      throw new W(`Invalid value for operator '${u}' on attribute '${r}' in entity '${s}'. Expected ${l}, but received: ${typeof f}`, o);
  };
  switch (n) {
    case "in":
    case "$in":
      if (!Array.isArray(e))
        throw new W(`Operator '${n}' for attribute '${r}' in entity '${s}' must be an array, but received: ${typeof e}`, o);
      for (const u of e)
        c(n, t, u);
      break;
    case "$not":
    case "$ne":
    case "$gt":
    case "$lt":
    case "$gte":
    case "$lte":
      c(n, t, e);
      break;
    case "$like":
    case "$ilike":
      if (c(n, "string", e), n === "$ilike" && !i.isIndexed)
        throw new W(`Operator '${n}' can only be used with indexed attributes, but '${r}' in entity '${s}' is not indexed`, o);
      break;
    case "$isNull":
      c(n, "boolean", e);
      break;
    default:
      throw new W(`Unknown operator '${n}' for attribute '${r}' in entity '${s}'`, o);
  }
}, nt = (n, e, t, r, s) => {
  const i = wc(t), o = t.valueType === "json";
  if (typeof n == "object" && n !== null && !Array.isArray(n)) {
    if (o)
      return;
    const c = n;
    for (const [u, l] of Object.entries(c))
      Tc(u, l, i, e, r, t, `${s}.${u}`);
  } else if (!Gs(n, i, o))
    throw new W(`Invalid value for attribute '${e}' in entity '${r}'. Expected ${i}, but received: ${typeof n}`, s);
}, Sc = (n, e, t, r, s) => {
  const i = n.split(".");
  if (i.length < 2)
    throw new W(`Invalid dot notation path '${n}'. Must contain at least one dot.`, s);
  let o = t;
  for (let l = 0; l < i.length - 1; l++) {
    const f = i[l], d = r.entities[o];
    if (!d)
      throw new W(`Entity '${o}' does not exist in schema while traversing dot notation path '${n}'.`, s);
    const y = d.links[f];
    if (!y) {
      const b = Object.keys(d.links);
      throw new W(`Link '${f}' does not exist on entity '${o}' in dot notation path '${n}'. Available links: ${b.length > 0 ? b.join(", ") : "none"}`, s);
    }
    o = y.entityName;
  }
  const a = i[i.length - 1], c = r.entities[o];
  if (!c)
    throw new W(`Target entity '${o}' does not exist in schema for dot notation path '${n}'.`, s);
  if (a === "id") {
    if (typeof e == "string" && !st(e))
      throw new W(`Invalid value for id field in entity '${o}'. Expected a UUID, but received: ${e}`, s);
    nt(e, n, new fe("string", !1, !0), t, s);
    return;
  }
  const u = c.attrs[a];
  if (Object.keys(c.links).includes(a)) {
    if (typeof e == "string" && !st(e))
      throw new W(`Invalid value for link '${a}' in entity '${o}'. Expected a UUID, but received: ${e}`, s);
    nt(e, n, new fe("string", !1, !0), t, s);
    return;
  }
  if (!u) {
    const l = Object.keys(c.attrs);
    throw new W(`Attribute '${a}' does not exist on entity '${o}' in dot notation path '${n}'. Available attributes: ${l.length > 0 ? l.join(", ") + ", id" : "id"}`, s);
  }
  nt(e, n, u, t, s);
}, Hs = (n, e, t, r) => {
  for (const [s, i] of Object.entries(n)) {
    if (s === "or" || s === "and") {
      if (Array.isArray(i))
        for (const u of i)
          typeof u == "object" && u !== null && Hs(u, e, t, `${r}.${s}[${u}]`);
      continue;
    }
    if (s === "id") {
      nt(i, "id", new fe("string", !1, !0), e, `${r}.id`);
      continue;
    }
    if (s.includes(".")) {
      Sc(s, i, e, t, `${r}.${s}`);
      continue;
    }
    const o = t.entities[e];
    if (!o)
      continue;
    const a = o.attrs[s], c = o.links[s];
    if (!a && !c) {
      const u = Object.keys(o.attrs), l = Object.keys(o.links);
      throw new W(`Attribute or link '${s}' does not exist on entity '${e}'. Available attributes: ${u.length > 0 ? u.join(", ") : "none"}. Available links: ${l.length > 0 ? l.join(", ") : "none"}`, `${r}.${s}`);
    }
    if (a)
      nt(i, s, a, e, `${r}.${s}`);
    else if (c) {
      if (typeof i == "string" && !st(i))
        throw new W(`Invalid value for link '${s}' in entity '${e}'. Expected a UUID, but received: ${i}`, `${r}.${s}`);
      const u = new fe("string", !1, !0);
      nt(i, s, u, e, `${r}.${s}`);
    }
  }
}, vc = (n, e, t, r, s = 0) => {
  for (const o of Object.keys(n))
    if (!zr.includes(o))
      throw new W(`Invalid query parameter '${o}' in $ object. Valid parameters are: ${zr.join(", ")}. Found: ${o}`, r);
  const i = [
    // 'limit', // only supported client side
    "offset",
    "before",
    "beforeInclusive",
    "after",
    "afterInclusive",
    "first",
    "last"
  ];
  for (const o of i)
    if (n[o] !== void 0 && s > 0)
      throw new W(`'${o}' can only be used on top-level namespaces. It cannot be used in nested queries.`, r);
  if (n.where && t) {
    if (typeof n.where != "object" || n.where === null)
      throw new W(`'where' clause must be an object in entity '${e}', but received: ${typeof n.where}`, r ? `${r}.where` : void 0);
    Hs(n.where, e, t, r ? `${r}.where` : "where");
  }
}, Js = (n, e, t, r, s = 0) => {
  var i;
  if (!n || typeof n != "object")
    throw new W(`Query part for entity '${e}' must be an object, but received: ${typeof n}`, r);
  for (const o of Object.keys(n))
    if (o !== "$") {
      if (t && !(o in t.entities[e].links)) {
        const c = Object.keys(t.entities[e].links);
        throw new W(`Link '${o}' does not exist on entity '${e}'. Available links: ${c.length > 0 ? c.join(", ") : "none"}`, `${r}.${o}`);
      }
      const a = n[o];
      if (typeof a == "object" && a !== null) {
        const c = (i = t == null ? void 0 : t.entities[e].links[o]) == null ? void 0 : i.entityName;
        c && Js(a, c, t, `${r}.${o}`, s + 1);
      }
    } else {
      const a = n[o];
      if (typeof a != "object" || a === null)
        throw new W(`Query parameter '$' must be an object in entity '${e}', but received: ${typeof a}`, `${r}.$`);
      vc(a, e, t, `${r}.$`, s);
    }
}, Vr = (n, e) => {
  if (typeof n != "object" || n === null)
    throw new W(`Query must be an object, but received: ${typeof n}${n === null ? " (null)" : ""}`);
  if (Array.isArray(n))
    throw new W(`Query must be an object, but received: ${typeof n}`);
  const t = n;
  for (const r of Object.keys(t)) {
    if (Array.isArray(n[r]))
      throw new W(`Query keys must be strings, but found key of type: ${typeof r}`, r);
    if (typeof r != "string")
      throw new W(`Query keys must be strings, but found key of type: ${typeof r}`, r);
    if (r !== "$$ruleParams") {
      if (e && !e.entities[r]) {
        const s = Object.keys(e.entities);
        throw new W(`Entity '${r}' does not exist in schema. Available entities: ${s.length > 0 ? s.join(", ") : "none"}`, r);
      }
      Js(t[r], r, e, r, 0);
    }
  }
}, Qr = (n) => typeof n != "string" ? !1 : Gt(n) ? !0 : st(n);
class Te extends Error {
  constructor(e) {
    super(e), this.name = "TransactionValidationError";
  }
}
const Ys = (n) => n.length > 0 ? n.join(", ") : "none", Ec = (n, e) => new Te(`Entity '${n}' does not exist in schema. Available entities: ${Ys(e)}`), fn = {
  string: (n) => typeof n == "string",
  number: (n) => typeof n == "number" && !isNaN(n),
  boolean: (n) => typeof n == "boolean",
  date: (n) => n instanceof Date || typeof n == "string" || typeof n == "number",
  json: () => !0
}, Oc = (n, e) => {
  var t;
  return n == null ? !0 : ((t = fn[e.valueType]) == null ? void 0 : t.call(fn, n)) ?? !1;
}, Zs = (n, e) => {
  const t = e.entities[n];
  if (!t)
    throw Ec(n, Object.keys(e.entities));
  return t;
}, dn = (n, e, t) => {
  const r = Zs(n, t);
  if (typeof e != "object" || e === null)
    throw new Te(`Arguments for data operation on entity '${n}' must be an object, but received: ${typeof e}`);
  for (const [s, i] of Object.entries(e)) {
    if (s === "id")
      continue;
    const o = r.attrs[s];
    if (o && !Oc(i, o))
      throw new Te(`Invalid value for attribute '${s}' in entity '${n}'. Expected ${o.valueType}, but received: ${typeof i}`);
  }
}, Br = (n, e, t) => {
  const r = Zs(n, t);
  if (typeof e != "object" || e === null)
    throw new Te(`Arguments for link operation on entity '${n}' must be an object, but received: ${typeof e}`);
  for (const [s, i] of Object.entries(e)) {
    if (!r.links[s]) {
      const a = Object.keys(r.links);
      throw new Te(`Link '${s}' does not exist on entity '${n}'. Available links: ${Ys(a)}`);
    }
    if (i != null) {
      if (Array.isArray(i)) {
        for (const a of i)
          if (!Qr(a))
            throw new Te(`Invalid entity ID in link '${s}' for entity '${n}'. Expected a UUID or a lookup, but received: ${a}`);
      } else if (!Qr(i))
        throw new Te(`Invalid UUID in link '${s}' for entity '${n}'. Expected a UUID, but received: ${i}`);
    }
  }
}, kc = {
  create: dn,
  update: dn,
  merge: dn,
  link: Br,
  unlink: Br,
  delete: () => {
  }
}, Ac = (n, e) => {
  if (!e)
    return;
  const [t, r, s, i] = n;
  if (!Array.isArray(s) && !st(s))
    throw new Te(`Invalid id for entity '${r}'. Expected a UUID, but received: ${s}`);
  if (typeof r != "string")
    throw new Te(`Entity name must be a string, but received: ${typeof r}`);
  const o = kc[t];
  o && i !== void 0 && o(r, i, e);
}, Cc = (n, e) => {
  const t = Array.isArray(n) ? n : [n];
  for (const r of t) {
    if (!r || typeof r != "object")
      throw new Te(`Transaction chunk must be an object, but received: ${typeof r}`);
    if (!Array.isArray(r.__ops))
      throw new Te(`Transaction chunk must have __ops array, but received: ${typeof r.__ops}`);
    for (const s of r.__ops) {
      if (!Array.isArray(s))
        throw new Te(`Transaction operation must be an array, but received: ${typeof s}`);
      Ac(s, e);
    }
  }
};
let Xs = 0;
class Gr {
  constructor(e) {
    p(this, "type", "ws");
    p(this, "conn");
    p(this, "id");
    p(this, "onopen");
    p(this, "onmessage");
    p(this, "onclose");
    p(this, "onerror");
    this.id = `${this.type}_${Xs++}`, this.conn = new WebSocket(e), this.conn.onopen = (t) => {
      this.onopen && this.onopen({ target: this });
    }, this.conn.onmessage = (t) => {
      this.onmessage && this.onmessage({
        target: this,
        message: JSON.parse(t.data.toString())
      });
    }, this.conn.onclose = (t) => {
      this.onclose && this.onclose({ target: this });
    }, this.conn.onerror = (t) => {
      this.onerror && this.onerror({ target: this });
    };
  }
  close() {
    this.conn.close();
  }
  isOpen() {
    return this.conn.readyState === (WebSocket.OPEN ?? 1);
  }
  isConnecting() {
    return this.conn.readyState === (WebSocket.CONNECTING ?? 0);
  }
  send(e) {
    return this.conn.send(JSON.stringify(e));
  }
}
class Ic {
  constructor(e, t, r) {
    p(this, "type", "sse");
    p(this, "initParams", null);
    p(this, "sendQueue", []);
    p(this, "sendPromise");
    p(this, "closeFired", !1);
    p(this, "sseInitTimeout");
    p(this, "ES");
    p(this, "messageUrl");
    p(this, "conn");
    p(this, "url");
    p(this, "id");
    p(this, "onopen");
    p(this, "onmessage");
    p(this, "onclose");
    p(this, "onerror");
    this.id = `${this.type}_${Xs++}`, this.url = t, this.messageUrl = r || this.url, this.ES = e, this.conn = new e(t), this.sseInitTimeout = setTimeout(() => {
      this.initParams || this.handleError();
    }, 1e4), this.conn.onmessage = (s) => {
      const i = JSON.parse(s.data);
      if (Array.isArray(i))
        for (const o of i)
          this.handleMessage(o);
      else
        this.handleMessage(i);
    }, this.conn.onerror = (s) => {
      this.handleError();
    };
  }
  handleMessage(e) {
    if (e.op === "sse-init") {
      this.initParams = {
        machineId: e["machine-id"],
        sessionId: e["session-id"],
        sseToken: e["sse-token"]
      }, this.onopen && this.onopen({ target: this }), clearTimeout(this.sseInitTimeout);
      return;
    }
    this.onmessage && this.onmessage({
      target: this,
      message: e
    });
  }
  // Runs the onerror and closes the connection
  handleError() {
    try {
      this.onerror && this.onerror({ target: this });
    } finally {
      this.handleClose();
    }
  }
  handleClose() {
    this.conn.close(), this.onclose && !this.closeFired && (this.closeFired = !0, this.onclose({ target: this }));
  }
  async postMessages(e) {
    var t, r, s;
    try {
      (await fetch(this.messageUrl, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          machine_id: (t = this.initParams) == null ? void 0 : t.machineId,
          session_id: (r = this.initParams) == null ? void 0 : r.sessionId,
          sse_token: (s = this.initParams) == null ? void 0 : s.sseToken,
          messages: e
        })
      })).ok || this.handleError();
    } catch {
      this.handleError();
    }
  }
  async flushQueue() {
    if (this.sendPromise || !this.sendQueue.length)
      return;
    const e = this.sendQueue;
    this.sendQueue = [];
    const t = this.postMessages(e);
    this.sendPromise = t, t.then(() => {
      this.sendPromise = null, this.flushQueue();
    });
  }
  send(e) {
    if (!this.isOpen() || !this.initParams)
      throw this.isConnecting() ? new Error("Failed to execute 'send' on 'EventSource': Still in CONNECTING state.") : this.conn.readyState === this.ES.CLOSED ? new Error("EventSource is already in CLOSING or CLOSED state.") : new Error("EventSource is in invalid state.");
    this.sendQueue.push(e), this.flushQueue();
  }
  isOpen() {
    return this.conn.readyState === this.ES.OPEN && this.initParams !== null;
  }
  isConnecting() {
    return this.conn.readyState === this.ES.CONNECTING || this.conn.readyState === this.ES.OPEN && this.initParams === null;
  }
  close() {
    this.handleClose();
  }
}
function Pc(n, e) {
  const t = n.values;
  if (t) {
    const r = ks(t.attrsStore, null);
    if (r) {
      for (const s of t.entities || [])
        s.store.useDateObjects = e, s.store = Os(r, s.store);
      t.attrsStore = r;
    }
  }
  return n;
}
function Mc(n, e) {
  var t;
  if (e.values) {
    const r = [];
    for (const s of (t = e.values) == null ? void 0 : t.entities) {
      const i = Es(s.store);
      r.push({ ...s, store: i });
    }
    return {
      ...e,
      values: { attrsStore: e.values.attrsStore.toJSON(), entities: r }
    };
  } else
    return e;
}
function Rc(n, e, t) {
  var i, o;
  const r = (i = e == null ? void 0 : e.state) == null ? void 0 : i.txId, s = (o = t == null ? void 0 : t.state) == null ? void 0 : o.txId;
  return r && (!s || r > s) ? e : s && (!r || s > r) ? t : e || t;
}
function hn(n, e, t) {
  return Ns({ store: e, attrsStore: t, pageInfo: null, aggregate: null }, n.query).data[n.table][0];
}
function Hr(n, e, t, r) {
  var o;
  const s = (o = te(t, n.table, "id")) == null ? void 0 : o.id;
  if (!s)
    return -1;
  const i = ot(e.eav, [r, s, r]);
  return i ? i[3] : -1;
}
function Jr(n, e, t) {
  for (const { action: r, triple: s } of t)
    switch (r) {
      case "added":
        Is(n, e, s);
        break;
      case "removed":
        As(n, e, s);
        break;
    }
}
function jc(n, e, t) {
  var s, i;
  const r = {};
  for (const { action: o, triple: a } of t) {
    const [c, u, l] = a, f = (i = (s = e.getAttr(u)) == null ? void 0 : s["forward-identity"]) == null ? void 0 : i[2];
    if (!f)
      continue;
    const d = r[c] ?? {};
    r[c] = d;
    const y = d[f] ?? {};
    switch (o) {
      case "added":
        y.newValue = l;
        break;
      case "removed":
        y.oldValue === void 0 && (y.oldValue = l);
        break;
    }
    d[f] = y;
  }
  for (const [o, a] of Object.entries(r))
    for (const [c, { oldValue: u, newValue: l }] of Object.entries(a))
      u === l && delete a[c];
  return r;
}
function Rt(n, e) {
  return { [n.table]: e.map((t) => t.entity) };
}
function xc(n, e) {
  var r;
  if (n.orderFieldType)
    return n.orderFieldType;
  const t = n.orderField === "serverCreatedAt" ? "number" : (r = te(e(), n.table, n.orderField)) == null ? void 0 : r["checked-data-type"];
  return n.orderFieldType = t, t;
}
function $c(n, e, t) {
  const r = e;
  if (n.orderField === "serverCreatedAt") {
    t.sort(n.orderDirection === "asc" ? function(o, a) {
      return yt(o.entity.id, o.serverCreatedAt, a.entity.id, a.serverCreatedAt, r);
    } : function(o, a) {
      return yt(a.entity.id, a.serverCreatedAt, o.entity.id, o.serverCreatedAt, r);
    });
    return;
  }
  const s = n.orderField;
  t.sort(n.orderDirection === "asc" ? function(o, a) {
    return yt(o.entity.id, o.entity[s], a.entity.id, a.entity[s], r);
  } : function(o, a) {
    return yt(a.entity.id, a.entity[s], o.entity.id, o.entity[s], r);
  });
}
var Ue;
(function(n) {
  n.InitialSyncBatch = "InitialSyncBatch", n.InitialSyncComplete = "InitialSyncComplete", n.LoadFromStorage = "LoadFromStorage", n.SyncTransaction = "SyncTransaction", n.Error = "Error";
})(Ue || (Ue = {}));
class Dc {
  constructor(e, t, r, s, i, o) {
    p(this, "trySend");
    p(this, "subs");
    // Using any for the SyncCallback because we'd need Reactor to be typed
    p(this, "callbacks", {});
    p(this, "config");
    p(this, "idToHash", {});
    p(this, "log");
    p(this, "createStore");
    p(this, "getAttrs");
    this.trySend = e, this.config = r, this.log = s, this.createStore = i, this.getAttrs = o, this.subs = new jn({
      persister: t,
      merge: Rc,
      serialize: Mc,
      parse: (a, c) => Pc(c, this.config.useDateObjects),
      objectSize: (a) => {
        var c;
        return ((c = a.values) == null ? void 0 : c.entities.length) || 0;
      },
      logger: s,
      gc: {
        maxAgeMs: 1e3 * 60 * 60 * 24 * 7 * 52,
        // 1 year
        maxEntries: 1e3,
        // Size of each sub is the number of entity
        maxSize: 1e6
        // 1 million entities
      }
    });
  }
  beforeUnload() {
    this.subs.flush();
  }
  subscribe(e, t) {
    const r = ee(e);
    return this.callbacks[r] = this.callbacks[r] || [], this.callbacks[r].push(t), this.initSubscription(e, r, t), (s) => {
      this.unsubscribe(r, t, s == null ? void 0 : s.keepSubscription);
    };
  }
  unsubscribe(e, t, r) {
    const s = (this.callbacks[e] || []).filter((i) => i !== t);
    if (this.callbacks[e] = s, !s.length) {
      delete this.callbacks[e];
      const i = this.subs.currentValue[e];
      i != null && i.state && this.clearSubscriptionData(i.state.subscriptionId, !!r), r || this.subs.updateInPlace((o) => {
        delete o[e];
      });
    }
  }
  sendStart(e) {
    this.trySend(H(), {
      op: "start-sync",
      q: e
    });
  }
  sendResync(e, t, r) {
    this.idToHash[t.subscriptionId] = e.hash, this.trySend(t.subscriptionId, {
      op: "resync-table",
      "subscription-id": t.subscriptionId,
      "tx-id": r,
      token: t.token
    });
  }
  sendRemove(e, t) {
    this.trySend(H(), {
      op: "remove-sync",
      "subscription-id": e.subscriptionId,
      "keep-subscription": t
    });
  }
  async initSubscription(e, t, r) {
    var u, l, f, d;
    await this.subs.waitForKeyToLoad(t);
    const s = this.subs.currentValue[t];
    if (s && s.state && s.state.txId) {
      this.sendResync(s, s.state, s.state.txId), (u = s.values) != null && u.entities && r && r({
        type: Ue.LoadFromStorage,
        data: Rt(s, (l = s.values) == null ? void 0 : l.entities)
      });
      return;
    }
    const i = Object.keys(e)[0], o = ((d = (f = e[i]) == null ? void 0 : f.$) == null ? void 0 : d.order) || { serverCreatedAt: "asc" }, [a, c] = Object.entries(o)[0];
    this.subs.updateInPlace((y) => {
      y[t] = {
        query: e,
        hash: t,
        table: i,
        orderDirection: c,
        orderField: a,
        createdAt: Date.now(),
        updatedAt: Date.now()
      };
    }), this.sendStart(e);
  }
  async flushPending() {
    for (const e of Object.keys(this.callbacks)) {
      await this.subs.waitForKeyToLoad(e);
      const t = this.subs.currentValue[e];
      t ? await this.initSubscription(t.query, t.hash) : this.log.error("Missing sub for hash in flushPending", e);
    }
  }
  onStartSyncOk(e) {
    const t = e["subscription-id"], r = e.q, s = ee(r);
    this.idToHash[t] = s, this.subs.updateInPlace((i) => {
      const o = i[s];
      if (!o)
        return this.log.error("Missing sub for hash", s, "subscription-id", t, "query", r), i;
      o.state = {
        subscriptionId: t,
        token: e.token
      };
    });
  }
  notifyCbs(e, t) {
    for (const r of this.callbacks[e] || [])
      r(t);
  }
  onSyncLoadBatch(e) {
    const t = e["subscription-id"], r = e["join-rows"], s = this.idToHash[t];
    if (!s) {
      this.log.error("Missing hash for subscription", e);
      return;
    }
    const i = [], o = this.subs.currentValue[s];
    if (!o) {
      this.log.error("Missing sub for hash", s, e);
      return;
    }
    const a = o.values ?? {
      entities: [],
      attrsStore: this.getAttrs()
    };
    o.values = a;
    const c = a.entities;
    for (const u of r) {
      const l = this.createStore(u), f = hn(o, l, a.attrsStore);
      c.push({
        store: l,
        entity: f,
        serverCreatedAt: Hr(o, l, a.attrsStore, f.id)
      }), i.push(f);
    }
    this.subs.updateInPlace((u) => {
      u[s] = o, u[s].updatedAt = Date.now();
    }), o.values && this.notifyCbs(s, {
      type: Ue.InitialSyncBatch,
      data: Rt(o, o.values.entities),
      batch: i
    });
  }
  onSyncInitFinish(e) {
    var i;
    const t = e["subscription-id"], r = this.idToHash[t];
    if (!r) {
      this.log.error("Missing hash for subscription", e);
      return;
    }
    this.subs.updateInPlace((o) => {
      const a = o[r];
      if (!a) {
        this.log.error("Missing sub for hash", r, e);
        return;
      }
      const c = a.state;
      if (!c)
        return this.log.error("Sub never set init, missing result", a, e), o;
      c.txId = e["tx-id"], a.updatedAt = Date.now();
    });
    const s = this.subs.currentValue[r];
    s && this.notifyCbs(r, {
      type: Ue.InitialSyncComplete,
      data: Rt(s, ((i = s.values) == null ? void 0 : i.entities) || [])
    });
  }
  onSyncUpdateTriples(e) {
    var o;
    const t = e["subscription-id"], r = this.idToHash[t];
    if (!r) {
      this.log.error("Missing hash for subscription", e);
      return;
    }
    const s = this.subs.currentValue[r];
    if (!s) {
      this.log.error("Missing sub for hash", r, e);
      return;
    }
    const i = s.state;
    if (!i) {
      this.log.error("Missing state for sub", s, e);
      return;
    }
    for (const a of e.txes) {
      if (i.txId && i.txId >= a["tx-id"])
        continue;
      i.txId = a["tx-id"];
      const c = [], u = {};
      for (const w of a.changes) {
        const S = u[w.triple[0]] ?? [];
        u[w.triple[0]] = S, S.push(w);
      }
      const l = s.values ?? {
        entities: [],
        attrsStore: this.getAttrs()
      }, f = l.entities;
      s.values = l;
      const d = [];
      e: for (const [w, S] of Object.entries(u))
        for (let _ = 0; _ < f.length; _++) {
          const m = f[_];
          if (po(m.store, w)) {
            Jr(m.store, l.attrsStore, S);
            const A = hn(s, m.store, l.attrsStore), v = jc(m.store, l.attrsStore, S)[w];
            A ? (d.push({
              oldEntity: m.entity,
              newEntity: A,
              changedFields: v || {}
            }), m.entity = A) : c.push(_), delete u[w];
            continue e;
          }
        }
      const y = [];
      for (const [w, S] of Object.entries(u)) {
        const _ = this.createStore([]);
        Jr(_, l.attrsStore, S);
        const m = hn(s, _, l.attrsStore);
        if (!m) {
          this.log.error("No entity found after applying change", {
            sub: s,
            changes: S,
            store: _
          });
          continue;
        }
        f.push({
          store: _,
          entity: m,
          serverCreatedAt: Hr(s, _, l.attrsStore, m.id)
        }), y.push(m);
      }
      const b = [];
      for (const w of c.sort().reverse())
        b.push(f[w].entity), f.splice(w, 1);
      const g = xc(s, this.getAttrs);
      $c(s, g, f), this.notifyCbs(r, {
        type: Ue.SyncTransaction,
        data: Rt(s, (o = s.values) == null ? void 0 : o.entities),
        added: y,
        removed: b,
        updated: d
      });
    }
    this.subs.updateInPlace((a) => {
      a[r] = s, a[r].updatedAt = Date.now();
    });
  }
  clearSubscriptionData(e, t) {
    const r = this.idToHash[e];
    if (r) {
      delete this.idToHash[e];
      const s = this.subs.currentValue[r];
      if (s.state && this.sendRemove(s.state, t), t ? this.subs.unloadKey(r) : this.subs.updateInPlace((i) => {
        delete i[r];
      }), s)
        return s;
    }
  }
  onStartSyncError(e) {
    const t = ee(e["original-event"].q), r = {
      message: e.message || "Uh-oh, something went wrong. Ping Joe & Stopa.",
      status: e.status,
      type: e.type,
      hint: e.hint
    }, s = Object.keys(e["original-event"].q)[0];
    this.notifyCbs(t, {
      type: Ue.Error,
      data: { [s]: [] },
      error: r
    });
  }
  onResyncError(e) {
    const t = e["original-event"]["subscription-id"], r = this.clearSubscriptionData(t, !1);
    r && this.initSubscription(r.query, r.hash);
  }
}
function Uc({ WStream: n, opts: e, startStream: t, appendStream: r, registerStream: s }) {
  const i = e.clientId;
  let o = null, a = null, c = null;
  const u = H();
  let l = !1, f = !1;
  const d = [], y = [], b = [];
  let g = !1, w = 0, S = 0;
  const _ = [], m = new TextEncoder();
  function A() {
    f = !0;
    for (const O of d)
      O(a ?? void 0);
  }
  function v(O) {
    return d.push(O), f && O(a ?? void 0), () => {
      const C = d.indexOf(O);
      C !== -1 && d.splice(C, 1);
    };
  }
  function I(O) {
    return b.push(O), () => {
      const C = b.indexOf(O);
      C !== -1 && b.splice(C, 1);
    };
  }
  e.waitUntil && e.waitUntil(new Promise((O) => {
    b.push(O);
  }));
  function j() {
    for (const O of b)
      try {
        O();
      } catch {
      }
  }
  function N(O) {
    return y.push(O), o && O(o), () => {
      const C = y.indexOf(O);
      C !== -1 && y.splice(C, 1);
    };
  }
  function Q(O) {
    o = O;
    for (const C of y)
      C(o);
  }
  function ge() {
    g = !0;
  }
  function Ae(O) {
    let C = w, L = 0, V = 0;
    for (const { byteLen: J } of _) {
      const ae = C + J;
      if (ae > O)
        break;
      C = ae, L++, V += J;
    }
    L > 0 && (w += V, S -= V, _.splice(0, L));
  }
  function re(O, C) {
    a = C, A(), O.error(C), j();
  }
  async function se() {
    const O = await t({
      clientId: i,
      reconnectToken: u,
      ruleParams: e.ruleParams
    });
    switch (O.type) {
      case "ok": {
        const { streamId: C, offset: L } = O;
        o = C, Ae(L), _.length && r({
          streamId: C,
          chunks: _.map((V) => V.chunk),
          offset: w
        }), g = !1;
        break;
      }
      case "disconnect": {
        ge();
        break;
      }
      case "error": {
        c && re(c, O.error);
        break;
      }
    }
  }
  function B() {
    ge(), se();
  }
  function k({ offset: O, done: C }) {
    Ae(O), C && (l = !0, j());
  }
  function M(O) {
    return l ? (re(O, new le("Stream has been closed.")), null) : o || (re(O, new le("Stream has not been initialized.")), null);
  }
  async function x(O) {
    c = O;
    let C = !0, L = 0;
    for (; C; ) {
      let V = Date.now() + Math.min(15e3, 500 * (L - 1));
      C = !1;
      const J = await t({
        clientId: e.clientId,
        reconnectToken: u,
        ruleParams: e.ruleParams
      });
      switch (J.type) {
        case "ok": {
          const { streamId: ae, offset: at } = J;
          if (at !== 0) {
            const Se = new le("Write stream is corrupted");
            re(O, Se);
            return;
          }
          Q(ae), s(ae, {
            onDisconnect: ge,
            onFlush: k,
            onConnectionReconnect: se,
            onAppendFailed: B
          }), g = !1;
          return;
        }
        case "disconnect": {
          C = !0, ge(), L++, await new Promise((ae) => {
            setTimeout(ae, Math.max(0, V - Date.now()));
          });
          break;
        }
        case "error": {
          re(O, J.error);
          return;
        }
      }
    }
  }
  class q extends n {
    constructor(C, L) {
      super(C, L);
    }
    async streamId() {
      return o || new Promise((C, L) => {
        const V = [], J = () => {
          for (const Se of V)
            Se();
        }, ae = (Se) => {
          C(Se), J();
        }, at = (Se) => {
          L(Se || new le("Stream is closed.")), J();
        };
        V.push(N(ae)), V.push(v(at));
      });
    }
  }
  return {
    stream: new q({
      // TODO(dww): accept a storage so that write streams can survive across
      //            browser restarts
      async start(O) {
        try {
          await x(O);
        } catch (C) {
          re(O, C);
        }
      },
      write(O, C) {
        const L = M(C);
        if (L) {
          const V = m.encode(O).length;
          _.push({ chunk: O, byteLen: V });
          const J = w + S;
          S += V, g || r({ streamId: L, chunks: [O], offset: J });
        }
      },
      close() {
        o ? r({
          streamId: o,
          chunks: [],
          offset: w + S,
          isDone: !0
        }) : j(), A();
      },
      abort(O) {
        o ? r({
          streamId: o,
          chunks: [],
          offset: w + S,
          isDone: !0,
          abortReason: O
        }) : j(), A();
      }
    }),
    addCompleteCb: I,
    closed() {
      return f;
    }
  };
}
class Lc {
  constructor() {
    p(this, "items", []);
    p(this, "resolvers", []);
    p(this, "isClosed", !1);
  }
  push(e) {
    if (this.isClosed)
      return;
    const t = this.resolvers.shift();
    t ? t({ value: e, done: !1 }) : this.items.push(e);
  }
  close() {
    for (this.isClosed = !0; this.resolvers.length > 0; )
      this.resolvers.shift()({ value: void 0, done: !0 });
  }
  async *[Symbol.asyncIterator]() {
    for (; ; )
      if (this.items.length > 0)
        yield this.items.shift();
      else {
        if (this.isClosed)
          return;
        {
          const { value: e, done: t } = await new Promise((r) => {
            this.resolvers.push(r);
          });
          if (t || !e)
            return;
          yield e;
        }
      }
  }
}
function Fc({ RStream: n, opts: e, startStream: t, cancelStream: r }) {
  let s = e.byteOffset || 0, i = !1;
  const o = new TextDecoder("utf-8"), a = new TextEncoder();
  let c, u = !1;
  const l = [];
  function f() {
    u = !0;
    for (const _ of l)
      _();
  }
  function d(_) {
    return l.push(_), () => {
      const m = l.indexOf(_);
      m !== -1 && l.splice(m, 1);
    };
  }
  function y(_, m) {
    _.error(m), f();
  }
  let b = 0;
  async function g(_, m) {
    c = H();
    const A = { ..._ || {}, eventId: c };
    for await (const v of t(A)) {
      if (i)
        return;
      if (v.type === "reconnect")
        return { retry: !0 };
      if (v.type === "error") {
        y(m, v.error);
        return;
      }
      if (v.offset > s) {
        y(m, new le("Stream is corrupted.")), i = !0;
        return;
      }
      let I = s - v.offset;
      if (v.files && v.files.length) {
        const j = new AbortController();
        let N = fetch(v.files[0].url, {
          signal: j.signal
        });
        for (let Q = 0; Q < v.files.length; Q++) {
          const ge = v.files[Q + 1], re = await N;
          if (ge && (N = fetch(ge.url, { signal: j.signal })), !re.ok) {
            if (b++, b > 10) {
              y(m, new le("Unable to process stream."));
              return;
            }
            return { retry: !0 };
          }
          if (re.body) {
            const se = re.body.getReader();
            try {
              for (; ; ) {
                const { done: B, value: k } = await se.read();
                if (B)
                  break;
                if (i) {
                  j.abort();
                  return;
                }
                let M = k;
                if (I > 0 && (M = k.subarray(I), I -= k.length - M.length), !M.length)
                  continue;
                s += M.length;
                const x = o.decode(M);
                m.enqueue(x);
              }
            } finally {
              se.releaseLock();
            }
          } else {
            const se = await re.arrayBuffer();
            let B = se;
            if (i) {
              j.abort();
              return;
            }
            if (I > 0 && (B = new Uint8Array(se).subarray(I), I -= se.byteLength - B.length), !B.byteLength)
              continue;
            s += B.byteLength;
            const k = o.decode(B);
            m.enqueue(k);
          }
        }
      }
      if (b = 0, v.content) {
        let j = v.content, N = a.encode(v.content);
        if (I > 0) {
          const Q = N.subarray(I);
          if (I -= N.length - Q.length, !Q.length)
            continue;
          N = Q, j = o.decode(Q);
        }
        s += N.length, m.enqueue(j);
      }
    }
  }
  async function w(_) {
    let m = !0, A = 0;
    for (; m; ) {
      m = !1;
      let v = Date.now() + Math.min(15e3, 500 * (A - 1));
      const I = await g({ ...e, offset: s }, _);
      I != null && I.retry && (m = !0, A++, v < Date.now() - 3e5 && (A = 0), await new Promise((j) => {
        setTimeout(j, Math.max(0, v - Date.now()));
      }));
    }
    !i && !u && (_.close(), f());
  }
  return {
    stream: new n({
      start(_) {
        w(_);
      },
      cancel(_) {
        i = !0, c && r({ eventId: c }), f();
      }
    }),
    addCloseCb: d,
    closed() {
      return u;
    }
  };
}
class Nc {
  constructor({ WStream: e, RStream: t, trySend: r, log: s }) {
    p(this, "trySend");
    p(this, "WStream");
    p(this, "RStream");
    p(this, "writeStreams", {});
    p(this, "startWriteStreamCbs", {});
    p(this, "readStreamIterators", {});
    p(this, "log");
    p(this, "activeStreams", /* @__PURE__ */ new Set());
    this.WStream = e, this.RStream = t, this.trySend = r, this.log = s;
  }
  createWriteStream(e) {
    const { stream: t, addCompleteCb: r } = Uc({
      WStream: this.WStream,
      startStream: this.startWriteStream.bind(this),
      appendStream: this.appendStream.bind(this),
      registerStream: this.registerWriteStream.bind(this),
      opts: e
    });
    return this.activeStreams.add(t), r(() => {
      this.activeStreams.delete(t);
    }), t;
  }
  createReadStream(e) {
    const { stream: t, addCloseCb: r } = Fc({
      RStream: this.RStream,
      opts: e,
      startStream: this.startReadStream.bind(this),
      cancelStream: this.cancelReadStream.bind(this)
    });
    return this.activeStreams.add(t), r(() => {
      this.activeStreams.delete(t);
    }), t;
  }
  startWriteStream(e) {
    const t = H();
    let r = null;
    const s = new Promise((o) => {
      r = o;
    });
    this.startWriteStreamCbs[t] = r;
    const i = {
      op: "start-stream",
      "client-id": e.clientId,
      "reconnect-token": e.reconnectToken
    };
    return e.ruleParams && (i["rule-params"] = e.ruleParams), this.trySend(t, i), s;
  }
  registerWriteStream(e, t) {
    this.writeStreams[e] = t;
  }
  appendStream({ streamId: e, chunks: t, isDone: r, offset: s, abortReason: i }) {
    const o = {
      op: "append-stream",
      "stream-id": e,
      chunks: t,
      offset: s,
      done: !!r
    };
    i && (o["abort-reason"] = i), this.trySend(H(), o);
  }
  onAppendFailed(e) {
    const t = this.writeStreams[e["stream-id"]];
    t && t.onAppendFailed();
  }
  onStartStreamOk(e) {
    const t = this.startWriteStreamCbs[e["client-event-id"]];
    if (!t) {
      this.log.info("No stream for start-stream-ok", e);
      return;
    }
    t({ type: "ok", streamId: e["stream-id"], offset: e.offset }), delete this.startWriteStreamCbs[e["client-event-id"]];
  }
  onStreamFlushed(e) {
    const t = e["stream-id"], r = this.writeStreams[t];
    if (!r) {
      this.log.info("No stream cbs for stream-flushed", e);
      return;
    }
    r.onFlush({ offset: e.offset, done: e.done }), e.done && delete this.writeStreams[t];
  }
  startReadStream({ eventId: e, clientId: t, streamId: r, offset: s, ruleParams: i }) {
    const o = { op: "subscribe-stream" };
    if (!r && !t)
      throw new Error("Must provide one of streamId or clientId to subscribe to the stream.");
    r && (o["stream-id"] = r), t && (o["client-id"] = t), s && (o.offset = s), i && (o["rule-params"] = i);
    const a = new Lc();
    return this.readStreamIterators[e] = a, this.trySend(e, o), a;
  }
  cancelReadStream({ eventId: e }) {
    const t = {
      op: "unsubscribe-stream",
      "subscribe-event-id": e
    };
    this.trySend(H(), t), delete this.readStreamIterators[e];
  }
  onStreamAppend(e) {
    var s;
    const t = e["client-event-id"], r = this.readStreamIterators[t];
    if (!r) {
      this.log.info("No iterator for read stream", e);
      return;
    }
    if (e.error) {
      e.retry ? r.push({ type: "reconnect" }) : r.push({
        type: "error",
        error: new le(e.error)
      }), r.close(), delete this.readStreamIterators[t];
      return;
    }
    ((s = e.files) != null && s.length || e.content) && r.push({
      type: "append",
      offset: e.offset,
      files: e.files,
      content: e.content
    }), e.done && (r.close(), delete this.readStreamIterators[t]);
  }
  onConnectionStatusChange(e) {
    for (const t of Object.values(this.startWriteStreamCbs))
      t({ type: "disconnect" });
    if (this.startWriteStreamCbs = {}, e !== Ce.AUTHENTICATED)
      for (const { onDisconnect: t } of Object.values(this.writeStreams))
        t();
    else {
      for (const { onConnectionReconnect: t } of Object.values(this.writeStreams))
        t();
      for (const t of Object.values(this.readStreamIterators))
        t.push({ type: "reconnect" }), t.close();
      this.readStreamIterators = {};
    }
  }
  onRecieveError(e) {
    const t = e["original-event"];
    switch (t.op) {
      case "append-stream": {
        const r = t["stream-id"], s = this.writeStreams[r];
        s == null || s.onAppendFailed();
        break;
      }
      case "start-stream": {
        const r = e["client-event-id"], s = this.startWriteStreamCbs[r];
        s && (s({
          type: "error",
          error: new le(e.message || "Unknown error", e.hint)
        }), delete this.startWriteStreamCbs[r]);
        break;
      }
      case "subscribe-stream": {
        const r = e["client-event-id"], s = this.readStreamIterators[r];
        s && (s.push({
          type: "error",
          error: new le(e.message || "Unknown error", e.hint)
        }), s.close(), delete this.readStreamIterators[r]);
        break;
      }
    }
  }
  hasActiveStreams() {
    return this.activeStreams.size > 0;
  }
}
const Ce = {
  CONNECTING: "connecting",
  OPENED: "opened",
  AUTHENTICATED: "authenticated",
  CLOSED: "closed",
  ERRORED: "errored"
}, Kc = 3e4, Wc = 3e4, qc = 200, zc = 1e3 * 60, Vc = {
  apiURI: "https://api.instantdb.com",
  websocketURI: "wss://api.instantdb.com/runtime/session"
}, pn = "_instant_oauth_redirect", yn = "_instant_extra_fields_id", jt = "oauthExtraFields", Ge = "currentUser";
function Qc({ transportType: n, appId: e, apiURI: t, wsURI: r, EventSourceImpl: s }) {
  if (!s)
    return new Gr(`${r}?app_id=${e}`);
  switch (n) {
    case "ws":
      return new Gr(`${r}?app_id=${e}`);
    case "sse":
      return new Ic(s, `${t}/runtime/sse?app_id=${e}`);
    default:
      throw new Error("Unknown transport type " + n);
  }
}
function Bc() {
  return typeof window < "u" || typeof chrome < "u";
}
const Yr = {
  "set-presence": !0,
  "set-presence-ok": !0,
  "refresh-presence": !0,
  "patch-presence": !0
};
function Gc(n, e) {
  var r;
  const t = typeof n == "string" ? JSON.parse(n) : n;
  if ((r = t == null ? void 0 : t.result) != null && r.store) {
    const s = ks(t.result.attrsStore, t.result.store);
    if (s) {
      const i = t.result.store;
      t.result.store = Os(s, {
        ...i,
        useDateObjects: e
      }), t.result.attrsStore = s;
    }
  }
  return t;
}
function Hc(n, e) {
  const { result: t, ...r } = e, s = (
    /** @type {import('./reactorTypes.ts').QuerySubInStorage} */
    r
  );
  if (t) {
    const i = {
      ...t,
      store: Es(t.store),
      attrsStore: t.attrsStore.toJSON()
    };
    s.result = i;
  }
  return s;
}
function Jc(n, e) {
  switch (n) {
    case "pendingMutations":
      return new Map(typeof e == "string" ? JSON.parse(e) : e);
    default:
      return e;
  }
}
function Yc(n, e) {
  switch (n) {
    case "pendingMutations":
      return [...e.entries()];
    default:
      return e;
  }
}
function Zc(n, e, t) {
  const r = e == null ? void 0 : e.result, s = t == null ? void 0 : t.result;
  return r && !s && t && (t.result = r), t || e;
}
function bn(n) {
  return [...n].sort((e, t) => {
    const [r, s] = e, [i, o] = t, a = s.order || 0, c = o.order || 0;
    return a == c ? r < i ? -1 : r > i ? 1 : 0 : a - c;
  });
}
class Xc {
  constructor(e, t = qs, r = zs, s, i) {
    /** @type {s.AttrsStore | undefined} */
    p(this, "attrs");
    p(this, "_isOnline", !0);
    p(this, "_isShutdown", !1);
    p(this, "status", Ce.CONNECTING);
    /** @type {PersistedObject<string, QuerySub, QuerySubInStorage>} */
    p(this, "querySubs");
    /** @type {PersistedObject} */
    p(this, "kv");
    /** @type {SyncTable} */
    p(this, "_syncTable");
    /** @type {InstantStream} */
    p(this, "_instantStream");
    /** @type {Record<string, Array<{ q: any, cb: (data: any) => any }>>} */
    p(this, "queryCbs", {});
    /** @type {Record<string, Array<{ q: any, eventId: string, dfd: Deferred }>>} */
    p(this, "queryOnceDfds", {});
    p(this, "authCbs", []);
    p(this, "attrsCbs", []);
    p(this, "mutationErrorCbs", []);
    p(this, "connectionStatusCbs", []);
    p(this, "config");
    p(this, "mutationDeferredStore", /* @__PURE__ */ new Map());
    p(this, "_reconnectTimeoutId", null);
    p(this, "_reconnectTimeoutMs", 0);
    /** @type {Connection} */
    p(this, "_transport");
    /** @type {TransportType} */
    p(this, "_transportType", "ws");
    /** @type {EventSourceConstructor} */
    p(this, "_EventSource");
    /** @type {boolean | null} */
    p(this, "_wsOk", null);
    p(this, "_localIdPromises", {});
    p(this, "_errorMessage", null);
    /** @type {Promise<null | {error: {message: string}}> | null}**/
    p(this, "_oauthCallbackResponse", null);
    /** @type {null | import('./utils/linkIndex.ts').LinkIndex}} */
    p(this, "_linkIndex", null);
    /** @type BroadcastChannel | undefined */
    p(this, "_broadcastChannel");
    /** @type {Record<string, {roomType: string; isConnected: boolean; error: any}>} */
    p(this, "_rooms", {});
    /** @type {Record<string, boolean>} */
    p(this, "_roomsPendingLeave", {});
    p(this, "_presence", {});
    p(this, "_broadcastQueue", []);
    p(this, "_broadcastSubs", {});
    /** @type {{isLoading: boolean; error: any | undefined, user: any | undefined}} */
    p(this, "_currentUserCached", { isLoading: !0, error: void 0, user: void 0 });
    p(this, "_beforeUnloadCbs", []);
    p(this, "_dataForQueryCache", {});
    /** @type {Logger} */
    p(this, "_log");
    p(this, "_pendingTxCleanupTimeout");
    p(this, "_pendingMutationCleanupThreshold");
    p(this, "_inFlightMutationEventIds", /* @__PURE__ */ new Set());
    /** @type FrameworkClient | null */
    p(this, "_frameworkClient", null);
    p(this, "_onMergeKv", (e, t, r) => {
      switch (e) {
        case "pendingMutations": {
          const s = (t == null ? void 0 : t.entries()) ?? [], i = (r == null ? void 0 : r.entries()) ?? [], o = new Map([...s, ...i]);
          return (t ? this._rewriteMutationsSorted(this.attrs, t) : []).forEach(([c, u]) => {
            var l;
            !((l = r == null ? void 0 : r.pendingMutations) != null && l.has(c)) && !u["tx-id"] && this._sendMutation(c, u);
          }), o;
        }
        default:
          return r || t;
      }
    });
    // ---------------------------
    // Queries
    p(this, "getPreviousResult", (e) => {
      var r;
      const t = ee(e);
      return (r = this.dataForQuery(t)) == null ? void 0 : r.data;
    });
    /** Re-run instaql and call all callbacks with new data */
    p(this, "notifyOne", (e) => {
      var i;
      const t = this.queryCbs[e] ?? [], r = (i = this._dataForQueryCache[e]) == null ? void 0 : i.data, s = this.dataForQuery(e);
      s != null && s.data && (this._dataForQueryCache[e] = s, !Lt(s.data, r) && t.forEach((o) => o.cb(s.data)));
    });
    p(this, "notifyOneQueryOnce", (e) => {
      var s;
      const t = this.queryOnceDfds[e] ?? [], r = (s = this.dataForQuery(e)) == null ? void 0 : s.data;
      t.forEach((i) => {
        this._completeQueryOnce(i.q, e, i.dfd), i.dfd.resolve(r);
      });
    });
    p(this, "notifyQueryError", (e, t) => {
      (this.queryCbs[e] || []).forEach((s) => s.cb({ error: t }));
    });
    /** Applies transactions locally and sends transact message to server */
    p(this, "pushTx", (e) => {
      this.config.disableValidation || Cc(e, this.config.schema);
      try {
        const t = Za({
          attrsStore: this.optimisticAttrs(),
          schema: this.config.schema,
          stores: Object.values(this.querySubs.currentValue).map((r) => {
            var s;
            return (s = r == null ? void 0 : r.result) == null ? void 0 : s.store;
          }),
          useDateObjects: this.config.useDateObjects
        }, e);
        return this.pushOps(t);
      } catch (t) {
        return this.pushOps([], t);
      }
    });
    /**
     * @param {*} txSteps
     * @param {*} [error]
     * @returns
     */
    p(this, "pushOps", (e, t) => {
      const r = H(), s = [...this._pendingMutations().values()], i = Math.max(0, ...s.map((c) => c.order || 0)) + 1, o = {
        op: "transact",
        "tx-steps": e,
        created: Date.now(),
        error: t,
        order: i
      };
      this._updatePendingMutations((c) => {
        c.set(r, o);
      });
      const a = new Kr();
      return this.mutationDeferredStore.set(r, a), this._sendMutation(r, o), this.notifyAll(), a.promise;
    });
    p(this, "_transportOnOpen", (e) => {
      const t = e.target;
      if (this._transport !== t) {
        this._log.info("[socket][open]", t.id, "skip; this is no longer the current transport");
        return;
      }
      this._log.info("[socket][open]", this._transport.id), this._setStatus(Ce.OPENED), this.getCurrentUser().then((r) => {
        var s;
        this._trySend(H(), {
          op: "init",
          "app-id": this.config.appId,
          "refresh-token": (s = r.user) == null ? void 0 : s.refresh_token,
          versions: this.versions,
          // If an admin token is provided for an app, we will
          // skip all permission checks. This is an advanced feature,
          // to let users write internal tools
          // This option is not exposed in `Config`, as it's
          // not ready for prime time
          "__admin-token": this.config.__adminToken
        });
      }).catch((r) => {
        this._log.error("[socket][error]", t.id, r);
      });
    });
    p(this, "_transportOnMessage", (e) => {
      const t = e.target, r = e.message;
      if (this._transport !== t) {
        this._log.info("[socket][message]", t.id, r, "skip; this is no longer the current transport");
        return;
      }
      if (!this._wsOk && t.type === "ws" && (this._wsOk = !0), this._transportType = "ws", Array.isArray(e.message))
        for (const s of e.message)
          this._handleReceive(t.id, s);
      else
        this._handleReceive(t.id, e.message);
    });
    p(this, "_transportOnError", (e) => {
      const t = e.target;
      if (this._transport !== t) {
        this._log.info("[socket][error]", t.id, "skip; this is no longer the current transport");
        return;
      }
      this._log.error("[socket][error]", t.id, e);
    });
    p(this, "_scheduleReconnect", () => {
      !this._wsOk && this._transportType !== "sse" && (this._transportType = "sse", this._reconnectTimeoutMs = 0), setTimeout(() => {
        if (this._reconnectTimeoutMs = Math.min(this._reconnectTimeoutMs + 1e3, 1e4), !this._isOnline) {
          this._log.info("[socket][close]", this._transport.id, "we are offline, no need to start socket");
          return;
        }
        this._startSocket();
      }, this._reconnectTimeoutMs);
    });
    p(this, "_transportOnClose", (e) => {
      const t = e.target;
      if (this._transport !== t) {
        this._log.info("[socket][close]", t.id, "skip; this is no longer the current transport");
        return;
      }
      this._setStatus(Ce.CLOSED);
      for (const r of Object.values(this._rooms))
        r.isConnected = !1;
      if (this._isShutdown) {
        this._log.info("[socket][close]", t.id, "Reactor has been shut down and will not reconnect");
        return;
      }
      this._log.info("[socket][close]", t.id, "schedule reconnect, ms =", this._reconnectTimeoutMs), this._scheduleReconnect();
    });
    if (this._EventSource = i, this.config = { ...Vc, ...e }, this.queryCacheLimit = this.config.queryCacheLimit ?? 10, this._pendingTxCleanupTimeout = this.config.pendingTxCleanupTimeout ?? Wc, this._pendingMutationCleanupThreshold = this.config.pendingMutationCleanupThreshold ?? qc, this._log = mc(e.verbose || rr || Vs, () => this._reactorStats()), this.versions = { ...s || {}, "@instantdb/core": sr }, this.config.schema && (this._linkIndex = qr(this.config.schema)), !!Bc()) {
      if (!e.appId)
        throw new Error("Instant must be initialized with an appId.");
      if (!st(e.appId))
        throw new Error(`Instant must be initialized with a valid appId. \`${e.appId}\` is not a valid uuid.`);
      typeof BroadcastChannel == "function" && (this._broadcastChannel = new BroadcastChannel("@instantdb"), this._broadcastChannel.addEventListener("message", async (o) => {
        var a;
        try {
          if (((a = o.data) == null ? void 0 : a.type) === "auth") {
            const c = await this.getCurrentUser({
              forceReadFromStorage: !0
            });
            await this.updateUser(c.user).catch((u) => {
              this._log.error("[error] update user", u);
            });
          }
        } catch (c) {
          this._log.error("[error] handle broadcast channel", c);
        }
      })), this._initStorage(t), this._syncTable = new Dc(this._trySendAuthed.bind(this), new t(this.config.appId, "syncSubs"), {
        useDateObjects: this.config.useDateObjects
      }, this._log, (o) => pt(this.ensureAttrs(), o, this.config.enableCardinalityInference, this.config.useDateObjects), () => this.ensureAttrs()), this._instantStream = new Nc({
        WStream: this.config.WritableStream || WritableStream,
        RStream: this.config.ReadableStream || ReadableStream,
        trySend: this._trySendAuthed.bind(this),
        log: this._log
      }), this._oauthCallbackResponse = this._oauthLoginInit(), this.getCurrentUser().then((o) => {
        this.syncUserToEndpoint(o.user);
      }), setInterval(async () => {
        const o = await this.getCurrentUser();
        this.syncUserToEndpoint(o.user);
      }, zc), r.getIsOnline().then((o) => {
        this._isOnline = o, this._startSocket(), r.listen((a) => {
          a !== this._isOnline && (this._log.info("[network] online =", a), this._isOnline = a, this._isOnline ? this._startSocket() : (this._log.info("Changing status from", this.status, "to", Ce.CLOSED), this._setStatus(Ce.CLOSED)));
        });
      }), typeof addEventListener < "u" && (this._beforeUnload = this._beforeUnload.bind(this), addEventListener("beforeunload", this._beforeUnload));
    }
  }
  getFrameworkClient() {
    return this._frameworkClient;
  }
  /** @param {FrameworkClient} client  */
  setFrameworkClient(e) {
    this._frameworkClient = e;
  }
  ensureAttrs() {
    if (!this.attrs)
      throw new Error("attrs have not loaded.");
    return this.attrs;
  }
  updateSchema(e) {
    this.config = {
      ...this.config,
      schema: e,
      cardinalityInference: !!e
    }, this._linkIndex = e ? qr(this.config.schema) : null;
  }
  _reactorStats() {
    return {
      inFlightMutationCount: this._inFlightMutationEventIds.size,
      storedMutationCount: this._pendingMutations().size,
      transportType: this._transportType
    };
  }
  _onQuerySubLoaded(e) {
    this.kv.waitForKeyToLoad("pendingMutations").then(() => this.notifyOne(e));
  }
  _initStorage(e) {
    this.querySubs = new jn({
      persister: new e(this.config.appId, "querySubs"),
      merge: Zc,
      serialize: Hc,
      parse: (t, r) => Gc(r, this.config.useDateObjects),
      // objectSize
      objectSize: (t) => {
        var r, s, i;
        return ((i = (s = (r = t == null ? void 0 : t.result) == null ? void 0 : r.store) == null ? void 0 : s.triples) == null ? void 0 : i.length) ?? 0;
      },
      logger: this._log,
      preloadEntryCount: 10,
      gc: {
        maxAgeMs: 1e3 * 60 * 60 * 24 * 7 * 52,
        // 1 year
        maxEntries: 1e3,
        // Size of each query is the number of triples
        maxSize: 1e6
        // 1 million triples
      }
    }), this.querySubs.onKeyLoaded = (t) => this._onQuerySubLoaded(t), this.kv = new jn({
      persister: new e(this.config.appId, "kv"),
      merge: this._onMergeKv,
      serialize: Yc,
      parse: Jc,
      objectSize: () => 0,
      logger: this._log,
      saveThrottleMs: 100,
      idleCallbackMaxWaitMs: 100,
      // Don't GC the kv store
      gc: null
    }), this.kv.onKeyLoaded = (t) => {
      t === "pendingMutations" && this.notifyAll();
    }, this.kv.waitForKeyToLoad("pendingMutations"), this.kv.waitForKeyToLoad(Ge), this._beforeUnloadCbs.push(() => {
      this.kv.flush(), this.querySubs.flush();
    });
  }
  _beforeUnload() {
    for (const e of this._beforeUnloadCbs)
      e();
    this._syncTable.beforeUnload();
  }
  /**
   * @param {'enqueued' | 'pending' | 'synced' | 'timeout' |  'error' } status
   * @param {string} eventId
   * @param {{message?: string, type?: string, status?: number, hint?: unknown}} [errorMsg]
   */
  _finishTransaction(e, t, r) {
    const s = this.mutationDeferredStore.get(t);
    this.mutationDeferredStore.delete(t);
    const i = e !== "error" && e !== "timeout";
    if (!s && !i && console.error("Mutation failed", { status: e, eventId: t, ...r }), !!s)
      if (i)
        s.resolve({ status: e, eventId: t });
      else if (r != null && r.type) {
        const { status: o, ...a } = r;
        s.reject(new Zt({
          // @ts-expect-error body.type is not constant typed
          body: a,
          status: o ?? 0
        }));
      } else
        s.reject(new le((r == null ? void 0 : r.message) || "Unknown error", r == null ? void 0 : r.hint));
  }
  _setStatus(e, t) {
    this.status = e, this._errorMessage = t, this.notifyConnectionStatusSubs(e), this._instantStream.onConnectionStatusChange(e);
  }
  _flushEnqueuedRoomData(e) {
    var s, i;
    const t = (i = (s = this._presence[e]) == null ? void 0 : s.result) == null ? void 0 : i.user, r = this._broadcastQueue[e];
    if (this._broadcastQueue[e] = [], t && this._trySetPresence(e, t), r)
      for (const o of r) {
        const { topic: a, roomType: c, data: u } = o;
        this._tryBroadcast(e, c, a, u);
      }
  }
  /**
   * Does the same thing as add-query-ok
   * but called as a result of receiving query info from ssr
   * @param {any} q
   * @param {{ triples: any; pageInfo: any; }} result
   * @param {boolean} enableCardinalityInference
   */
  _addQueryData(e, t, r) {
    if (!this.attrs)
      throw new Error("Attrs in reactor have not been set");
    const s = ee(e), i = this.ensureAttrs(), o = pt(this.attrs, t.triples, r, this.config.useDateObjects);
    this.querySubs.updateInPlace((a) => {
      a[s] = {
        result: {
          store: o,
          attrsStore: i,
          pageInfo: t.pageInfo,
          processedTxId: void 0,
          isExternal: !0
        },
        q: e
      };
    }), this._cleanupPendingMutationsQueries(), this.notifyOne(s), this.notifyOneQueryOnce(s), this._cleanupPendingMutationsTimeout();
  }
  _handleReceive(e, t) {
    var s, i, o, a, c, u, l;
    const r = !!this.config.schema && ("cardinalityInference" in this.config ? !!this.config.cardinalityInference : !0);
    switch (Yr[t.op] || this._log.info("[receive]", e, t.op, t), t.op) {
      case "init-ok": {
        this._setStatus(Ce.AUTHENTICATED), this._reconnectTimeoutMs = 0, this._setAttrs(t.attrs), this._flushPendingMessages(), this._sessionId = t["session-id"];
        for (const y of Object.keys(this._rooms)) {
          const b = (i = (s = this._presence[y]) == null ? void 0 : s.result) == null ? void 0 : i.user, g = (o = this._rooms[y]) == null ? void 0 : o.roomType;
          this._tryJoinRoom(g, y, b);
        }
        break;
      }
      case "add-query-exists": {
        this.notifyOneQueryOnce(ee(t.q));
        break;
      }
      case "add-query-ok": {
        const { q: y, result: b } = t, g = ee(y);
        if (!this._hasQueryListeners() && !this.querySubs.currentValue[g])
          break;
        const w = (c = (a = b == null ? void 0 : b[0]) == null ? void 0 : a.data) == null ? void 0 : c["page-info"], S = (l = (u = b == null ? void 0 : b[0]) == null ? void 0 : u.data) == null ? void 0 : l.aggregate, _ = Wr(b), m = this.ensureAttrs(), A = pt(m, _, r, this.config.useDateObjects);
        this.querySubs.updateInPlace((v) => {
          if (!v[g]) {
            this._log.info("Missing value in querySubs", { hash: g, q: y });
            return;
          }
          v[g].result = {
            store: A,
            attrsStore: m,
            pageInfo: w,
            aggregate: S,
            processedTxId: t["processed-tx-id"]
          };
        }), this._cleanupPendingMutationsQueries(), this.notifyOne(g), this.notifyOneQueryOnce(g), this._cleanupPendingMutationsTimeout();
        break;
      }
      case "start-sync-ok": {
        this._syncTable.onStartSyncOk(t);
        break;
      }
      case "sync-load-batch": {
        this._syncTable.onSyncLoadBatch(t);
        break;
      }
      case "sync-init-finish": {
        this._syncTable.onSyncInitFinish(t);
        break;
      }
      case "sync-update-triples": {
        this._syncTable.onSyncUpdateTriples(t);
        break;
      }
      case "start-stream-ok": {
        this._instantStream.onStartStreamOk(t);
        break;
      }
      case "stream-flushed": {
        this._instantStream.onStreamFlushed(t);
        break;
      }
      case "append-failed": {
        this._instantStream.onAppendFailed(t);
        break;
      }
      case "stream-append": {
        this._instantStream.onStreamAppend(t);
        break;
      }
      case "refresh-ok": {
        const { computations: y, attrs: b } = t, g = t["processed-tx-id"];
        b && this._setAttrs(b), this._cleanupPendingMutationsTimeout();
        const w = this._rewriteMutations(this.ensureAttrs(), this._pendingMutations(), g);
        w !== this._pendingMutations() && this.kv.updateInPlace((m) => {
          m.pendingMutations = w;
        });
        const S = bn(w.entries()), _ = y.map((m) => {
          var B, k, M, x;
          const A = m["instaql-query"], v = m["instaql-result"], I = ee(A), j = Wr(v), N = this.ensureAttrs(), Q = pt(N, j, r, this.config.useDateObjects), { store: ge, attrsStore: Ae } = this._applyOptimisticUpdates(Q, N, S, g), re = (k = (B = v == null ? void 0 : v[0]) == null ? void 0 : B.data) == null ? void 0 : k["page-info"], se = (x = (M = v == null ? void 0 : v[0]) == null ? void 0 : M.data) == null ? void 0 : x.aggregate;
          return {
            q: A,
            hash: I,
            store: ge,
            attrsStore: Ae,
            pageInfo: re,
            aggregate: se
          };
        });
        _.forEach(({ hash: m, q: A, store: v, attrsStore: I, pageInfo: j, aggregate: N }) => {
          this.querySubs.updateInPlace((Q) => {
            if (!Q[m]) {
              this._log.error("Missing value in querySubs", { hash: m, q: A });
              return;
            }
            Q[m].result = {
              store: v,
              attrsStore: I,
              pageInfo: j,
              aggregate: N,
              processedTxId: g
            };
          });
        }), this._cleanupPendingMutationsQueries(), _.forEach(({ hash: m }) => {
          this.notifyOne(m);
        });
        break;
      }
      case "transact-ok": {
        const { "client-event-id": y, "tx-id": b } = t;
        this._inFlightMutationEventIds.delete(y);
        const w = this._rewriteMutations(this.ensureAttrs(), this._pendingMutations()).get(y);
        if (!w)
          break;
        this._updatePendingMutations((_) => {
          _.set(y, {
            ..._.get(y),
            "tx-id": b,
            confirmed: Date.now()
          });
        });
        const S = [];
        for (const _ of w["tx-steps"])
          if (_[0] === "add-attr") {
            const m = _[1];
            S.push(m);
          }
        if (S.length) {
          const _ = Object.values(this.ensureAttrs().attrs);
          this._setAttrs([..._, ...S]);
        }
        this._finishTransaction("synced", y), this._cleanupPendingMutationsTimeout();
        break;
      }
      case "patch-presence": {
        const y = t["room-id"];
        this._trySetRoomConnected(y, !0), this._patchPresencePeers(y, t.edits), this._notifyPresenceSubs(y);
        break;
      }
      case "refresh-presence": {
        const y = t["room-id"];
        this._trySetRoomConnected(y, !0), this._setPresencePeers(y, t.data), this._notifyPresenceSubs(y);
        break;
      }
      case "server-broadcast": {
        const y = t["room-id"], b = t.topic;
        this._trySetRoomConnected(y, !0), this._notifyBroadcastSubs(y, b, t);
        break;
      }
      case "join-room-ok": {
        const y = t["room-id"];
        if (!this._rooms[y]) {
          this._roomsPendingLeave[y] && (this._tryLeaveRoom(y), delete this._roomsPendingLeave[y]);
          break;
        }
        this._trySetRoomConnected(y, !0), this._flushEnqueuedRoomData(y);
        break;
      }
      case "leave-room-ok": {
        const y = t["room-id"];
        this._trySetRoomConnected(y, !1);
        break;
      }
      case "join-room-error":
        const f = t["room-id"], d = this._rooms[f];
        d && (d.error = t.error), this._notifyPresenceSubs(f);
        break;
      case "error":
        this._handleReceiveError(t);
        break;
      default:
        this._log.info("Unknown op", t.op, t);
        break;
    }
  }
  createWriteStream(e) {
    return this._instantStream.createWriteStream(e);
  }
  createReadStream(e) {
    return this._instantStream.createReadStream(e);
  }
  _pendingMutations() {
    return this.kv.currentValue.pendingMutations ?? /* @__PURE__ */ new Map();
  }
  _updatePendingMutations(e) {
    this.kv.updateInPlace((t) => {
      const r = t.pendingMutations ?? /* @__PURE__ */ new Map();
      t.pendingMutations = r, e(r);
    });
  }
  /**
   * @param {'timeout' | 'error'} status
   * @param {string} eventId
   * @param {{message?: string, type?: string, status?: number, hint?: unknown}} errorMsg
   */
  _handleMutationError(e, t, r) {
    const s = this._pendingMutations().get(t);
    if (s && (e !== "timeout" || !s["tx-id"])) {
      this._updatePendingMutations((o) => (o.delete(t), o)), this._inFlightMutationEventIds.delete(t);
      const i = {
        message: r.message,
        hint: r.hint
      };
      this.notifyAll(), this.notifyAttrsSubs(), this.notifyMutationErrorSubs(i), this._finishTransaction(e, t, r);
    }
  }
  _handleReceiveError(e) {
    var a, c, u, l, f, d;
    console.log("error", e);
    const t = e["client-event-id"];
    this._inFlightMutationEventIds.delete(t);
    const r = this._pendingMutations().get(t), s = {
      message: e.message || "Uh-oh, something went wrong. Ping Joe & Stopa."
    };
    if (e.hint && (s.hint = e.hint), r) {
      this._handleMutationError("error", t, e);
      return;
    }
    if ((a = e["original-event"]) != null && a.hasOwnProperty("q") && ((c = e["original-event"]) == null ? void 0 : c.op) === "add-query") {
      const y = (u = e["original-event"]) == null ? void 0 : u.q, b = ee(y);
      this.notifyQueryError(ee(y), s), this.notifyQueryOnceError(y, b, t, s);
      return;
    }
    if (((l = e["original-event"]) == null ? void 0 : l.op) === "init") {
      if (e.type === "record-not-found" && ((f = e.hint) == null ? void 0 : f["record-type"]) === "app-user") {
        this.changeCurrentUser(null);
        return;
      }
      this._setStatus(Ce.ERRORED, s), this.notifyAll();
      return;
    }
    switch ((d = e["original-event"]) == null ? void 0 : d.op) {
      case "resync-table": {
        this._syncTable.onResyncError(e);
        return;
      }
      case "start-sync": {
        this._syncTable.onStartSyncError(e);
        return;
      }
      case "start-stream":
      case "append-stream":
      case "subscribe-stream":
      case "unsubscribe-stream": {
        this._instantStream.onRecieveError(e);
        return;
      }
    }
    const o = { ...e };
    delete o.message, delete o.hint, console.error(e.message, o), e.hint && console.error(`This error comes with some debugging information. Here it is: 
`, e.hint);
  }
  notifyQueryOnceError(e, t, r, s) {
    var o;
    const i = (o = this.queryOnceDfds[t]) == null ? void 0 : o.find((a) => a.eventId === r);
    i && (i.dfd.reject(s), this._completeQueryOnce(e, t, i.dfd));
  }
  _setAttrs(e) {
    this.attrs = new Ke(e.reduce((t, r) => (t[r.id] = r, t), {}), this._linkIndex), this.notifyAttrsSubs();
  }
  _startQuerySub(e, t) {
    const r = H();
    return this.querySubs.updateInPlace((s) => {
      s[t] = s[t] || { q: e, result: null, eventId: r }, s[t].lastAccessed = Date.now();
    }), this._trySendAuthed(r, { op: "add-query", q: e }), r;
  }
  subscribeTable(e, t) {
    return this._syncTable.subscribe(e, t);
  }
  /**
   *  When a user subscribes to a query the following side effects occur:
   *
   *  - We update querySubs to include the new query
   *  - We update queryCbs to include the new cb
   *  - If we already have a result for the query we call cb immediately
   *  - We send the server an `add-query` message
   *
   *  Returns an unsubscribe function
   */
  subscribeQuery(e, t, r) {
    this.config.disableValidation || Vr(e, this.config.schema), r && "ruleParams" in r && (e = { $$ruleParams: r.ruleParams, ...e });
    const s = ee(e), i = this.getPreviousResult(e);
    return i && t(i), this.queryCbs[s] = this.queryCbs[s] ?? [], this.queryCbs[s].push({ q: e, cb: t }), this._startQuerySub(e, s), () => {
      this._unsubQuery(e, s, t);
    };
  }
  queryOnce(e, t) {
    this.config.disableValidation || Vr(e, this.config.schema), t && "ruleParams" in t && (e = { $$ruleParams: t.ruleParams, ...e });
    const r = new Kr();
    if (!this._isOnline)
      return r.reject(new Error("We can't run `queryOnce`, because the device is offline.")), r.promise;
    if (!this.querySubs)
      return r.reject(new Error("We can't run `queryOnce` on the backend. Use adminAPI.query instead: https://www.instantdb.com/docs/backend#query")), r.promise;
    const s = ee(e), i = this._startQuerySub(e, s);
    return this.queryOnceDfds[s] = this.queryOnceDfds[s] ?? [], this.queryOnceDfds[s].push({ q: e, dfd: r, eventId: i }), setTimeout(() => r.reject(new Error("Query timed out")), Kc), r.promise;
  }
  _completeQueryOnce(e, t, r) {
    this.queryOnceDfds[t] && (this.queryOnceDfds[t] = this.queryOnceDfds[t].filter((s) => s.dfd !== r), this._cleanupQuery(e, t));
  }
  _unsubQuery(e, t, r) {
    this.queryCbs[t] && (this.queryCbs[t] = this.queryCbs[t].filter((s) => s.cb !== r), this._cleanupQuery(e, t));
  }
  _hasQueryListeners(e) {
    var t, r;
    return !!((t = this.queryCbs[e]) != null && t.length || (r = this.queryOnceDfds[e]) != null && r.length);
  }
  _cleanupQuery(e, t) {
    this._hasQueryListeners(t) || (delete this.queryCbs[t], delete this.queryOnceDfds[t], delete this._dataForQueryCache[t], this.querySubs.unloadKey(t), this._trySendAuthed(H(), { op: "remove-query", q: e }));
  }
  // When we `pushTx`, it's possible that we don't yet have `this.attrs`
  // This means that `tx-steps` in `pendingMutations` will include `add-attr`
  // commands for attrs that already exist.
  //
  // This will also affect `add-triple` and `retract-triple` which
  // reference attr-ids that do not match the server.
  //
  // We fix this by rewriting `tx-steps` in each `pendingMutation`.
  // We remove `add-attr` commands for attrs that already exist.
  // We update `add-triple` and `retract-triple` commands to use the
  // server attr-ids.
  /**
   *
   * @param {s.AttrsStore} attrs
   * @param {any} muts
   * @param {number} [processedTxId]
   */
  _rewriteMutations(e, t, r) {
    if (!e)
      return t;
    if (!t)
      return /* @__PURE__ */ new Map();
    const s = (l) => {
      const [f, d, y] = l["forward-identity"];
      return te(e, d, y);
    }, i = (l) => {
      const [f, d, y] = l["forward-identity"];
      return Ve(e, d, y);
    }, o = { attrIdMap: {}, refSwapAttrIds: /* @__PURE__ */ new Set() };
    let a = !1;
    const c = (l, f) => {
      const d = [];
      for (const y of l) {
        const [b] = y;
        if (b === "add-attr") {
          const [w, S] = y, _ = s(S);
          if (_ && S.id !== _.id) {
            o.attrIdMap[S.id] = _.id, a = !0;
            continue;
          }
          if (S["value-type"] === "ref") {
            const m = i(S);
            if (m) {
              o.attrIdMap[S.id] = m.id, o.refSwapAttrIds.add(S.id), a = !0;
              continue;
            }
          }
        }
        if (r && f && r >= f && b === "add-attr" || b === "update-attr" || b === "delete-attr") {
          a = !0;
          continue;
        }
        const g = a ? Aa(o, y) : y;
        d.push(g);
      }
      return a ? d : l;
    }, u = /* @__PURE__ */ new Map();
    for (const [l, f] of t.entries())
      u.set(l, {
        ...f,
        "tx-steps": c(f["tx-steps"], f["tx-id"])
      });
    return a ? u : t;
  }
  _rewriteMutationsSorted(e, t) {
    return bn(this._rewriteMutations(e, t).entries());
  }
  // ---------------------------
  // Transact
  /**
   * @returns {s.AttrsStore}
   */
  optimisticAttrs() {
    var i, o;
    const e = [...this._pendingMutations().values()].flatMap((a) => a["tx-steps"]), t = new Set(e.filter(([a, c]) => a === "delete-attr").map(([a, c]) => c)), r = [];
    for (const [a, c] of e)
      if (a === "add-attr")
        r.push(c);
      else if (a === "update-attr" && c.id && ((i = this.attrs) != null && i.getAttr(c.id))) {
        const u = { ...this.attrs.getAttr(c.id), ...c };
        r.push(u);
      }
    if (!t.size && !r.length)
      return this.attrs || new Ke({}, this._linkIndex);
    const s = { ...((o = this.attrs) == null ? void 0 : o.attrs) || {} };
    for (const a of r)
      s[a.id] = a;
    for (const a of t)
      delete s[a];
    return new Ke(s, this._linkIndex);
  }
  /** Runs instaql on a query and a store */
  dataForQuery(e, t = !0) {
    const r = this._errorMessage;
    if (r)
      return { error: r };
    if (!this.querySubs || !this.kv.currentValue.pendingMutations)
      return;
    const s = this.querySubs.version(), i = this.querySubs.currentValue, o = this.kv.version(), a = this._pendingMutations(), { q: c, result: u } = i[e] || {};
    if (!u)
      return;
    const l = this._dataForQueryCache[e];
    if (l && s === l.querySubVersion && o === l.pendingMutationsVersion)
      return l;
    let f = u.store, d = u.attrsStore;
    const { pageInfo: y, aggregate: b, processedTxId: g } = u, w = this._rewriteMutationsSorted(d, a);
    if (t) {
      const _ = this._applyOptimisticUpdates(f, d, w, g);
      f = _.store, d = _.attrsStore;
    }
    return { data: Ns({ store: f, attrsStore: d, pageInfo: y, aggregate: b }, c), querySubVersion: s, pendingMutationsVersion: o };
  }
  _applyOptimisticUpdates(e, t, r, s) {
    for (const [i, o] of r)
      if (!o["tx-id"] || s && o["tx-id"] > s) {
        const a = ko(e, t, o["tx-steps"]);
        e = a.store, t = a.attrsStore;
      }
    return { store: e, attrsStore: t };
  }
  /** Re-compute all subscriptions */
  notifyAll() {
    Object.keys(this.queryCbs).forEach((e) => {
      this.querySubs.waitForKeyToLoad(e).then(() => this.notifyOne(e)).catch(() => this.notifyOne(e));
    });
  }
  loadedNotifyAll() {
    this.kv.waitForKeyToLoad("pendingMutations").then(() => this.notifyAll()).catch(() => this.notifyAll());
  }
  shutdown() {
    var e;
    this._log.info("[shutdown]", this.config.appId), this._isShutdown = !0, (e = this._transport) == null || e.close();
  }
  /**
   * Sends mutation to server and schedules a timeout to cancel it if
   * we don't hear back in time.
   * Note: If we're offline we don't schedule a timeout, we'll schedule it
   * later once we're back online and send the mutation again
   *
   */
  _sendMutation(e, t) {
    if (t.error) {
      this._handleMutationError("error", e, {
        message: t.error.message
      });
      return;
    }
    if (this.status !== Ce.AUTHENTICATED) {
      this._finishTransaction("enqueued", e);
      return;
    }
    const r = Math.max(6e3, Math.min(
      this._inFlightMutationEventIds.size + 1,
      // Defensive code in case we don't clean up in flight mutation event ids
      this._pendingMutations().size + 1
    ) * 6e3);
    this._isOnline ? (this._trySend(e, t), setTimeout(() => {
      this._isOnline && this._handleMutationError("timeout", e, {
        message: "transaction timed out"
      });
    }, r)) : this._finishTransaction("enqueued", e);
  }
  // ---------------------------
  // Websocket
  /** Send messages we accumulated while we were connecting */
  _flushPendingMessages() {
    Object.keys(this.queryCbs).map((i) => this.querySubs.currentValue[i]).filter((i) => i).forEach(({ eventId: i, q: o }) => {
      this._trySendAuthed(i, { op: "add-query", q: o });
    }), Object.values(this.queryOnceDfds).flat().forEach(({ eventId: i, q: o }) => {
      this._trySendAuthed(i, { op: "add-query", q: o });
    });
    const r = this._rewriteMutations(this.ensureAttrs(), this._pendingMutations());
    r !== this._pendingMutations() && this.kv.updateInPlace((i) => {
      i.pendingMutations = r;
    }), bn(r.entries()).forEach(([i, o]) => {
      o["tx-id"] || this._sendMutation(i, o);
    }), this._syncTable.flushPending();
  }
  /**
   * Clean up pendingMutations that all queries have seen
   */
  _cleanupPendingMutationsQueries() {
    let e = Number.MAX_SAFE_INTEGER;
    for (const { result: t } of Object.values(this.querySubs.currentValue))
      t != null && t.processedTxId && (e = Math.min(e, t == null ? void 0 : t.processedTxId));
    this._updatePendingMutations((t) => {
      for (const [r, s] of Array.from(t.entries()))
        s["tx-id"] && s["tx-id"] <= e && t.delete(r);
    });
  }
  /**
   * After mutations is confirmed by server, we give each query 30 sec
   * to update its results. If that doesn't happen, we assume query is
   * unaffected by this mutation and it’s safe to delete it from local queue
   */
  _cleanupPendingMutationsTimeout() {
    if (this._pendingMutations().size < this._pendingMutationCleanupThreshold)
      return;
    const e = Date.now();
    this._updatePendingMutations((t) => {
      for (const [r, s] of Array.from(t.entries()))
        s.confirmed && s.confirmed + this._pendingTxCleanupTimeout < e && t.delete(r);
    });
  }
  _trySendAuthed(...e) {
    this.status === Ce.AUTHENTICATED && this._trySend(...e);
  }
  _trySend(e, t, r) {
    if (this._transport.isOpen()) {
      switch (Yr[t.op] || this._log.info("[send]", this._transport.id, t.op, {
        "client-event-id": e,
        ...t
      }), t.op) {
        case "transact": {
          this._inFlightMutationEventIds.add(e);
          break;
        }
        case "init":
          this._inFlightMutationEventIds.clear();
      }
      this._transport.send({ "client-event-id": e, ...t });
    }
  }
  _startSocket() {
    if (this._wsOk = null, this._isShutdown) {
      this._log.info("[socket][start]", this.config.appId, "Reactor has been shut down and will not start a new socket");
      return;
    }
    if (this._transport && this._transport.isConnecting()) {
      this._log.info("[socket][start]", this._transport.id, "maintained as current transport, we were still in a connecting state");
      return;
    }
    const e = this._transport;
    this._transport = Qc({
      transportType: this._transportType,
      appId: this.config.appId,
      apiURI: this.config.apiURI,
      wsURI: this.config.websocketURI,
      EventSourceImpl: this._EventSource
    }), this._transport.onopen = this._transportOnOpen, this._transport.onmessage = this._transportOnMessage, this._transport.onclose = this._transportOnClose, this._transport.onerror = this._transportOnError, this._log.info("[socket][start]", this._transport.id), e != null && e.isOpen() && (this._log.info("[socket][start]", this._transport.id, "close previous transport id = ", e.id), e.close());
  }
  /**
   * Given a key, returns a stable local id, unique to this device and app.
   *
   * This can be useful if you want to create guest ids for example.
   *
   * Note: If the user deletes their local storage, this id will change.
   *
   */
  async getLocalId(e) {
    const t = `localToken_${e}`;
    if (this.kv.currentValue[t])
      return this.kv.currentValue[t];
    const r = await this.kv.waitForKeyToLoad(t);
    if (r)
      return r;
    const s = H();
    return this.kv.updateInPlace((i) => {
      i[t] || (i[t] = s);
    }), await this.kv.waitForKeyToLoad(t);
  }
  // ----
  // Auth
  _replaceUrlAfterOAuth() {
    if (typeof URL > "u")
      return;
    const e = new URL(window.location.href);
    if (e.searchParams.get(pn)) {
      const t = e.toString();
      e.searchParams.delete(pn), e.searchParams.delete(yn), e.searchParams.delete("code"), e.searchParams.delete("error");
      const r = e.pathname + (e.searchParams.size ? "?" + e.searchParams : "") + e.hash;
      if (history.replaceState(history.state, "", r), // @ts-ignore (waiting for ts support)
      typeof navigation == "object" && // @ts-ignore (waiting for ts support)
      typeof navigation.addEventListener == "function" && // @ts-ignore (waiting for ts support)
      typeof navigation.removeEventListener == "function") {
        let s = !1;
        const i = (o) => {
          var a;
          s || (s = !0, navigation.removeEventListener("navigate", i), !o.userInitiated && o.navigationType === "replace" && ((a = o.destination) == null ? void 0 : a.url) === t && history.replaceState(history.state, "", r));
        };
        navigation.addEventListener("navigate", i);
      }
    }
  }
  /**
   *
   * @returns Promise<null | {error: {message: string}}>
   */
  async _oauthLoginInit() {
    var i, o, a, c;
    if (typeof window > "u" || typeof window.location > "u" || typeof URLSearchParams > "u")
      return null;
    const e = new URLSearchParams(window.location.search);
    if (!e.get(pn))
      return null;
    const t = e.get("error");
    if (t)
      return this._replaceUrlAfterOAuth(), { error: { message: t } };
    const r = e.get("code");
    if (!r)
      return null;
    const s = e.get(yn);
    this._replaceUrlAfterOAuth();
    try {
      let u;
      const l = await this.kv.waitForKeyToLoad(jt);
      s && l && (u = l[s]), l && this.kv.updateInPlace((b) => {
        delete b[jt];
      });
      const f = await this._getCurrentUser(), d = (f == null ? void 0 : f.type) === "guest", { user: y } = await Fr({
        apiURI: this.config.apiURI,
        appId: this.config.appId,
        code: r,
        refreshToken: d ? f.refresh_token : void 0,
        extraFields: u
      });
      return this.setCurrentUser(y), null;
    } catch (u) {
      return ((i = u == null ? void 0 : u.body) == null ? void 0 : i.type) === "record-not-found" && ((a = (o = u == null ? void 0 : u.body) == null ? void 0 : o.hint) == null ? void 0 : a["record-type"]) === "app-oauth-code" && await this._hasCurrentUser() ? null : { error: { message: ((c = u == null ? void 0 : u.body) == null ? void 0 : c.message) || "Error logging in." } };
    }
  }
  async _waitForOAuthCallbackResponse() {
    return await this._oauthCallbackResponse;
  }
  __subscribeMutationErrors(e) {
    return this.mutationErrorCbs.push(e), () => {
      this.mutationErrorCbs = this.mutationErrorCbs.filter((t) => t !== e);
    };
  }
  subscribeAuth(e) {
    this.authCbs.push(e);
    const t = this._currentUserCached;
    t.isLoading || e(this._currentUserCached);
    let r = !1;
    return this.getCurrentUser().then((s) => {
      r || Lt(s, t) || e(s);
    }), () => {
      r = !0, this.authCbs = this.authCbs.filter((s) => s !== e);
    };
  }
  async getAuth() {
    const { user: e, error: t } = await this.getCurrentUser();
    if (t)
      throw new le("Could not get current user: " + t.message);
    return e ?? null;
  }
  subscribeConnectionStatus(e) {
    return this.connectionStatusCbs.push(e), () => {
      this.connectionStatusCbs = this.connectionStatusCbs.filter((t) => t !== e);
    };
  }
  subscribeAttrs(e) {
    return this.attrsCbs.push(e), this.attrs && e(this.attrs.attrs), () => {
      this.attrsCbs = this.attrsCbs.filter((t) => t !== e);
    };
  }
  notifyAuthSubs(e) {
    this.authCbs.forEach((t) => t(e));
  }
  notifyMutationErrorSubs(e) {
    this.mutationErrorCbs.forEach((t) => t(e));
  }
  notifyAttrsSubs() {
    if (!this.attrs)
      return;
    const e = this.optimisticAttrs();
    this.attrsCbs.forEach((t) => t(e.attrs));
  }
  notifyConnectionStatusSubs(e) {
    this.connectionStatusCbs.forEach((t) => t(e));
  }
  async setCurrentUser(e) {
    this.kv.updateInPlace((t) => {
      t[Ge] = e;
    }), await this.kv.waitForKeyToLoad(Ge);
  }
  getCurrentUserCached() {
    return this._currentUserCached;
  }
  /**
   * @param {{ forceReadFromStorage?: boolean }} [opts]
   * @returns {Promise<User | undefined>}
   */
  async _getCurrentUser(e) {
    e != null && e.forceReadFromStorage && await this.kv.unloadKey(Ge);
    const t = await this.kv.waitForKeyToLoad(Ge);
    return typeof t == "string" ? JSON.parse(t) : t;
  }
  /**
   * @param {{ forceReadFromStorage?: boolean }} [opts]
   * @returns {Promise<AuthState>}
   */
  async getCurrentUser(e) {
    const t = await this._waitForOAuthCallbackResponse();
    if (t != null && t.error) {
      const r = { error: t.error, user: void 0 };
      return this._currentUserCached = { isLoading: !1, ...r }, this._currentUserCached;
    }
    try {
      const s = { user: await this._getCurrentUser(e), error: void 0 };
      return this._currentUserCached = {
        isLoading: !1,
        ...s
      }, this._currentUserCached;
    } catch (r) {
      return {
        user: void 0,
        isLoading: !1,
        error: { message: (r == null ? void 0 : r.message) || "Error loading user" }
      };
    }
  }
  async _hasCurrentUser() {
    const e = await this.kv.waitForKeyToLoad(Ge);
    return typeof e == "string" ? JSON.parse(e) != null : e != null;
  }
  async changeCurrentUser(e) {
    var r;
    const { user: t } = await this.getCurrentUser();
    if (!Lt(t, e)) {
      this._frameworkClient = null, await this.setCurrentUser(e), await this.updateUser(e);
      try {
        await this.kv.flush(), (r = this._broadcastChannel) == null || r.postMessage({ type: "auth" });
      } catch (s) {
        console.error("Error posting message to broadcast channel", s);
      }
    }
  }
  async syncUserToEndpoint(e) {
    if (this.config.firstPartyPath)
      try {
        await fetch(this.config.firstPartyPath + "/", {
          method: "POST",
          body: JSON.stringify({
            type: "sync-user",
            appId: this.config.appId,
            user: e
          }),
          headers: {
            "Content-Type": "application/json"
          }
        });
      } catch (t) {
        this._log.error("Error syncing user with external endpoint", t);
      }
  }
  async updateUser(e) {
    try {
      await this.syncUserToEndpoint(e);
    } catch (r) {
      this._log.error("Error syncing user with external endpoint", r);
    }
    const t = { error: void 0, user: e };
    this._currentUserCached = { isLoading: !1, ...t }, this._dataForQueryCache = {}, this.querySubs.updateInPlace((r) => {
      Object.keys(r).forEach((s) => {
        delete r[s].result;
      });
    }), this.querySubs.clearUnloadedKeys(), this._updatePendingMutations((r) => {
      for (const [s, i] of r.entries())
        this.mutationDeferredStore.get(s) && this._finishTransaction("error", s, {
          message: "User changed while transaction was in progress.",
          type: "user-changed"
        });
      r.clear();
    }), this._reconnectTimeoutMs = 0, this._transport.close(), this._oauthCallbackResponse = null, this.notifyAuthSubs(t);
  }
  sendMagicCode({ email: e }) {
    return oc({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      email: e
    });
  }
  async signInWithMagicCode(e) {
    var i, o;
    const t = await this.getCurrentUser(), r = ((i = t == null ? void 0 : t.user) == null ? void 0 : i.type) === "guest", s = await ac({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      email: e.email,
      code: e.code,
      refreshToken: r ? (o = t == null ? void 0 : t.user) == null ? void 0 : o.refresh_token : void 0,
      extraFields: e.extraFields
    });
    return await this.changeCurrentUser(s.user), s;
  }
  async signInWithCustomToken(e) {
    const t = await cc({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      refreshToken: e
    });
    return await this.changeCurrentUser(t.user), t;
  }
  async signInAsGuest() {
    const e = await uc({
      apiURI: this.config.apiURI,
      appId: this.config.appId
    });
    return await this.changeCurrentUser(e.user), e;
  }
  potentiallyInvalidateToken(e, t) {
    var i;
    const r = (i = e == null ? void 0 : e.user) == null ? void 0 : i.refresh_token;
    if (!r)
      return;
    if (t.invalidateToken === !1) {
      this._log.info("[auth-invalidate] skipped invalidateToken");
      return;
    }
    fc({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      refreshToken: r
    }).then(() => {
      this._log.info("[auth-invalidate] completed invalidateToken");
    }).catch((o) => {
    });
  }
  async signOut(e) {
    const t = await this.getCurrentUser();
    this.potentiallyInvalidateToken(t, e), await this.changeCurrentUser(null);
  }
  /**
   * Creates an OAuth authorization URL.
   *
   * @param {Object} params - The parameters to create the authorization URL.
   * @param {string} params.clientName - The name of the client requesting authorization.
   * @param {string} params.redirectURL - The URL to redirect users to after authorization.
   * @param {Record<string, any>} [params.extraFields] - Extra fields to write to $users on creation
   * @returns {string} The created authorization URL.
   */
  createAuthorizationURL({ clientName: e, redirectURL: t, extraFields: r }) {
    const { apiURI: s, appId: i } = this.config;
    let o = t;
    if (r) {
      const a = `${Math.random().toString(36).slice(2)}`;
      this.kv.updateInPlace((c) => {
        const u = c[jt] || {};
        u[a] = r, c[jt] = u;
      }), o = `${t}${t.includes("?") ? "&" : "?"}${yn}=${a}`;
    }
    return `${s}/runtime/oauth/start?app_id=${i}&client_name=${e}&redirect_uri=${encodeURIComponent(o)}`;
  }
  /**
   * @param {Object} params
   * @param {string} params.code - The code received from the OAuth service.
   * @param {string} [params.codeVerifier] - The code verifier used to generate the code challenge.
   * @param {Record<string, any>} [params.extraFields] - Extra fields to write to $users on creation
   */
  async exchangeCodeForToken({ code: e, codeVerifier: t, extraFields: r }) {
    var a, c;
    const s = await this.getCurrentUser(), i = ((a = s == null ? void 0 : s.user) == null ? void 0 : a.type) === "guest", o = await Fr({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      code: e,
      codeVerifier: t,
      refreshToken: i ? (c = s == null ? void 0 : s.user) == null ? void 0 : c.refresh_token : void 0,
      extraFields: r
    });
    return await this.changeCurrentUser(o.user), o;
  }
  issuerURI() {
    const { apiURI: e, appId: t } = this.config;
    return `${e}/runtime/${t}`;
  }
  /**
   * @param {Object} params
   * @param {string} params.clientName - The name of the client requesting authorization.
   * @param {string} params.idToken - The id_token from the external service
   * @param {string | null | undefined} [params.nonce] - The nonce used when requesting the id_token from the external service
   * @param {Record<string, any>} [params.extraFields] - Extra fields to write to $users on creation
   */
  async signInWithIdToken(e) {
    var i;
    const t = await this.getCurrentUser(), r = (i = t == null ? void 0 : t.user) == null ? void 0 : i.refresh_token, s = await lc({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      idToken: e.idToken,
      clientName: e.clientName,
      nonce: e.nonce,
      refreshToken: r,
      extraFields: e.extraFields
    });
    return await this.changeCurrentUser(s.user), s;
  }
  // --------
  // Rooms
  /**
   * @param {string} roomType
   * @param {string} roomId
   * @param {any | null | undefined} [initialPresence] -- initial presence data to send when joining the room
   * @returns () => void
   */
  joinRoom(e, t, r) {
    let s = !1;
    this._rooms[t] || (s = !0, this._rooms[t] = {
      roomType: e,
      isConnected: !1,
      error: void 0
    }), this._presence[t] = this._presence[t] || {};
    const i = this._presence[t].result;
    return r && !i && (this._presence[t].result = this._presence[t].result || {}, this._presence[t].result.user = r, this._notifyPresenceSubs(t)), s && this._tryJoinRoom(e, t, r), () => {
      this._cleanupRoom(t);
    };
  }
  _cleanupRoom(e) {
    var t, r, s;
    if (!((r = (t = this._presence[e]) == null ? void 0 : t.handlers) != null && r.length) && !Object.keys(this._broadcastSubs[e] ?? {}).length) {
      const i = (s = this._rooms[e]) == null ? void 0 : s.isConnected;
      delete this._rooms[e], delete this._presence[e], delete this._broadcastSubs[e], i ? this._tryLeaveRoom(e) : this._roomsPendingLeave[e] = !0;
    }
  }
  // --------
  // Presence
  // TODO: look into typing again
  getPresence(e, t, r = {}) {
    const s = this._rooms[t], i = this._presence[t];
    return !s || !i || !i.result ? null : {
      ...gc(i.result, r, this._sessionId),
      isLoading: !s.isConnected,
      error: s.error
    };
  }
  // TODO: look into typing again
  publishPresence(e, t, r) {
    const s = this._rooms[t], i = this._presence[t];
    if (!s || !i)
      return;
    i.result = i.result || {};
    const o = {
      ...i.result.user,
      ...r
    };
    i.result.user = o, s.isConnected && (this._trySetPresence(t, o), this._notifyPresenceSubs(t));
  }
  _trySetPresence(e, t) {
    this._trySendAuthed(H(), {
      op: "set-presence",
      "room-id": e,
      data: t
    });
  }
  _tryJoinRoom(e, t, r) {
    this._trySendAuthed(H(), {
      op: "join-room",
      "room-type": e,
      "room-id": t,
      data: r
    }), delete this._roomsPendingLeave[t];
  }
  _tryLeaveRoom(e) {
    this._trySendAuthed(H(), { op: "leave-room", "room-id": e });
  }
  _trySetRoomConnected(e, t) {
    const r = this._rooms[e];
    r && (r.isConnected = t);
  }
  // TODO: look into typing again
  subscribePresence(e, t, r, s) {
    const i = this.joinRoom(
      e,
      t,
      // Oct 28, 2025
      // Note: initialData is deprecated.
      // Keeping here for backwards compatibility
      r.initialPresence || r.initialData
    ), o = { ...r, roomId: t, cb: s, prev: null };
    return this._presence[t] = this._presence[t] || {}, this._presence[t].handlers = this._presence[t].handlers || [], this._presence[t].handlers.push(o), this._notifyPresenceSub(t, o), () => {
      var a, c;
      this._presence[t].handlers = ((c = (a = this._presence[t]) == null ? void 0 : a.handlers) == null ? void 0 : c.filter((u) => u !== o)) ?? [], i();
    };
  }
  _notifyPresenceSubs(e) {
    var t, r;
    (r = (t = this._presence[e]) == null ? void 0 : t.handlers) == null || r.forEach((s) => {
      this._notifyPresenceSub(e, s);
    });
  }
  _notifyPresenceSub(e, t) {
    const r = this.getPresence("", e, t);
    r && (t.prev && !_c(r, t.prev) || (t.prev = r, t.cb(r)));
  }
  _patchPresencePeers(e, t) {
    var o, a, c;
    const r = ((a = (o = this._presence[e]) == null ? void 0 : o.result) == null ? void 0 : a.peers) || {};
    let s = Object.fromEntries(Object.entries(r).map(([u, l]) => [u, { data: l }]));
    (c = this._presence[e]) == null || c.result;
    const i = Qt(s, (u) => {
      for (let [l, f, d] of t)
        switch (f) {
          case "+":
            Bi(u, l, d);
            break;
          case "r":
            kr(u, l, d);
            break;
          case "-":
            Ss(u, l);
            break;
        }
      delete u[this._sessionId];
    });
    this._setPresencePeers(e, i);
  }
  _setPresencePeers(e, t) {
    const r = { ...t };
    delete r[this._sessionId];
    const s = Object.fromEntries(Object.entries(r).map(([i, o]) => [i, o.data]));
    this._presence = Qt(this._presence, (i) => {
      kr(i, [e, "result", "peers"], s);
    });
  }
  // --------
  // Broadcast
  publishTopic({ roomType: e, roomId: t, topic: r, data: s }) {
    const i = this._rooms[t];
    if (i) {
      if (!i.isConnected) {
        this._broadcastQueue[t] = this._broadcastQueue[t] ?? [], this._broadcastQueue[t].push({ topic: r, roomType: e, data: s });
        return;
      }
      this._tryBroadcast(t, e, r, s);
    }
  }
  _tryBroadcast(e, t, r, s) {
    this._trySendAuthed(H(), {
      op: "client-broadcast",
      "room-id": e,
      roomType: t,
      topic: r,
      data: s
    });
  }
  subscribeTopic(e, t, r, s) {
    const i = this.joinRoom(e, t);
    return this._broadcastSubs[t] = this._broadcastSubs[t] || {}, this._broadcastSubs[t][r] = this._broadcastSubs[t][r] || [], this._broadcastSubs[t][r].push(s), this._presence[t] = this._presence[t] || {}, () => {
      this._broadcastSubs[t][r] = this._broadcastSubs[t][r].filter((o) => o !== s), this._broadcastSubs[t][r].length || delete this._broadcastSubs[t][r], i();
    };
  }
  _notifyBroadcastSubs(e, t, r) {
    var s, i, o;
    (o = (i = (s = this._broadcastSubs) == null ? void 0 : s[e]) == null ? void 0 : i[t]) == null || o.forEach((a) => {
      var l, f, d, y, b, g;
      const c = (l = r.data) == null ? void 0 : l.data, u = r.data["peer-id"] === this._sessionId ? (d = (f = this._presence[e]) == null ? void 0 : f.result) == null ? void 0 : d.user : (g = (b = (y = this._presence[e]) == null ? void 0 : y.result) == null ? void 0 : b.peers) == null ? void 0 : g[r.data["peer-id"]];
      return a(c, u);
    });
  }
  // --------
  // Storage
  async uploadFile(e, t, r) {
    var o;
    const s = await this.getCurrentUser(), i = (o = s == null ? void 0 : s.user) == null ? void 0 : o.refresh_token;
    return dc({
      ...r,
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      path: e,
      file: t,
      refreshToken: i
    });
  }
  async deleteFile(e) {
    var i;
    const t = await this.getCurrentUser(), r = (i = t == null ? void 0 : t.user) == null ? void 0 : i.refresh_token;
    return await hc({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      path: e,
      refreshToken: r
    });
  }
  // Deprecated Storage API (Jan 2025)
  // ---------------------------------
  async upload(e, t) {
    var c;
    const r = await this.getCurrentUser(), s = (c = r == null ? void 0 : r.user) == null ? void 0 : c.refresh_token, i = e || t.name, o = await pc({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      fileName: i,
      refreshToken: s
    });
    return await yc(o, t);
  }
  async getDownloadUrl(e) {
    var i;
    const t = await this.getCurrentUser(), r = (i = t == null ? void 0 : t.user) == null ? void 0 : i.refresh_token;
    return await bc({
      apiURI: this.config.apiURI,
      appId: this.config.appId,
      path: e,
      refreshToken: r
    });
  }
}
function eu(n, e) {
  return new en(
    ei(n, e),
    // (XXX): LinksDef<any> stems from TypeScript’s inability to reconcile the
    // type EntitiesWithLinks<EntitiesWithoutLinks, Links> with
    // EntitiesWithoutLinks. TypeScript is strict about ensuring that types are
    // correctly aligned and does not allow for substituting a type that might
    // be broader or have additional properties.
    e,
    void 0
  );
}
function tu(n) {
  return new Xt(n, {});
}
function nu() {
  return new fe("string", !0, !1);
}
function ru() {
  return new fe("number", !0, !1);
}
function su() {
  return new fe("boolean", !0, !1);
}
function iu() {
  return new fe("date", !0, !1);
}
function ou() {
  return new fe("json", !0, !1);
}
function au() {
  return new fe("json", !0, !1);
}
function ei(n, e) {
  var s, i, o, a;
  const t = { fwd: {}, rev: {} };
  for (const c of Object.values(e))
    (s = t.fwd)[i = c.forward.on] || (s[i] = {}), (o = t.rev)[a = c.reverse.on] || (o[a] = {}), t.fwd[c.forward.on][c.forward.label] = {
      entityName: c.reverse.on,
      cardinality: c.forward.has
    }, t.rev[c.reverse.on][c.reverse.label] = {
      entityName: c.forward.on,
      cardinality: c.reverse.has
    };
  return Object.fromEntries(Object.entries(n).map(([c, u]) => [
    c,
    new Xt(u.attrs, {
      ...t.fwd[c],
      ...t.rev[c]
    })
  ]));
}
function cu({ entities: n, links: e, rooms: t }) {
  const r = e ?? {}, s = t ?? {};
  return new en(
    ei(n, r),
    // (XXX): LinksDef<any> stems from TypeScript's inability to reconcile the
    // type EntitiesWithLinks<EntitiesWithoutLinks, Links> with
    // EntitiesWithoutLinks. TypeScript is strict about ensuring that types are
    // correctly aligned and does not allow for substituting a type that might
    // be broader or have additional properties.
    r,
    s
  );
}
const al = {
  // constructs
  graph: eu,
  schema: cu,
  entity: tu,
  // value types
  string: nu,
  number: ru,
  boolean: su,
  date: iu,
  json: ou,
  any: au
};
let xt;
function uu(n, e) {
  xt == null || xt.dispose();
  const t = yu(e), r = du(e, a), s = fu(lu(n));
  function i(l) {
    var f;
    l.source === s.element.contentWindow && ((f = l.data) == null ? void 0 : f.type) === "close" && t.isVisible() && a();
  }
  function o(l) {
    const f = l.shiftKey && l.ctrlKey && l.key === "0", d = l.key === "Escape" || l.key === "Esc";
    (f || d && t.isVisible()) && a();
  }
  function a() {
    t.isVisible() ? t.element.style.display = "none" : (t.element.style.display = "block", t.element.contains(s.element) || t.element.appendChild(s.element));
  }
  function c() {
    t.element.remove(), r.element.remove(), removeEventListener("keydown", o), removeEventListener("message", i);
  }
  function u() {
    document.body.appendChild(t.element), document.body.appendChild(r.element), addEventListener("keydown", o), addEventListener("message", i), xt = {
      dispose: c
    };
  }
  return u();
}
function lu(n) {
  return `${rr || Qs ? "http://localhost:3000" : "https://instantdb.com"}/_devtool?appId=${n}`;
}
function fu(n) {
  const e = document.createElement("iframe");
  return e.src = n, e.className = "instant-devtool-iframe", Object.assign(e.style, {
    width: "100%",
    height: "100%",
    backgroundColor: "white",
    border: "none"
  }), { element: e };
}
function du(n, e) {
  const t = `
    <svg width="32" height="32" viewBox="0 0 512 512" fill="none" xmlns="http://www.w3.org/2000/svg">
      <rect width="512" height="512" fill="black"/>
      <rect x="97.0973" y="91.3297" width="140" height="330" fill="white"/>
    </svg>
  `, r = document.createElement("button");
  return r.innerHTML = t, r.className = "instant-devtool-toggler", Object.assign(r.style, {
    // pos
    position: "fixed",
    ...hu(n.position),
    height: "32px",
    width: "32px",
    // layout
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    zIndex: "9010",
    // look
    padding: "0",
    margin: "0",
    border: "none",
    cursor: "pointer"
  }), r.addEventListener("click", e), { element: r };
}
function hu(n) {
  switch (n) {
    case "bottom-left":
      return { bottom: "24px", left: "24px" };
    case "bottom-right":
      return { bottom: "24px", right: "24px" };
    case "top-right":
      return { top: "24px", right: "24px" };
    case "top-left":
      return { top: "24px", left: "24px" };
  }
}
function pu(n) {
  switch (n) {
    case "bottom-left":
      return { bottom: "24px", right: "24px", left: "60px", top: "72px" };
    case "bottom-right":
      return { bottom: "24px", left: "24px", right: "60px", top: "72px" };
    case "top-right":
      return { top: "24px", left: "24px", right: "60px", bottom: "72px" };
    case "top-left":
      return { top: "24px", right: "24px", left: "60px", bottom: "72px" };
  }
}
function yu(n) {
  const e = document.createElement("div");
  Object.assign(e.style, {
    position: "fixed",
    ...pu(n.position),
    display: "block",
    borderRadius: "4px",
    border: "1px #ccc solid",
    boxShadow: "0px 0px 8px #00000044",
    backgroundColor: "#eee",
    zIndex: "999990"
  }), e.style.display = "none", e.className = "instant-devtool-container";
  function t() {
    return e.style.display !== "none";
  }
  return { element: e, isVisible: t };
}
function bu(n, e) {
  return e && e.refresh_token ? new Response(JSON.stringify({ ok: !0 }), {
    headers: {
      "Content-Type": "application/json",
      // 7 day expiry
      "Set-Cookie": `instant_user_${n.appId}=${encodeURIComponent(JSON.stringify(e))}; Path=/; HttpOnly; Secure; SameSite=Strict; Max-Age=604800`
    }
  }) : new Response(JSON.stringify({ ok: !0 }), {
    headers: {
      "Content-Type": "application/json",
      // remove the cookie (some browsers)
      "Set-Cookie": `instant_user_${n.appId}=; Path=/; HttpOnly; Secure; SameSite=Strict; Max-Age=-1`
    }
  });
}
function $t(n, e) {
  return new Response(JSON.stringify({ ok: !1, error: e }), {
    status: n,
    headers: { "Content-Type": "application/json" }
  });
}
const cl = (n) => ({
  POST: async (e) => {
    let t;
    try {
      t = await e.json();
    } catch {
      return $t(400, "Invalid JSON body");
    }
    if (!t.type)
      return $t(400, 'Missing "type" field');
    if (t.appId !== n.appId)
      return $t(403, "App ID mismatch");
    switch (t.type) {
      case "sync-user":
        return bu(n, t.user ?? null);
      default:
        return $t(400, `Unknown type: ${t.type}`);
    }
  }
});
function ut(n, e) {
  if (!n)
    throw new Error("[assertion error] " + e);
}
const gu = (n) => JSON.stringify(n), bt = (n, e = !1) => JSON.stringify([n, e]), Zr = (n) => {
  const [e, t] = JSON.parse(n);
  return { cursor: e, afterInclusive: t };
}, He = (n, e, t = !1) => `${n}:${bt(e, t)}`, _u = (n) => !!n.endCursor, mu = (n) => {
  var t;
  const e = Array.from(n.values());
  return e.length === 0 ? !1 : ((t = e[e.length - 1]) == null ? void 0 : t.hasMore) || !1;
}, Xr = (n) => {
  if (!n)
    return {
      serverCreatedAt: "asc"
    };
  const e = Object.keys(n).at(0);
  return e ? {
    [e]: n[e] === "asc" ? "desc" : "asc"
  } : {
    serverCreatedAt: "asc"
  };
}, ti = (n) => n && Object.keys(n).length > 0 ? n : {
  serverCreatedAt: "asc"
}, wu = (n, e) => {
  const t = [
    ...Array.from(e.values()).slice().reverse(),
    ...Array.from(n.values())
  ], r = [
    ...Array.from(e.values()).slice().reverse().flatMap((s) => s.data.slice().reverse()),
    ...Array.from(n.values()).flatMap((s) => s.data)
  ];
  return { chunks: t, data: r };
}, es = ["bootstrap", "bootstrap", "bootstrap", 1], Tu = (n, e, t, r) => {
  var Ae, re, se, B;
  const { entityName: s, entityQuery: i } = ni(e), o = ((Ae = i.$) == null ? void 0 : Ae.limit) || 10, a = s, c = ti((re = i.$) == null ? void 0 : re.order), u = /* @__PURE__ */ new Map(), l = /* @__PURE__ */ new Map(), f = /* @__PURE__ */ new Map();
  let d = !1, y = !0, b = null, g = null;
  const w = (k) => {
    t({ error: k, data: void 0, canLoadNextPage: !1 });
  }, S = () => {
    if (!y)
      return;
    const { chunks: k, data: M } = wu(u, l);
    t({
      data: { [a]: M },
      // @ts-expect-error hidden debug variable
      chunks: k,
      canLoadNextPage: mu(u)
    });
  }, _ = (k, M) => {
    u.set(bt(k, M.afterInclusive), M), S();
  }, m = (k, M) => {
    l.set(bt(k), M), N(), S();
  }, A = (k, M) => {
    var O, C;
    const { cursor: x } = Zr(k), q = f.get(He("reverse", x));
    q == null || q();
    const z = n.subscribeQuery({
      [a]: {
        ...i,
        $: {
          after: x,
          before: M.endCursor,
          beforeInclusive: !0,
          where: (O = i.$) == null ? void 0 : O.where,
          fields: (C = i.$) == null ? void 0 : C.fields,
          order: Xr(c)
        }
      }
    }, (L) => {
      if (L.error)
        return w(L.error);
      const V = L.data[a], J = L.pageInfo[a];
      ut(V && J, "Expected query subscription to contain rows and pageInfo"), m(x, {
        data: V,
        status: "frozen",
        hasMore: J.hasNextPage,
        endCursor: J.endCursor
      });
    }, r);
    f.set(He("reverse", x), z);
  }, v = (k) => {
    var x, q;
    const M = n.subscribeQuery({
      [a]: {
        ...i,
        $: {
          limit: o,
          after: k,
          where: (x = i.$) == null ? void 0 : x.where,
          fields: (q = i.$) == null ? void 0 : q.fields,
          order: Xr(c)
        }
      }
    }, (z) => {
      if (z.error)
        return w(z.error);
      const O = z.data[a], C = z.pageInfo[a];
      ut(O && C, "Expected rows and pageInfo"), m(k, {
        data: O,
        status: "bootstrapping",
        hasMore: C.hasNextPage,
        endCursor: C.endCursor
      });
    }, r);
    f.set(He("reverse", k), M);
  }, I = (k, M = !1) => {
    var q, z;
    const x = n.subscribeQuery({
      [a]: {
        ...i,
        $: {
          limit: o,
          after: k,
          afterInclusive: M,
          where: (q = i.$) == null ? void 0 : q.where,
          fields: (z = i.$) == null ? void 0 : z.fields,
          order: c
        }
      }
    }, (O) => {
      if (O.error)
        return w(O.error);
      const C = O.data[a], L = O.pageInfo[a];
      ut(C && L, "Page info and rows"), _(k, {
        data: C,
        status: "bootstrapping",
        hasMore: L.hasNextPage,
        endCursor: L.endCursor,
        afterInclusive: M
      });
    }, r);
    f.set(He("forward", k, M), x);
  }, j = (k, M = !1) => {
    var C, L;
    const x = bt(k, M), q = f.get(He("forward", k, M));
    q == null || q();
    const z = u.get(x);
    if (!(z != null && z.endCursor))
      return;
    const O = n.subscribeQuery({
      [a]: {
        ...i,
        $: {
          after: k,
          afterInclusive: z.afterInclusive,
          before: z.endCursor,
          beforeInclusive: !0,
          where: (C = i.$) == null ? void 0 : C.where,
          fields: (L = i.$) == null ? void 0 : L.fields,
          order: c
        }
      }
    }, (V) => {
      if (V.error)
        return w(V.error);
      const J = V.data[a], ae = V.pageInfo[a];
      ut(J && ae, "Expected rows and pageInfo"), _(k, {
        data: J,
        status: "frozen",
        hasMore: ae.hasNextPage,
        endCursor: ae.endCursor,
        afterInclusive: z.afterInclusive
      });
    }, r);
    f.set(He("forward", k, M), O);
  }, N = () => {
    const k = Array.from(l.entries()).at(-1);
    if (!k)
      return;
    const [M, x] = k;
    if (!(x != null && x.hasMore) || !_u(x))
      return;
    const q = `${M}:${gu(x.endCursor)}`;
    q != b && (b = q, A(M, x), v(x.endCursor));
  }, Q = () => {
    const k = Array.from(u.entries()).at(-1);
    if (!k)
      return;
    const [M, x] = k;
    if (!x.endCursor)
      return;
    const { cursor: q, afterInclusive: z } = Zr(M);
    j(q, z), I(x.endCursor);
  };
  return g = n.subscribeQuery({
    [a]: {
      ...i,
      $: {
        limit: o,
        where: (se = i.$) == null ? void 0 : se.where,
        fields: (B = i.$) == null ? void 0 : B.fields,
        order: c
      }
    }
  }, async (k) => {
    var z;
    if (d)
      return;
    if (k.error)
      return w(k.error);
    const M = k.pageInfo[a], x = (z = k == null ? void 0 : k.data) == null ? void 0 : z[a];
    if (ut(x && M, "Expected rows and pageInfo"), x.length < o) {
      _(es, {
        data: x,
        status: "pre-bootstrap"
      });
      return;
    }
    const q = M.startCursor;
    q && (u.delete(bt(es)), I(q, !0), v(M.startCursor), d = !0, await n._reactor.querySubs.flush(), g == null || g(), g = null);
  }, r), {
    unsubscribe: () => {
      if (y) {
        y = !1, g == null || g(), g = null;
        for (const k of f.values())
          k == null || k();
        f.clear();
      }
    },
    loadNextPage: Q
  };
}, Su = (n, e, t) => {
  var u, l, f, d;
  if (!e)
    return {
      canLoadNextPage: !1,
      data: void 0,
      error: void 0
    };
  const { entityName: r, entityQuery: s } = ni(e), i = ((u = s.$) == null ? void 0 : u.limit) || 10, o = ti((l = s.$) == null ? void 0 : l.order);
  let a = e ? ri({
    [r]: {
      ...s,
      $: {
        limit: i,
        where: (f = s.$) == null ? void 0 : f.where,
        fields: (d = s.$) == null ? void 0 : d.fields,
        order: o
      }
    }
  }) : null;
  t && "ruleParams" in t && (a = {
    $$ruleParams: t.ruleParams,
    ...a
  });
  const c = n._reactor.getPreviousResult(a);
  return {
    canLoadNextPage: !1,
    data: (c == null ? void 0 : c.data) || void 0,
    error: void 0
  };
}, ni = (n) => {
  if (Object.keys(n).length !== 1)
    throw new W("subscribeInfiniteQuery expects exactly one entity");
  const [t, r] = Object.entries(n)[0];
  if (!t || !r)
    throw new W("No query provided for infinite query");
  return { entityName: t, entityQuery: r };
}, vu = {
  apiURI: "https://api.instantdb.com",
  websocketURI: "wss://api.instantdb.com/runtime/session"
};
function Eu() {
  return globalThis.__instantDbSchemaHashStore = globalThis.__instantDbSchemaHashStore ?? /* @__PURE__ */ new WeakMap(), globalThis.__instantDbSchemaHashStore;
}
function Ou() {
  return globalThis.__instantDbStore = globalThis.__instantDbStore ?? {}, globalThis.__instantDbStore;
}
function xn(n) {
  const e = n.__adminToken;
  return n.appId + "_" + (n.websocketURI || "default_ws_uri") + "_" + (n.apiURI || "default_api_uri") + "_" + (e || "client_only") + "_" + n.useDateObjects;
}
const $n = Ou(), ts = Eu();
class ku {
  constructor(e) {
    p(this, "db");
    /**
     * Sends a magic code to the user's email address.
     *
     * Once you send the magic code, see {@link auth.signInWithMagicCode} to let the
     * user verify.
     *
     * @see https://instantdb.com/docs/auth
     * @example
     *  db.auth.sendMagicCode({email: "example@gmail.com"})
     *    .catch((err) => console.error(err.body?.message))
     */
    p(this, "sendMagicCode", (e) => this.db.sendMagicCode(e));
    /**
     * Verify a magic code that was sent to the user's email address.
     *
     * @see https://instantdb.com/docs/auth
     *
     * @example
     *  db.auth.signInWithMagicCode({email: "example@gmail.com", code: "123456"})
     *       .catch((err) => console.error(err.body?.message))
     */
    p(this, "signInWithMagicCode", (e) => this.db.signInWithMagicCode(e));
    /**
     * Sign in a user with a refresh token
     *
     * @see https://instantdb.com/docs/backend#frontend-auth-sign-in-with-token
     *
     * @example
     *   // Get the token from your backend
     *   const token = await fetch('/signin', ...);
     *   //Sign in
     *   db.auth.signInWithToken(token);
     */
    p(this, "signInWithToken", (e) => this.db.signInWithCustomToken(e));
    /**
     * Sign in as guest, creating a new user without email
     *
     * @see https://instantdb.com/docs/auth
     *
     * @example
     *   db.auth.signInAsGuest();
     */
    p(this, "signInAsGuest", () => this.db.signInAsGuest());
    /**
     * Create an authorization url to sign in with an external provider.
     *
     * @see https://instantdb.com/docs/auth
     *
     * @example
     *   // Get the authorization url from your backend
     *   const url = db.auth.createAuthorizationUrl({
     *     clientName: "google",
     *     redirectURL: window.location.href,
     *   });
     *
     *   // Put it in a sign in link
     *   <a href={url}>Log in with Google</a>
     */
    p(this, "createAuthorizationURL", (e) => this.db.createAuthorizationURL(e));
    /**
     * Sign in with the id_token from an external provider like Google
     *
     * @see https://instantdb.com/docs/auth
     * @example
     *   db.auth
     *  .signInWithIdToken({
     *    // Token from external service
     *    idToken: id_token,
     *    // The name you gave the client when you registered it with Instant
     *    clientName: "google",
     *    // The nonce, if any, that you used when you initiated the auth flow
     *    // with the external service.
     *    nonce: your_nonce
     *  })
     *  .catch((err) => console.error(err.body?.message));
     *
     */
    p(this, "signInWithIdToken", (e) => this.db.signInWithIdToken(e));
    /**
     * Sign in with the id_token from an external provider like Google
     *
     * @see https://instantdb.com/docs/auth
     * @example
     *   db.auth
     *  .exchangeOAuthCode({
     *    // code received in redirect from OAuth callback
     *    code: code
     *    // The PKCE code_verifier, if any, that you used when you
     *    // initiated the auth flow
     *    codeVerifier: your_code_verifier
     *  })
     *  .catch((err) => console.error(err.body?.message));
     *
     */
    p(this, "exchangeOAuthCode", (e) => this.db.exchangeCodeForToken(e));
    /**
     * OpenID Discovery path for use with tools like
     * expo-auth-session that use auto-discovery of
     * OAuth parameters.
     *
     * @see https://instantdb.com/docs/auth
     * @example
     *   const discovery = useAutoDiscovery(
     *     db.auth.issuerURI()
     *   );
     */
    p(this, "issuerURI", () => this.db.issuerURI());
    /**
     * Sign out the current user
     */
    p(this, "signOut", (e = { invalidateToken: !0 }) => this.db.signOut(e));
    this.db = e;
  }
}
class Au {
  constructor(e) {
    p(this, "db");
    /**
     * Uploads file at the provided path.
     *
     * @see https://instantdb.com/docs/storage
     * @example
     *   const [file] = e.target.files; // result of file input
     *   const data = await db.storage.uploadFile('photos/demo.png', file);
     */
    p(this, "uploadFile", (e, t, r = {}) => this.db.uploadFile(e, t, r));
    /**
     * Deletes a file by path name.
     *
     * @deprecated Use `db.transact` to delete files instead:
     * @example
     *   // Delete by id
     *   db.transact(db.tx.$files[fileId].delete());
     *
     *   // Delete by path
     *   db.transact(db.tx.$files[lookup('path', 'photos/demo.png')].delete());
     *
     *   // Delete multiple files
     *   db.transact(fileIds.map(id => db.tx.$files[id].delete()));
     *
     * @see https://instantdb.com/docs/storage
     */
    p(this, "delete", (e) => this.db.deleteFile(e));
    // Deprecated Storage API (Jan 2025)
    // ---------------------------------
    /**
     * @deprecated. Use `db.storage.uploadFile` instead
     * remove in the future.
     */
    p(this, "upload", (e, t) => this.db.upload(e, t));
    /**
     * @deprecated Use `db.storage.uploadFile` instead
     */
    p(this, "put", this.upload);
    /**
     * @deprecated. getDownloadUrl will be removed in the future.
     * Use `useQuery` instead to query and fetch for valid urls
     *
     * db.useQuery({
     *   $files: {
     *     $: {
     *       where: {
     *         path: "moop.png"
     *       }
     *     }
     *   }
     * })
     */
    p(this, "getDownloadUrl", (e) => this.db.getDownloadUrl(e));
    this.db = e;
  }
}
class Cu {
  constructor(e) {
    p(this, "db");
    /**
     * Creates a new ReadableStream for the given clientId.
     *
     * @example
     *   const stream = db.streams.createReadStream({clientId: clientId})
     *   for await (const chunk of stream) {
     *     console.log(chunk);
     *   }
     */
    p(this, "createReadStream", (e) => this.db.createReadStream(e));
    /**
     * Creates a new WritableStream for the given clientId.
     *
     * @example
     *   const writeStream = db.streams.createWriteStream({clientId: clientId})
     *   const writer = writeStream.getWriter();
     *   writer.write('Hello world');
     *   writer.close();
     */
    p(this, "createWriteStream", (e) => this.db.createWriteStream(e));
    this.db = e;
  }
}
function ri(n) {
  return JSON.parse(JSON.stringify(n));
}
class Iu {
  constructor(e) {
    p(this, "_reactor");
    p(this, "auth");
    p(this, "storage");
    p(this, "streams");
    p(this, "tx", nr());
    this._reactor = e, this.auth = new ku(this._reactor), this.storage = new Au(this._reactor), this.streams = new Cu(this._reactor);
  }
  /**
   * Use this to write data! You can create, update, delete, and link objects
   *
   * @see https://instantdb.com/docs/instaml
   *
   * @example
   *   // Create a new object in the `goals` namespace
   *   const goalId = id();
   *   db.transact(db.tx.goals[goalId].update({title: "Get fit"}))
   *
   *   // Update the title
   *   db.transact(db.tx.goals[goalId].update({title: "Get super fit"}))
   *
   *   // Delete it
   *   db.transact(db.tx.goals[goalId].delete())
   *
   *   // Or create an association:
   *   todoId = id();
   *   db.transact([
   *    db.tx.todos[todoId].update({ title: 'Go on a run' }),
   *    db.tx.goals[goalId].link({todos: todoId}),
   *  ])
   */
  transact(e) {
    return this._reactor.pushTx(e);
  }
  getLocalId(e) {
    return this._reactor.getLocalId(e);
  }
  /**
   * Use this to query your data!
   *
   * @see https://instantdb.com/docs/instaql
   *
   * @example
   *  // listen to all goals
   *  db.subscribeQuery({ goals: {} }, (resp) => {
   *    console.log(resp.data.goals)
   *  })
   *
   *  // goals where the title is "Get Fit"
   *  db.subscribeQuery(
   *    { goals: { $: { where: { title: "Get Fit" } } } },
   *    (resp) => {
   *      console.log(resp.data.goals)
   *    }
   *  )
   *
   *  // all goals, _alongside_ their todos
   *  db.subscribeQuery({ goals: { todos: {} } }, (resp) => {
   *    console.log(resp.data.goals)
   *  });
   */
  subscribeQuery(e, t, r) {
    return this._reactor.subscribeQuery(e, t, r);
  }
  /**
   * Subscribe to a query and incrementally load more items
   *
   * Only one top level namespace in the query is allowed.
   * @example
   * const { unsubscribe, loadNextPage } = db.subscribeInfiniteQuery({
   *   posts: {
   *     $: {
   *       limit: 20,   // Load 20 posts at a time
   *       order: {
   *         createdAt: 'desc',
   *       },
   *     },
   *   },
   *   (resp) => {
   *     console.log(resp.data.posts);
   *   }
   * });
   */
  subscribeInfiniteQuery(e, t, r) {
    return Tu(this, e, t, r);
  }
  /**
   * Listen for the logged in state. This is useful
   * for deciding when to show a login screen.
   *
   * @see https://instantdb.com/docs/auth
   * @example
   *   const unsub = db.subscribeAuth((auth) => {
   *     if (auth.user) {
   *     console.log('logged in as', auth.user.email)
   *    } else {
   *      console.log('logged out')
   *    }
   *  })
   */
  subscribeAuth(e) {
    return this._reactor.subscribeAuth(e);
  }
  /**
   * One time query for the logged in state. This is useful
   * for scenarios where you want to know the current auth
   * state without subscribing to changes.
   *
   * @see https://instantdb.com/docs/auth
   * @example
   *   const user = await db.getAuth();
   *   console.log('logged in as', user.email)
   */
  getAuth() {
    return this._reactor.getAuth();
  }
  /**
   * Listen for connection status changes to Instant. This is useful
   * for building things like connectivity indicators
   *
   * @see https://www.instantdb.com/docs/patterns#connection-status
   * @example
   *   const unsub = db.subscribeConnectionStatus((status) => {
   *     const connectionState =
   *       status === 'connecting' || status === 'opened'
   *         ? 'authenticating'
   *       : status === 'authenticated'
   *         ? 'connected'
   *       : status === 'closed'
   *         ? 'closed'
   *       : status === 'errored'
   *         ? 'errored'
   *       : 'unexpected state';
   *
   *     console.log('Connection status:', connectionState);
   *   });
   */
  subscribeConnectionStatus(e) {
    return this._reactor.subscribeConnectionStatus(e);
  }
  /**
   * Join a room to publish and subscribe to topics and presence.
   *
   * @see https://instantdb.com/docs/presence-and-topics
   * @example
   * // init
   * const db = init();
   * const room = db.joinRoom(roomType, roomId);
   * // usage
   * const unsubscribeTopic = room.subscribeTopic("foo", console.log);
   * const unsubscribePresence = room.subscribePresence({}, console.log);
   * room.publishTopic("hello", { message: "hello world!" });
   * room.publishPresence({ name: "joe" });
   * // later
   * unsubscribePresence();
   * unsubscribeTopic();
   * room.leaveRoom();
   */
  joinRoom(e = "_defaultRoomType", t = "_defaultRoomId", r) {
    return {
      leaveRoom: this._reactor.joinRoom(e, t, r == null ? void 0 : r.initialPresence),
      subscribeTopic: (i, o) => this._reactor.subscribeTopic(e, t, i, o),
      subscribePresence: (i, o) => this._reactor.subscribePresence(e, t, i, o),
      publishTopic: (i, o) => this._reactor.publishTopic({ roomType: e, roomId: t, topic: i, data: o }),
      publishPresence: (i) => this._reactor.publishPresence(e, t, i),
      getPresence: (i) => this._reactor.getPresence(e, t, i)
    };
  }
  shutdown() {
    delete $n[xn(this._reactor.config)], this._reactor.shutdown();
  }
  /**
   * Use this for one-off queries.
   * Returns local data if available, otherwise fetches from the server.
   * Because we want to avoid stale data, this method will throw an error
   * if the user is offline or there is no active connection to the server.
   *
   * @see https://instantdb.com/docs/instaql
   *
   * @example
   *
   *  const resp = await db.queryOnce({ goals: {} });
   *  console.log(resp.data.goals)
   */
  queryOnce(e, t) {
    return this._reactor.queryOnce(e, t);
  }
  /**
   * @deprecated This is an experimental function that is not yet ready for production use.
   * Use this function to sync an entire namespace.
   * It has many limitations that will be removed in the future:
   * 1. Must be used with an admin token
   * 2. Does not support permissions
   * 3. Does not support where clauses
   * 4. Does not support links
   * It also does not support multiple top-level namespaces. For example,
   *  {posts: {}, users: {}} is invalid. Only `posts` or `users` is allowed, but not both.
   */
  _syncTableExperimental(e, t) {
    return this._reactor.subscribeTable(e, t);
  }
}
function ns(n) {
  if (!n)
    return "0";
  const e = ts.get(n);
  if (e)
    return e;
  const t = ee(n);
  return ts.set(n, t), t;
}
function Pu(n, e) {
  return ns(n._reactor.config.schema) !== ns(e);
}
function Mu(n, e, t, r, s) {
  var u;
  const i = {
    ...n,
    appId: (u = n.appId) == null ? void 0 : u.trim(),
    useDateObjects: n.useDateObjects ?? !1
  }, o = $n[xn(i)];
  if (o)
    return Pu(o, i.schema) && o._reactor.updateSchema(i.schema), o;
  const a = new Xc({
    ...vu,
    ...i,
    cardinalityInference: !!i.schema
  }, e || qs, t || zs, { ...r || {}, "@instantdb/core": sr }, s), c = new Iu(a);
  return $n[xn(i)] = c, Ru(i.appId, i.devtool), c;
}
function Ru(n, e) {
  if (typeof window > "u" || typeof window.location > "u" || typeof document > "u" || typeof e == "boolean" && !e)
    return;
  const t = {
    position: "bottom-right",
    allowedHosts: ["localhost"],
    ...typeof e == "object" ? e : {}
  };
  t.allowedHosts.includes(window.location.hostname) && uu(n, t);
}
var Dn = { exports: {} }, lt = {};
/**
 * @license React
 * react-jsx-runtime.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var rs;
function ju() {
  if (rs) return lt;
  rs = 1;
  var n = fs, e = Symbol.for("react.element"), t = Symbol.for("react.fragment"), r = Object.prototype.hasOwnProperty, s = n.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentOwner, i = { key: !0, ref: !0, __self: !0, __source: !0 };
  function o(a, c, u) {
    var l, f = {}, d = null, y = null;
    u !== void 0 && (d = "" + u), c.key !== void 0 && (d = "" + c.key), c.ref !== void 0 && (y = c.ref);
    for (l in c) r.call(c, l) && !i.hasOwnProperty(l) && (f[l] = c[l]);
    if (a && a.defaultProps) for (l in c = a.defaultProps, c) f[l] === void 0 && (f[l] = c[l]);
    return { $$typeof: e, type: a, key: d, ref: y, props: f, _owner: s.current };
  }
  return lt.Fragment = t, lt.jsx = o, lt.jsxs = o, lt;
}
var ft = {}, ss;
function xu() {
  if (ss) return ft;
  ss = 1;
  var n = {};
  /**
   * @license React
   * react-jsx-runtime.development.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   */
  return n.NODE_ENV !== "production" && function() {
    var e = fs, t = Symbol.for("react.element"), r = Symbol.for("react.portal"), s = Symbol.for("react.fragment"), i = Symbol.for("react.strict_mode"), o = Symbol.for("react.profiler"), a = Symbol.for("react.provider"), c = Symbol.for("react.context"), u = Symbol.for("react.forward_ref"), l = Symbol.for("react.suspense"), f = Symbol.for("react.suspense_list"), d = Symbol.for("react.memo"), y = Symbol.for("react.lazy"), b = Symbol.for("react.offscreen"), g = Symbol.iterator, w = "@@iterator";
    function S(h) {
      if (h === null || typeof h != "object")
        return null;
      var T = g && h[g] || h[w];
      return typeof T == "function" ? T : null;
    }
    var _ = e.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
    function m(h) {
      {
        for (var T = arguments.length, E = new Array(T > 1 ? T - 1 : 0), P = 1; P < T; P++)
          E[P - 1] = arguments[P];
        A("error", h, E);
      }
    }
    function A(h, T, E) {
      {
        var P = _.ReactDebugCurrentFrame, F = P.getStackAddendum();
        F !== "" && (T += "%s", E = E.concat([F]));
        var K = E.map(function(U) {
          return String(U);
        });
        K.unshift("Warning: " + T), Function.prototype.apply.call(console[h], console, K);
      }
    }
    var v = !1, I = !1, j = !1, N = !1, Q = !1, ge;
    ge = Symbol.for("react.module.reference");
    function Ae(h) {
      return !!(typeof h == "string" || typeof h == "function" || h === s || h === o || Q || h === i || h === l || h === f || N || h === b || v || I || j || typeof h == "object" && h !== null && (h.$$typeof === y || h.$$typeof === d || h.$$typeof === a || h.$$typeof === c || h.$$typeof === u || // This needs to include all possible module reference object
      // types supported by any Flight configuration anywhere since
      // we don't know which Flight build this will end up being used
      // with.
      h.$$typeof === ge || h.getModuleId !== void 0));
    }
    function re(h, T, E) {
      var P = h.displayName;
      if (P)
        return P;
      var F = T.displayName || T.name || "";
      return F !== "" ? E + "(" + F + ")" : E;
    }
    function se(h) {
      return h.displayName || "Context";
    }
    function B(h) {
      if (h == null)
        return null;
      if (typeof h.tag == "number" && m("Received an unexpected object in getComponentNameFromType(). This is likely a bug in React. Please file an issue."), typeof h == "function")
        return h.displayName || h.name || null;
      if (typeof h == "string")
        return h;
      switch (h) {
        case s:
          return "Fragment";
        case r:
          return "Portal";
        case o:
          return "Profiler";
        case i:
          return "StrictMode";
        case l:
          return "Suspense";
        case f:
          return "SuspenseList";
      }
      if (typeof h == "object")
        switch (h.$$typeof) {
          case c:
            var T = h;
            return se(T) + ".Consumer";
          case a:
            var E = h;
            return se(E._context) + ".Provider";
          case u:
            return re(h, h.render, "ForwardRef");
          case d:
            var P = h.displayName || null;
            return P !== null ? P : B(h.type) || "Memo";
          case y: {
            var F = h, K = F._payload, U = F._init;
            try {
              return B(U(K));
            } catch {
              return null;
            }
          }
        }
      return null;
    }
    var k = Object.assign, M = 0, x, q, z, O, C, L, V;
    function J() {
    }
    J.__reactDisabledLog = !0;
    function ae() {
      {
        if (M === 0) {
          x = console.log, q = console.info, z = console.warn, O = console.error, C = console.group, L = console.groupCollapsed, V = console.groupEnd;
          var h = {
            configurable: !0,
            enumerable: !0,
            value: J,
            writable: !0
          };
          Object.defineProperties(console, {
            info: h,
            log: h,
            warn: h,
            error: h,
            group: h,
            groupCollapsed: h,
            groupEnd: h
          });
        }
        M++;
      }
    }
    function at() {
      {
        if (M--, M === 0) {
          var h = {
            configurable: !0,
            enumerable: !0,
            writable: !0
          };
          Object.defineProperties(console, {
            log: k({}, h, {
              value: x
            }),
            info: k({}, h, {
              value: q
            }),
            warn: k({}, h, {
              value: z
            }),
            error: k({}, h, {
              value: O
            }),
            group: k({}, h, {
              value: C
            }),
            groupCollapsed: k({}, h, {
              value: L
            }),
            groupEnd: k({}, h, {
              value: V
            })
          });
        }
        M < 0 && m("disabledDepth fell below zero. This is a bug in React. Please file an issue.");
      }
    }
    var Se = _.ReactCurrentDispatcher, tn;
    function kt(h, T, E) {
      {
        if (tn === void 0)
          try {
            throw Error();
          } catch (F) {
            var P = F.stack.trim().match(/\n( *(at )?)/);
            tn = P && P[1] || "";
          }
        return `
` + tn + h;
      }
    }
    var nn = !1, At;
    {
      var oi = typeof WeakMap == "function" ? WeakMap : Map;
      At = new oi();
    }
    function or(h, T) {
      if (!h || nn)
        return "";
      {
        var E = At.get(h);
        if (E !== void 0)
          return E;
      }
      var P;
      nn = !0;
      var F = Error.prepareStackTrace;
      Error.prepareStackTrace = void 0;
      var K;
      K = Se.current, Se.current = null, ae();
      try {
        if (T) {
          var U = function() {
            throw Error();
          };
          if (Object.defineProperty(U.prototype, "props", {
            set: function() {
              throw Error();
            }
          }), typeof Reflect == "object" && Reflect.construct) {
            try {
              Reflect.construct(U, []);
            } catch (de) {
              P = de;
            }
            Reflect.construct(h, [], U);
          } else {
            try {
              U.call();
            } catch (de) {
              P = de;
            }
            h.call(U.prototype);
          }
        } else {
          try {
            throw Error();
          } catch (de) {
            P = de;
          }
          h();
        }
      } catch (de) {
        if (de && P && typeof de.stack == "string") {
          for (var $ = de.stack.split(`
`), ce = P.stack.split(`
`), Y = $.length - 1, X = ce.length - 1; Y >= 1 && X >= 0 && $[Y] !== ce[X]; )
            X--;
          for (; Y >= 1 && X >= 0; Y--, X--)
            if ($[Y] !== ce[X]) {
              if (Y !== 1 || X !== 1)
                do
                  if (Y--, X--, X < 0 || $[Y] !== ce[X]) {
                    var we = `
` + $[Y].replace(" at new ", " at ");
                    return h.displayName && we.includes("<anonymous>") && (we = we.replace("<anonymous>", h.displayName)), typeof h == "function" && At.set(h, we), we;
                  }
                while (Y >= 1 && X >= 0);
              break;
            }
        }
      } finally {
        nn = !1, Se.current = K, at(), Error.prepareStackTrace = F;
      }
      var Be = h ? h.displayName || h.name : "", De = Be ? kt(Be) : "";
      return typeof h == "function" && At.set(h, De), De;
    }
    function ai(h, T, E) {
      return or(h, !1);
    }
    function ci(h) {
      var T = h.prototype;
      return !!(T && T.isReactComponent);
    }
    function Ct(h, T, E) {
      if (h == null)
        return "";
      if (typeof h == "function")
        return or(h, ci(h));
      if (typeof h == "string")
        return kt(h);
      switch (h) {
        case l:
          return kt("Suspense");
        case f:
          return kt("SuspenseList");
      }
      if (typeof h == "object")
        switch (h.$$typeof) {
          case u:
            return ai(h.render);
          case d:
            return Ct(h.type, T, E);
          case y: {
            var P = h, F = P._payload, K = P._init;
            try {
              return Ct(K(F), T, E);
            } catch {
            }
          }
        }
      return "";
    }
    var ct = Object.prototype.hasOwnProperty, ar = {}, cr = _.ReactDebugCurrentFrame;
    function It(h) {
      if (h) {
        var T = h._owner, E = Ct(h.type, h._source, T ? T.type : null);
        cr.setExtraStackFrame(E);
      } else
        cr.setExtraStackFrame(null);
    }
    function ui(h, T, E, P, F) {
      {
        var K = Function.call.bind(ct);
        for (var U in h)
          if (K(h, U)) {
            var $ = void 0;
            try {
              if (typeof h[U] != "function") {
                var ce = Error((P || "React class") + ": " + E + " type `" + U + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof h[U] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`.");
                throw ce.name = "Invariant Violation", ce;
              }
              $ = h[U](T, U, P, E, null, "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED");
            } catch (Y) {
              $ = Y;
            }
            $ && !($ instanceof Error) && (It(F), m("%s: type specification of %s `%s` is invalid; the type checker function must return `null` or an `Error` but returned a %s. You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument).", P || "React class", E, U, typeof $), It(null)), $ instanceof Error && !($.message in ar) && (ar[$.message] = !0, It(F), m("Failed %s type: %s", E, $.message), It(null));
          }
      }
    }
    var li = Array.isArray;
    function rn(h) {
      return li(h);
    }
    function fi(h) {
      {
        var T = typeof Symbol == "function" && Symbol.toStringTag, E = T && h[Symbol.toStringTag] || h.constructor.name || "Object";
        return E;
      }
    }
    function di(h) {
      try {
        return ur(h), !1;
      } catch {
        return !0;
      }
    }
    function ur(h) {
      return "" + h;
    }
    function lr(h) {
      if (di(h))
        return m("The provided key is an unsupported type %s. This value must be coerced to a string before before using it here.", fi(h)), ur(h);
    }
    var fr = _.ReactCurrentOwner, hi = {
      key: !0,
      ref: !0,
      __self: !0,
      __source: !0
    }, dr, hr;
    function pi(h) {
      if (ct.call(h, "ref")) {
        var T = Object.getOwnPropertyDescriptor(h, "ref").get;
        if (T && T.isReactWarning)
          return !1;
      }
      return h.ref !== void 0;
    }
    function yi(h) {
      if (ct.call(h, "key")) {
        var T = Object.getOwnPropertyDescriptor(h, "key").get;
        if (T && T.isReactWarning)
          return !1;
      }
      return h.key !== void 0;
    }
    function bi(h, T) {
      typeof h.ref == "string" && fr.current;
    }
    function gi(h, T) {
      {
        var E = function() {
          dr || (dr = !0, m("%s: `key` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", T));
        };
        E.isReactWarning = !0, Object.defineProperty(h, "key", {
          get: E,
          configurable: !0
        });
      }
    }
    function _i(h, T) {
      {
        var E = function() {
          hr || (hr = !0, m("%s: `ref` is not a prop. Trying to access it will result in `undefined` being returned. If you need to access the same value within the child component, you should pass it as a different prop. (https://reactjs.org/link/special-props)", T));
        };
        E.isReactWarning = !0, Object.defineProperty(h, "ref", {
          get: E,
          configurable: !0
        });
      }
    }
    var mi = function(h, T, E, P, F, K, U) {
      var $ = {
        // This tag allows us to uniquely identify this as a React Element
        $$typeof: t,
        // Built-in properties that belong on the element
        type: h,
        key: T,
        ref: E,
        props: U,
        // Record the component responsible for creating this element.
        _owner: K
      };
      return $._store = {}, Object.defineProperty($._store, "validated", {
        configurable: !1,
        enumerable: !1,
        writable: !0,
        value: !1
      }), Object.defineProperty($, "_self", {
        configurable: !1,
        enumerable: !1,
        writable: !1,
        value: P
      }), Object.defineProperty($, "_source", {
        configurable: !1,
        enumerable: !1,
        writable: !1,
        value: F
      }), Object.freeze && (Object.freeze($.props), Object.freeze($)), $;
    };
    function wi(h, T, E, P, F) {
      {
        var K, U = {}, $ = null, ce = null;
        E !== void 0 && (lr(E), $ = "" + E), yi(T) && (lr(T.key), $ = "" + T.key), pi(T) && (ce = T.ref, bi(T, F));
        for (K in T)
          ct.call(T, K) && !hi.hasOwnProperty(K) && (U[K] = T[K]);
        if (h && h.defaultProps) {
          var Y = h.defaultProps;
          for (K in Y)
            U[K] === void 0 && (U[K] = Y[K]);
        }
        if ($ || ce) {
          var X = typeof h == "function" ? h.displayName || h.name || "Unknown" : h;
          $ && gi(U, X), ce && _i(U, X);
        }
        return mi(h, $, ce, F, P, fr.current, U);
      }
    }
    var sn = _.ReactCurrentOwner, pr = _.ReactDebugCurrentFrame;
    function Qe(h) {
      if (h) {
        var T = h._owner, E = Ct(h.type, h._source, T ? T.type : null);
        pr.setExtraStackFrame(E);
      } else
        pr.setExtraStackFrame(null);
    }
    var on;
    on = !1;
    function an(h) {
      return typeof h == "object" && h !== null && h.$$typeof === t;
    }
    function yr() {
      {
        if (sn.current) {
          var h = B(sn.current.type);
          if (h)
            return `

Check the render method of \`` + h + "`.";
        }
        return "";
      }
    }
    function Ti(h) {
      return "";
    }
    var br = {};
    function Si(h) {
      {
        var T = yr();
        if (!T) {
          var E = typeof h == "string" ? h : h.displayName || h.name;
          E && (T = `

Check the top-level render call using <` + E + ">.");
        }
        return T;
      }
    }
    function gr(h, T) {
      {
        if (!h._store || h._store.validated || h.key != null)
          return;
        h._store.validated = !0;
        var E = Si(T);
        if (br[E])
          return;
        br[E] = !0;
        var P = "";
        h && h._owner && h._owner !== sn.current && (P = " It was passed a child from " + B(h._owner.type) + "."), Qe(h), m('Each child in a list should have a unique "key" prop.%s%s See https://reactjs.org/link/warning-keys for more information.', E, P), Qe(null);
      }
    }
    function _r(h, T) {
      {
        if (typeof h != "object")
          return;
        if (rn(h))
          for (var E = 0; E < h.length; E++) {
            var P = h[E];
            an(P) && gr(P, T);
          }
        else if (an(h))
          h._store && (h._store.validated = !0);
        else if (h) {
          var F = S(h);
          if (typeof F == "function" && F !== h.entries)
            for (var K = F.call(h), U; !(U = K.next()).done; )
              an(U.value) && gr(U.value, T);
        }
      }
    }
    function vi(h) {
      {
        var T = h.type;
        if (T == null || typeof T == "string")
          return;
        var E;
        if (typeof T == "function")
          E = T.propTypes;
        else if (typeof T == "object" && (T.$$typeof === u || // Note: Memo only checks outer props here.
        // Inner props are checked in the reconciler.
        T.$$typeof === d))
          E = T.propTypes;
        else
          return;
        if (E) {
          var P = B(T);
          ui(E, h.props, "prop", P, h);
        } else if (T.PropTypes !== void 0 && !on) {
          on = !0;
          var F = B(T);
          m("Component %s declared `PropTypes` instead of `propTypes`. Did you misspell the property assignment?", F || "Unknown");
        }
        typeof T.getDefaultProps == "function" && !T.getDefaultProps.isReactClassApproved && m("getDefaultProps is only used on classic React.createClass definitions. Use a static property named `defaultProps` instead.");
      }
    }
    function Ei(h) {
      {
        for (var T = Object.keys(h.props), E = 0; E < T.length; E++) {
          var P = T[E];
          if (P !== "children" && P !== "key") {
            Qe(h), m("Invalid prop `%s` supplied to `React.Fragment`. React.Fragment can only have `key` and `children` props.", P), Qe(null);
            break;
          }
        }
        h.ref !== null && (Qe(h), m("Invalid attribute `ref` supplied to `React.Fragment`."), Qe(null));
      }
    }
    var mr = {};
    function wr(h, T, E, P, F, K) {
      {
        var U = Ae(h);
        if (!U) {
          var $ = "";
          (h === void 0 || typeof h == "object" && h !== null && Object.keys(h).length === 0) && ($ += " You likely forgot to export your component from the file it's defined in, or you might have mixed up default and named imports.");
          var ce = Ti();
          ce ? $ += ce : $ += yr();
          var Y;
          h === null ? Y = "null" : rn(h) ? Y = "array" : h !== void 0 && h.$$typeof === t ? (Y = "<" + (B(h.type) || "Unknown") + " />", $ = " Did you accidentally export a JSX literal instead of a component?") : Y = typeof h, m("React.jsx: type is invalid -- expected a string (for built-in components) or a class/function (for composite components) but got: %s.%s", Y, $);
        }
        var X = wi(h, T, E, F, K);
        if (X == null)
          return X;
        if (U) {
          var we = T.children;
          if (we !== void 0)
            if (P)
              if (rn(we)) {
                for (var Be = 0; Be < we.length; Be++)
                  _r(we[Be], h);
                Object.freeze && Object.freeze(we);
              } else
                m("React.jsx: Static children should always be an array. You are likely explicitly calling React.jsxs or React.jsxDEV. Use the Babel transform instead.");
            else
              _r(we, h);
        }
        if (ct.call(T, "key")) {
          var De = B(h), de = Object.keys(T).filter(function(Pi) {
            return Pi !== "key";
          }), cn = de.length > 0 ? "{key: someKey, " + de.join(": ..., ") + ": ...}" : "{key: someKey}";
          if (!mr[De + cn]) {
            var Ii = de.length > 0 ? "{" + de.join(": ..., ") + ": ...}" : "{}";
            m(`A props object containing a "key" prop is being spread into JSX:
  let props = %s;
  <%s {...props} />
React keys must be passed directly to JSX without using spread:
  let props = %s;
  <%s key={someKey} {...props} />`, cn, De, Ii, De), mr[De + cn] = !0;
          }
        }
        return h === s ? Ei(X) : vi(X), X;
      }
    }
    function Oi(h, T, E) {
      return wr(h, T, E, !0);
    }
    function ki(h, T, E) {
      return wr(h, T, E, !1);
    }
    var Ai = ki, Ci = Oi;
    ft.Fragment = s, ft.jsx = Ai, ft.jsxs = Ci;
  }(), ft;
}
var $u = {};
$u.NODE_ENV === "production" ? Dn.exports = ju() : Dn.exports = xu();
var oe = Dn.exports;
const Du = {
  isLoading: !0,
  data: void 0,
  pageInfo: void 0,
  error: void 0
};
function is(n) {
  return {
    isLoading: !n,
    data: void 0,
    pageInfo: void 0,
    error: void 0,
    ...n || {}
  };
}
function Uu(n, e, t, r) {
  e && t && "ruleParams" in t && (e = { $$ruleParams: t.ruleParams, ...e });
  const s = e ? ri(e) : null, i = ee(s), o = $e(is(n._reactor.getPreviousResult(s))), a = $e(Du), c = Me(
    (l) => (o.current = is(n._reactor.getPreviousResult(s)), l(), s ? n.subscribeQuery(s, (d) => {
      o.current = {
        isLoading: !d,
        // @ts-expect-error: ts thinks this will always be overwritten
        data: void 0,
        // @ts-expect-error: ts thinks this will always be overwritten
        pageInfo: void 0,
        // @ts-expect-error: ts thinks this will always be overwritten
        error: void 0,
        ...d
      }, l();
    }) : () => {
    }),
    // Build a new subscribe function if the query changes
    [i]
  );
  return { state: qt(c, () => o.current, () => a.current), query: s, queryHash: i };
}
const Lu = {
  error: void 0,
  data: void 0,
  isLoading: !0,
  canLoadNextPage: !1
};
function Fu({ core: n, query: e, opts: t }) {
  const r = $e(null), s = ee(e), i = ee(t), o = Su(n, e, t), a = $e({
    ...o,
    isLoading: !o.data && !o.error
  }), c = Me((f) => {
    if (r.current = null, a.current = {
      error: void 0,
      data: void 0,
      isLoading: !0,
      canLoadNextPage: !1
    }, f(), !e)
      return () => {
      };
    const d = n.subscribeInfiniteQuery(e, (y) => {
      a.current = {
        ...y,
        isLoading: !1
      }, f();
    }, t);
    return r.current = d, () => {
      d.unsubscribe(), r.current === d && (r.current = null);
    };
  }, [s, i]);
  return {
    ...qt(c, () => a.current, () => Lu),
    loadNextPage: () => {
      var f;
      (f = r.current) == null || f.loadNextPage();
    }
  };
}
function Nu() {
  const n = $e(null);
  We(() => {
    t();
  }, []);
  function e(r, s) {
    t(), n.current = setTimeout(s, r);
  }
  function t() {
    n.current && clearTimeout(n.current);
  }
  return { set: e, clear: t };
}
const Ku = 1e3;
function Wu(n, e, t) {
  const r = $e(t);
  r.current = t, We(() => n.core._reactor.subscribeTopic(n.type, n.id, e, (i, o) => {
    r.current(i, o);
  }), [n.id, e]);
}
function qu(n, e) {
  return We(() => n.core._reactor.joinRoom(n.type, n.id), [n.id]), Me((r) => {
    n.core._reactor.publishTopic({
      roomType: n.type,
      roomId: n.id,
      topic: e,
      data: r
    });
  }, [n.id, e]);
}
function zu(n, e = {}) {
  var o, a;
  const [t, r] = ds(() => n.core._reactor.getPresence(n.type, n.id, e) ?? {
    peers: {},
    isLoading: !0
  });
  We(() => n.core._reactor.subscribePresence(n.type, n.id, e, (u) => {
    r(u);
  }), [n.id, e.user, (o = e.peers) == null ? void 0 : o.join(), (a = e.keys) == null ? void 0 : a.join()]);
  const s = Me((c) => {
    n.core._reactor.publishPresence(n.type, n.id, c);
  }, [n.type, n.id]);
  return _n(() => ({
    ...t,
    publishPresence: s
  }), [t, s]);
}
function Vu(n, e, t) {
  We(() => n.core._reactor.joinRoom(n.type, n.id, e), [n.id]), We(() => n.core._reactor.publishPresence(n.type, n.id, e), [n.type, n.id, t ?? JSON.stringify(e)]);
}
function Qu(n, e, t = {}) {
  const r = Nu(), s = Le.usePresence(n, {
    keys: [e]
  }), i = _n(() => {
    const l = n._core._reactor.getPresence(n.type, n.id);
    return t != null && t.writeOnly ? [] : Object.values((l == null ? void 0 : l.peers) ?? {}).filter((f) => f[e] === !0);
  }, [t == null ? void 0 : t.writeOnly, s]), o = Me((l) => {
    n.core._reactor.publishPresence(n.type, n.id, {
      [e]: l
    }), l && ((t == null ? void 0 : t.timeout) === null || (t == null ? void 0 : t.timeout) === 0 || r.set((t == null ? void 0 : t.timeout) ?? Ku, () => {
      n.core._reactor.publishPresence(n.type, n.id, {
        [e]: null
      });
    }));
  }, [n.type, n.id, e, t == null ? void 0 : t.timeout, r]), a = Me((l) => {
    const d = !((t == null ? void 0 : t.stopOnEnter) && l.key === "Enter");
    o(d);
  }, [t.stopOnEnter, o]), c = Me(() => {
    o(!1);
  }, [o]), u = _n(() => ({ onKeyDown: a, onBlur: c }), [a, c]);
  return {
    active: i,
    setActive: o,
    inputProps: u
  };
}
const Le = {
  useTopicEffect: Wu,
  usePublishTopic: qu,
  usePresence: zu,
  useSyncPresence: Vu,
  useTypingIndicator: Qu
};
class Bu {
  constructor(e, t, r) {
    p(this, "core");
    /** @deprecated use `core` instead */
    p(this, "_core");
    p(this, "type");
    p(this, "id");
    /**
     * @deprecated
     * `db.room(...).useTopicEffect` is deprecated. You can replace it with `db.rooms.useTopicEffect`.
     *
     * @example
     *
     * // Before
     * const room = db.room('chat', 'room-id');
     * room.useTopicEffect('emoji', (message, peer) => {  });
     *
     * // After
     * const room = db.room('chat', 'room-id');
     * db.rooms.useTopicEffect(room, 'emoji', (message, peer) => {  });
     */
    p(this, "useTopicEffect", (e, t) => {
      Le.useTopicEffect(this, e, t);
    });
    /**
     * @deprecated
     * `db.room(...).usePublishTopic` is deprecated. You can replace it with `db.rooms.usePublishTopic`.
     *
     * @example
     *
     * // Before
     * const room = db.room('chat', 'room-id');
     * const publish = room.usePublishTopic('emoji');
     *
     * // After
     * const room = db.room('chat', 'room-id');
     * const publish = db.rooms.usePublishTopic(room, 'emoji');
     */
    p(this, "usePublishTopic", (e) => Le.usePublishTopic(this, e));
    /**
     * @deprecated
     * `db.room(...).usePresence` is deprecated. You can replace it with `db.rooms.usePresence`.
     *
     * @example
     *
     * // Before
     * const room = db.room('chat', 'room-id');
     * const { peers } = room.usePresence({ keys: ["name", "avatar"] });
     *
     * // After
     * const room = db.room('chat', 'room-id');
     * const { peers } = db.rooms.usePresence(room, { keys: ["name", "avatar"] });
     */
    p(this, "usePresence", (e = {}) => Le.usePresence(this, e));
    /**
     * @deprecated
     * `db.room(...).useSyncPresence` is deprecated. You can replace it with `db.rooms.useSyncPresence`.
     *
     * @example
     *
     * // Before
     * const room = db.room('chat', 'room-id');
     * room.useSyncPresence(room, { nickname });
     *
     * // After
     * const room = db.room('chat', 'room-id');
     * db.rooms.useSyncPresence(room, { nickname });
     */
    p(this, "useSyncPresence", (e, t) => Le.useSyncPresence(this, e, t));
    /**
     * @deprecated
     * `db.room(...).useTypingIndicator` is deprecated. You can replace it with `db.rooms.useTypingIndicator`.
     *
     * @example
     *
     * // Before
     * const room = db.room('chat', 'room-id');
     * const typing = room.useTypingIndiactor(room, 'chat-input');
     *
     * // After
     * const room = db.room('chat', 'room-id');
     * const typing = db.rooms.useTypingIndiactor(room, 'chat-input');
     */
    p(this, "useTypingIndicator", (e, t = {}) => Le.useTypingIndicator(this, e, t));
    this.core = e, this._core = this.core, this.type = t, this.id = r;
  }
}
const Gu = {
  isLoading: !0,
  user: void 0,
  error: void 0
};
class Nt {
  constructor(e, t) {
    p(this, "tx", nr());
    p(this, "auth");
    p(this, "storage");
    p(this, "streams");
    p(this, "core");
    /** @deprecated use `core` instead */
    p(this, "_core");
    /**
     * Returns a unique ID for a given `name`. It's stored in local storage,
     * so you will get the same ID across sessions.
     *
     * This is useful for generating IDs that could identify a local device or user.
     *
     * @example
     *  const deviceId = await db.getLocalId('device');
     */
    p(this, "getLocalId", (e) => this.core.getLocalId(e));
    /**
     * A hook that returns a unique ID for a given `name`. localIds are
     * stored in local storage, so you will get the same ID across sessions.
     *
     * Initially returns `null`, and then loads the localId.
     *
     * @example
     * const deviceId = db.useLocalId('device');
     * if (!deviceId) return null; // loading
     * console.log('Device ID:', deviceId)
     */
    p(this, "useLocalId", (e) => {
      const [t, r] = ds(null);
      return We(() => {
        (async () => {
          const i = await this.getLocalId(e);
          r(i);
        })();
      }, [e]), t;
    });
    /**
     * Hooks for working with rooms
     *
     * @see https://instantdb.com/docs/presence-and-topics
     *
     * @example
     *  const room = db.room('chat', roomId);
     *  const { peers } = db.rooms.usePresence(room);
     *  const publish = db.rooms.usePublishTopic(room, 'emoji');
     *  // ...
     */
    p(this, "rooms", Le);
    /**
     * Use this to write data! You can create, update, delete, and link objects
     *
     * @see https://instantdb.com/docs/instaml
     *
     * @example
     *   // Create a new object in the `goals` namespace
     *   const goalId = id();
     *   db.transact(db.tx.goals[goalId].update({title: "Get fit"}))
     *
     *   // Update the title
     *   db.transact(db.tx.goals[goalId].update({title: "Get super fit"}))
     *
     *   // Delete it
     *   db.transact(db.tx.goals[goalId].delete())
     *
     *   // Or create an association:
     *   todoId = id();
     *   db.transact([
     *    db.tx.todos[todoId].update({ title: 'Go on a run' }),
     *    db.tx.goals[goalId].link({todos: todoId}),
     *  ])
     */
    p(this, "transact", (e) => this.core.transact(e));
    /**
     * Use this to query your data!
     *
     * @see https://instantdb.com/docs/instaql
     *
     * @example
     *   // listen to all goals
     *   const { isLoading, error, data } = db.useQuery({ goals: {} });
     *
     *   // goals where the title is "Get Fit"
     *   const { isLoading, error, data } = db.useQuery({
     *     goals: { $: { where: { title: 'Get Fit' } } },
     *   });
     *
     *   // all goals, _alongside_ their todos
     *   const { isLoading, error, data } = db.useQuery({
     *     goals: { todos: {} },
     *   });
     *
     *   // skip if `user` is not logged in
     *   const { isLoading, error, data } = db.useQuery(
     *     auth.user ? { goals: {} } : null,
     *   );
     */
    p(this, "useQuery", (e, t) => Uu(this.core, e, t).state);
    /**
     * Listen for the logged in state. This is useful
     * for deciding when to show a login screen.
     *
     * Check out the docs for an example `Login` component too!
     *
     * @see https://instantdb.com/docs/auth
     * @example
     *  function App() {
     *    const { isLoading, user, error } = db.useAuth()
     *    if (isLoading) {
     *      return <div>Loading...</div>
     *    }
     *    if (error) {
     *      return <div>Uh oh! {error.message}</div>
     *    }
     *    if (user) {
     *      return <Main user={user} />
     *    }
     *    return <Login />
     *  }
     *
     */
    p(this, "useAuth", () => this._useAuth());
    /**
     * Subscribe to the currently logged in user.
     * If the user is not logged in, this hook with throw an Error.
     * You will want to protect any calls of this hook with a
     * <db.SignedIn> component, or your own logic based on db.useAuth()
     *
     * @see https://instantdb.com/docs/auth
     * @throws Error indicating user not signed in
     * @example
     *  function UserDisplay() {
     *    const user = db.useUser()
     *    return <div>Logged in as: {user.email}</div>
     *  }
     *
     *  <db.SignedIn>
     *    <UserDisplay />
     *  </db.SignedIn>
     *
     */
    p(this, "useUser", () => {
      const { user: e } = this.useAuth();
      if (!e)
        throw new le("useUser must be used within an auth-protected route");
      return e;
    });
    /**
     * Listen for connection status changes to Instant. Use this for things like
     * showing connection state to users
     *
     * @see https://www.instantdb.com/docs/patterns#connection-status
     * @example
     *  function App() {
     *    const status = db.useConnectionStatus()
     *    const connectionState =
     *      status === 'connecting' || status === 'opened'
     *        ? 'authenticating'
     *      : status === 'authenticated'
     *        ? 'connected'
     *      : status === 'closed'
     *        ? 'closed'
     *      : status === 'errored'
     *        ? 'errored'
     *      : 'unexpected state';
     *
     *    return <div>Connection state: {connectionState}</div>
     *  }
     */
    p(this, "useConnectionStatus", () => {
      const e = $e(this.core._reactor.status), t = Me((s) => this.core.subscribeConnectionStatus((o) => {
        o !== e.current && (e.current = o, s());
      }), []);
      return qt(
        t,
        () => e.current,
        // For SSR, always return 'connecting' as the initial state
        () => "connecting"
      );
    });
    /**
     * Use this for one-off queries.
     * Returns local data if available, otherwise fetches from the server.
     * Because we want to avoid stale data, this method will throw an error
     * if the user is offline or there is no active connection to the server.
     *
     * @see https://instantdb.com/docs/instaql
     *
     * @example
     *
     *  const resp = await db.queryOnce({ goals: {} });
     *  console.log(resp.data.goals)
     */
    p(this, "queryOnce", (e, t) => this.core.queryOnce(e, t));
    /**
     * Subscribe to a query and incrementally load more items
     *
     * Only one top level namespace in the query is allowed.
     *
     * Changing the query or options while the subscription is active will
     * reset the subscription and start over with new data.
     * @example
     * const {
     *   data,
     *   loadNextPage,
     *   canLoadNextPage,
     * } = db.useInfiniteQuery({
     *   posts: {
     *     $: {
     *       limit: 20,   // Load 20 posts at a time
     *       order: {
     *         createdAt: 'desc',
     *       },
     *     },
     *   },
     * });
     */
    p(this, "useInfiniteQuery", (e, t) => Fu({
      core: this.core,
      query: e,
      opts: t
    }));
    /**
     * Only render children if the user is signed in.
     * @see https://instantdb.com/docs/auth
     *
     * @example
     *  <db.SignedIn>
     *    <MyComponent />
     *  </db.SignedIn>
     *
     */
    p(this, "SignedIn", ({ children: e }) => {
      const t = this.useAuth();
      return t.isLoading || t.error || !t.user ? null : oe.jsx(oe.Fragment, { children: e });
    });
    /**
     * Only render children if the user is signed out.
     * @see https://instantdb.com/docs/auth
     *
     * @example
     *  <db.SignedOut>
     *    <MyComponent />
     *  </db.SignedOut>
     *
     */
    p(this, "SignedOut", ({ children: e }) => {
      const t = this.useAuth();
      return t.isLoading || t.error || t.user ? null : oe.jsx(oe.Fragment, { children: e });
    });
    this.core = Mu(
      e,
      // @ts-expect-error because TS can't resolve subclass statics
      e.Store || this.constructor.Store,
      // @ts-expect-error because TS can't resolve subclass statics
      this.constructor.NetworkListener,
      t,
      // @ts-expect-error because TS can't resolve subclass statics
      this.constructor.EventSourceImpl
    ), this._core = this.core, this.auth = this.core.auth, this.storage = this.core.storage, this.streams = this.core.streams;
  }
  /**
   * Obtain a handle to a room, which allows you to listen to topics and presence data
   *
   * If you don't provide a `type` or `id`, Instant will default to `_defaultRoomType` and `_defaultRoomId`
   * as the room type and id, respectively.
   *
   * @see https://instantdb.com/docs/presence-and-topics
   *
   * @example
   *  const room = db.room('chat', roomId);
   *  const { peers } = db.rooms.usePresence(room);
   */
  room(e = "_defaultRoomType", t = "_defaultRoomId") {
    return new Bu(this.core, e, t);
  }
  _useAuth() {
    const e = $e(this.core._reactor._currentUserCached), t = Me((s) => this.core.subscribeAuth((o) => {
      e.current = { isLoading: !1, ...o }, s();
    }), []);
    return qt(t, () => e.current, () => Gu);
  }
  /**
   * One time query for the logged in state. This is useful
   * for scenarios where you want to know the current auth
   * state without subscribing to changes.
   *
   * @see https://instantdb.com/docs/auth
   * @example
   *   const user = await db.getAuth();
   *   console.log('logged in as', user.email)
   */
  getAuth() {
    return this.core.getAuth();
  }
}
p(Nt, "Store"), p(Nt, "NetworkListener"), p(Nt, "EventSourceImpl");
class os extends Error {
  constructor(e, t) {
    super(e), this.name = "ParseError", this.type = t.type, this.field = t.field, this.value = t.value, this.line = t.line;
  }
}
function gn(n) {
}
function Hu(n) {
  if (typeof n == "function")
    throw new TypeError(
      "`callbacks` must be an object, got a function instead. Did you mean `{onEvent: fn}`?"
    );
  const { onEvent: e = gn, onError: t = gn, onRetry: r = gn, onComment: s } = n;
  let i = "", o = !0, a, c = "", u = "";
  function l(g) {
    const w = o ? g.replace(/^\xEF\xBB\xBF/, "") : g, [S, _] = Ju(`${i}${w}`);
    for (const m of S)
      f(m);
    i = _, o = !1;
  }
  function f(g) {
    if (g === "") {
      y();
      return;
    }
    if (g.startsWith(":")) {
      s && s(g.slice(g.startsWith(": ") ? 2 : 1));
      return;
    }
    const w = g.indexOf(":");
    if (w !== -1) {
      const S = g.slice(0, w), _ = g[w + 1] === " " ? 2 : 1, m = g.slice(w + _);
      d(S, m, g);
      return;
    }
    d(g, "", g);
  }
  function d(g, w, S) {
    switch (g) {
      case "event":
        u = w;
        break;
      case "data":
        c = `${c}${w}
`;
        break;
      case "id":
        a = w.includes("\0") ? void 0 : w;
        break;
      case "retry":
        /^\d+$/.test(w) ? r(parseInt(w, 10)) : t(
          new os(`Invalid \`retry\` value: "${w}"`, {
            type: "invalid-retry",
            value: w,
            line: S
          })
        );
        break;
      default:
        t(
          new os(
            `Unknown field "${g.length > 20 ? `${g.slice(0, 20)}…` : g}"`,
            { type: "unknown-field", field: g, value: w, line: S }
          )
        );
        break;
    }
  }
  function y() {
    c.length > 0 && e({
      id: a,
      event: u || void 0,
      // If the data buffer's last character is a U+000A LINE FEED (LF) character,
      // then remove the last character from the data buffer.
      data: c.endsWith(`
`) ? c.slice(0, -1) : c
    }), a = void 0, c = "", u = "";
  }
  function b(g = {}) {
    i && g.consume && f(i), o = !0, a = void 0, c = "", u = "", i = "";
  }
  return { feed: l, reset: b };
}
function Ju(n) {
  const e = [];
  let t = "", r = 0;
  for (; r < n.length; ) {
    const s = n.indexOf("\r", r), i = n.indexOf(`
`, r);
    let o = -1;
    if (s !== -1 && i !== -1 ? o = Math.min(s, i) : s !== -1 ? o = s : i !== -1 && (o = i), o === -1) {
      t = n.slice(r);
      break;
    } else {
      const a = n.slice(r, o);
      e.push(a), r = o + 1, n[r - 1] === "\r" && n[r] === `
` && r++;
    }
  }
  return [e, t];
}
class as extends Event {
  /**
   * Constructs a new `ErrorEvent` instance. This is typically not called directly,
   * but rather emitted by the `EventSource` object when an error occurs.
   *
   * @param type - The type of the event (should be "error")
   * @param errorEventInitDict - Optional properties to include in the error event
   */
  constructor(e, t) {
    var r, s;
    super(e), this.code = (r = t == null ? void 0 : t.code) != null ? r : void 0, this.message = (s = t == null ? void 0 : t.message) != null ? s : void 0;
  }
  /**
   * Node.js "hides" the `message` and `code` properties of the `ErrorEvent` instance,
   * when it is `console.log`'ed. This makes it harder to debug errors. To ease debugging,
   * we explicitly include the properties in the `inspect` method.
   *
   * This is automatically called by Node.js when you `console.log` an instance of this class.
   *
   * @param _depth - The current depth
   * @param options - The options passed to `util.inspect`
   * @param inspect - The inspect function to use (prevents having to import it from `util`)
   * @returns A string representation of the error
   */
  [Symbol.for("nodejs.util.inspect.custom")](e, t, r) {
    return r(cs(this), t);
  }
  /**
   * Deno "hides" the `message` and `code` properties of the `ErrorEvent` instance,
   * when it is `console.log`'ed. This makes it harder to debug errors. To ease debugging,
   * we explicitly include the properties in the `inspect` method.
   *
   * This is automatically called by Deno when you `console.log` an instance of this class.
   *
   * @param inspect - The inspect function to use (prevents having to import it from `util`)
   * @param options - The options passed to `Deno.inspect`
   * @returns A string representation of the error
   */
  [Symbol.for("Deno.customInspect")](e, t) {
    return e(cs(this), t);
  }
}
function Yu(n) {
  const e = globalThis.DOMException;
  return typeof e == "function" ? new e(n, "SyntaxError") : new SyntaxError(n);
}
function Un(n) {
  return n instanceof Error ? "errors" in n && Array.isArray(n.errors) ? n.errors.map(Un).join(", ") : "cause" in n && n.cause instanceof Error ? `${n}: ${Un(n.cause)}` : n.message : `${n}`;
}
function cs(n) {
  return {
    type: n.type,
    message: n.message,
    code: n.code,
    defaultPrevented: n.defaultPrevented,
    cancelable: n.cancelable,
    timeStamp: n.timeStamp
  };
}
var si = (n) => {
  throw TypeError(n);
}, ir = (n, e, t) => e.has(n) || si("Cannot " + t), R = (n, e, t) => (ir(n, e, "read from private field"), t ? t.call(n) : e.get(n)), ne = (n, e, t) => e.has(n) ? si("Cannot add the same private member more than once") : e instanceof WeakSet ? e.add(n) : e.set(n, t), G = (n, e, t, r) => (ir(n, e, "write to private field"), e.set(n, t), t), Pe = (n, e, t) => (ir(n, e, "access private method"), t), he, Fe, Ye, Kt, Ht, Tt, tt, St, xe, Ze, rt, Xe, gt, ve, Ln, Fn, Nn, us, Kn, Wn, _t, qn, zn;
class Wt extends EventTarget {
  constructor(e, t) {
    var r, s;
    super(), ne(this, ve), this.CONNECTING = 0, this.OPEN = 1, this.CLOSED = 2, ne(this, he), ne(this, Fe), ne(this, Ye), ne(this, Kt), ne(this, Ht), ne(this, Tt), ne(this, tt), ne(this, St, null), ne(this, xe), ne(this, Ze), ne(this, rt, null), ne(this, Xe, null), ne(this, gt, null), ne(this, Fn, async (i) => {
      var o;
      R(this, Ze).reset();
      const { body: a, redirected: c, status: u, headers: l } = i;
      if (u === 204) {
        Pe(this, ve, _t).call(this, "Server sent HTTP 204, not reconnecting", 204), this.close();
        return;
      }
      if (c ? G(this, Ye, new URL(i.url)) : G(this, Ye, void 0), u !== 200) {
        Pe(this, ve, _t).call(this, `Non-200 status code (${u})`, u);
        return;
      }
      if (!(l.get("content-type") || "").startsWith("text/event-stream")) {
        Pe(this, ve, _t).call(this, 'Invalid content type, expected "text/event-stream"', u);
        return;
      }
      if (R(this, he) === this.CLOSED)
        return;
      G(this, he, this.OPEN);
      const f = new Event("open");
      if ((o = R(this, gt)) == null || o.call(this, f), this.dispatchEvent(f), typeof a != "object" || !a || !("getReader" in a)) {
        Pe(this, ve, _t).call(this, "Invalid response body, expected a web ReadableStream", u), this.close();
        return;
      }
      const d = new TextDecoder(), y = a.getReader();
      let b = !0;
      do {
        const { done: g, value: w } = await y.read();
        w && R(this, Ze).feed(d.decode(w, { stream: !g })), g && (b = !1, R(this, Ze).reset(), Pe(this, ve, qn).call(this));
      } while (b);
    }), ne(this, Nn, (i) => {
      G(this, xe, void 0), !(i.name === "AbortError" || i.type === "aborted") && Pe(this, ve, qn).call(this, Un(i));
    }), ne(this, Kn, (i) => {
      typeof i.id == "string" && G(this, St, i.id);
      const o = new MessageEvent(i.event || "message", {
        data: i.data,
        origin: R(this, Ye) ? R(this, Ye).origin : R(this, Fe).origin,
        lastEventId: i.id || ""
      });
      R(this, Xe) && (!i.event || i.event === "message") && R(this, Xe).call(this, o), this.dispatchEvent(o);
    }), ne(this, Wn, (i) => {
      G(this, Tt, i);
    }), ne(this, zn, () => {
      G(this, tt, void 0), R(this, he) === this.CONNECTING && Pe(this, ve, Ln).call(this);
    });
    try {
      if (e instanceof URL)
        G(this, Fe, e);
      else if (typeof e == "string")
        G(this, Fe, new URL(e, Zu()));
      else
        throw new Error("Invalid URL");
    } catch {
      throw Yu("An invalid or illegal string was specified");
    }
    G(this, Ze, Hu({
      onEvent: R(this, Kn),
      onRetry: R(this, Wn)
    })), G(this, he, this.CONNECTING), G(this, Tt, 3e3), G(this, Ht, (r = t == null ? void 0 : t.fetch) != null ? r : globalThis.fetch), G(this, Kt, (s = t == null ? void 0 : t.withCredentials) != null ? s : !1), Pe(this, ve, Ln).call(this);
  }
  /**
   * Returns the state of this EventSource object's connection. It can have the values described below.
   *
   * [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventSource/readyState)
   *
   * Note: typed as `number` instead of `0 | 1 | 2` for compatibility with the `EventSource` interface,
   * defined in the TypeScript `dom` library.
   *
   * @public
   */
  get readyState() {
    return R(this, he);
  }
  /**
   * Returns the URL providing the event stream.
   *
   * [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventSource/url)
   *
   * @public
   */
  get url() {
    return R(this, Fe).href;
  }
  /**
   * Returns true if the credentials mode for connection requests to the URL providing the event stream is set to "include", and false otherwise.
   *
   * [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventSource/withCredentials)
   */
  get withCredentials() {
    return R(this, Kt);
  }
  /** [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventSource/error_event) */
  get onerror() {
    return R(this, rt);
  }
  set onerror(e) {
    G(this, rt, e);
  }
  /** [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventSource/message_event) */
  get onmessage() {
    return R(this, Xe);
  }
  set onmessage(e) {
    G(this, Xe, e);
  }
  /** [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventSource/open_event) */
  get onopen() {
    return R(this, gt);
  }
  set onopen(e) {
    G(this, gt, e);
  }
  addEventListener(e, t, r) {
    const s = t;
    super.addEventListener(e, s, r);
  }
  removeEventListener(e, t, r) {
    const s = t;
    super.removeEventListener(e, s, r);
  }
  /**
   * Aborts any instances of the fetch algorithm started for this EventSource object, and sets the readyState attribute to CLOSED.
   *
   * [MDN Reference](https://developer.mozilla.org/docs/Web/API/EventSource/close)
   *
   * @public
   */
  close() {
    R(this, tt) && clearTimeout(R(this, tt)), R(this, he) !== this.CLOSED && (R(this, xe) && R(this, xe).abort(), G(this, he, this.CLOSED), G(this, xe, void 0));
  }
}
he = /* @__PURE__ */ new WeakMap(), Fe = /* @__PURE__ */ new WeakMap(), Ye = /* @__PURE__ */ new WeakMap(), Kt = /* @__PURE__ */ new WeakMap(), Ht = /* @__PURE__ */ new WeakMap(), Tt = /* @__PURE__ */ new WeakMap(), tt = /* @__PURE__ */ new WeakMap(), St = /* @__PURE__ */ new WeakMap(), xe = /* @__PURE__ */ new WeakMap(), Ze = /* @__PURE__ */ new WeakMap(), rt = /* @__PURE__ */ new WeakMap(), Xe = /* @__PURE__ */ new WeakMap(), gt = /* @__PURE__ */ new WeakMap(), ve = /* @__PURE__ */ new WeakSet(), /**
* Connect to the given URL and start receiving events
*
* @internal
*/
Ln = function() {
  G(this, he, this.CONNECTING), G(this, xe, new AbortController()), R(this, Ht)(R(this, Fe), Pe(this, ve, us).call(this)).then(R(this, Fn)).catch(R(this, Nn));
}, Fn = /* @__PURE__ */ new WeakMap(), Nn = /* @__PURE__ */ new WeakMap(), /**
* Get request options for the `fetch()` request
*
* @returns The request options
* @internal
*/
us = function() {
  var n;
  const e = {
    // [spec] Let `corsAttributeState` be `Anonymous`…
    // [spec] …will have their mode set to "cors"…
    mode: "cors",
    redirect: "follow",
    headers: { Accept: "text/event-stream", ...R(this, St) ? { "Last-Event-ID": R(this, St) } : void 0 },
    cache: "no-store",
    signal: (n = R(this, xe)) == null ? void 0 : n.signal
  };
  return "window" in globalThis && (e.credentials = this.withCredentials ? "include" : "same-origin"), e;
}, Kn = /* @__PURE__ */ new WeakMap(), Wn = /* @__PURE__ */ new WeakMap(), /**
* Handles the process referred to in the EventSource specification as "failing a connection".
*
* @param error - The error causing the connection to fail
* @param code - The HTTP status code, if available
* @internal
*/
_t = function(n, e) {
  var t;
  R(this, he) !== this.CLOSED && G(this, he, this.CLOSED);
  const r = new as("error", { code: e, message: n });
  (t = R(this, rt)) == null || t.call(this, r), this.dispatchEvent(r);
}, /**
* Schedules a reconnection attempt against the EventSource endpoint.
*
* @param message - The error causing the connection to fail
* @param code - The HTTP status code, if available
* @internal
*/
qn = function(n, e) {
  var t;
  if (R(this, he) === this.CLOSED)
    return;
  G(this, he, this.CONNECTING);
  const r = new as("error", { code: e, message: n });
  (t = R(this, rt)) == null || t.call(this, r), this.dispatchEvent(r), G(this, tt, setTimeout(R(this, zn), R(this, Tt)));
}, zn = /* @__PURE__ */ new WeakMap(), /**
* ReadyState representing an EventSource currently trying to connect
*
* @public
*/
Wt.CONNECTING = 0, /**
* ReadyState representing an EventSource connection that is open (eg connected)
*
* @public
*/
Wt.OPEN = 1, /**
* ReadyState representing an EventSource connection that is closed (eg disconnected)
*
* @public
*/
Wt.CLOSED = 2;
function Zu() {
  const n = "document" in globalThis ? globalThis.document : void 0;
  return n && typeof n == "object" && "baseURI" in n && typeof n.baseURI == "string" ? n.baseURI : void 0;
}
class ii extends Nt {
}
p(ii, "EventSourceImpl", Wt);
function Xu(n) {
  const e = {
    ...n,
    useDateObjects: n.useDateObjects ?? !1
  };
  return new ii(e, {
    "@instantdb/react": sr
  });
}
const ul = Xu;
function ll({
  as: n = "div",
  spaceId: e,
  room: t,
  className: r,
  style: s,
  userCursorColor: i,
  children: o,
  renderCursor: a,
  propagate: c,
  zIndex: u
}) {
  const l = e || `cursors-space-default--${String(t.type)}-${t.id}`, f = t.usePresence({
    keys: [l]
  }), d = t._core._reactor.getPresence(t.type, t.id);
  function y(_, m) {
    const A = m.clientX, v = m.clientY, I = (A - _.left) / _.width * 100, j = (v - _.top) / _.height * 100;
    f.publishPresence({
      [l]: {
        x: A,
        y: v,
        xPercent: I,
        yPercent: j,
        color: i
      }
    });
  }
  function b(_) {
    c || _.stopPropagation();
    const m = _.currentTarget.getBoundingClientRect();
    y(m, _);
  }
  function g(_) {
    f.publishPresence({
      [l]: void 0
    });
  }
  function w(_) {
    if (_.touches.length !== 1)
      return;
    const m = _.touches[0];
    if (m.target instanceof Element) {
      c || _.stopPropagation();
      const A = m.target.getBoundingClientRect();
      y(A, m);
    }
  }
  function S(_) {
    f.publishPresence({
      [l]: void 0
    });
  }
  return ji(
    n,
    {
      onMouseMove: b,
      onMouseOut: g,
      onTouchMove: w,
      onTouchEnd: S,
      className: r,
      style: {
        position: "relative",
        ...s
      }
    },
    [
      o,
      /* @__PURE__ */ oe.jsx(
        "div",
        {
          style: {
            ...ls,
            ...tl,
            zIndex: u !== void 0 ? u : nl
          },
          children: Object.entries(f.peers).map(([_, m]) => {
            const A = m[l];
            return A ? /* @__PURE__ */ oe.jsx(
              "div",
              {
                style: {
                  ...ls,
                  transform: `translate(${A.xPercent}%, ${A.yPercent}%)`,
                  transformOrigin: "0 0",
                  transition: "transform 100ms"
                },
                children: a ? a({
                  color: A.color,
                  presence: d == null ? void 0 : d.peers[_]
                }) : /* @__PURE__ */ oe.jsx(el, { ...A })
              },
              _
            ) : null;
          })
        },
        l
      )
    ]
  );
}
function el({ color: n }) {
  const t = n || "black";
  return /* @__PURE__ */ oe.jsxs(
    "svg",
    {
      style: { height: 35, width: 35 },
      viewBox: "0 0 35 35",
      fill: "none",
      xmlns: "http://www.w3.org/2000/svg",
      children: [
        /* @__PURE__ */ oe.jsxs(
          "g",
          {
            fill: "rgba(0,0,0,.2)",
            transform: "matrix(1, 0, 0, 1, -11.999999046325684, -8.406899452209473)",
            children: [
              /* @__PURE__ */ oe.jsx("path", { d: "m12 24.4219v-16.015l11.591 11.619h-6.781l-.411.124z" }),
              /* @__PURE__ */ oe.jsx("path", { d: "m21.0845 25.0962-3.605 1.535-4.682-11.089 3.686-1.553z" })
            ]
          }
        ),
        /* @__PURE__ */ oe.jsxs(
          "g",
          {
            fill: "white",
            transform: "matrix(1, 0, 0, 1, -11.999999046325684, -8.406899452209473)",
            children: [
              /* @__PURE__ */ oe.jsx("path", { d: "m12 24.4219v-16.015l11.591 11.619h-6.781l-.411.124z" }),
              /* @__PURE__ */ oe.jsx("path", { d: "m21.0845 25.0962-3.605 1.535-4.682-11.089 3.686-1.553z" })
            ]
          }
        ),
        /* @__PURE__ */ oe.jsxs(
          "g",
          {
            fill: t,
            transform: "matrix(1, 0, 0, 1, -11.999999046325684, -8.406899452209473)",
            children: [
              /* @__PURE__ */ oe.jsx("path", { d: "m19.751 24.4155-1.844.774-3.1-7.374 1.841-.775z" }),
              /* @__PURE__ */ oe.jsx("path", { d: "m13 10.814v11.188l2.969-2.866.428-.139h4.768z" })
            ]
          }
        )
      ]
    }
  );
}
const ls = {
  position: "absolute",
  top: 0,
  left: 0,
  bottom: 0,
  right: 0
}, tl = {
  overflow: "hidden",
  pointerEvents: "none",
  userSelect: "none"
}, nl = 99999;
export {
  ll as Cursors,
  Zt as InstantAPIError,
  Nt as InstantReactAbstractDatabase,
  ii as InstantReactWebDatabase,
  Xa as StoreInterface,
  Ue as SyncTableCallbackEventType,
  cl as createInstantRouteHandler,
  al as i,
  H as id,
  Xu as init,
  ul as init_experimental,
  Ea as lookup,
  il as setInstantWarningsEnabled,
  ol as tx
};
