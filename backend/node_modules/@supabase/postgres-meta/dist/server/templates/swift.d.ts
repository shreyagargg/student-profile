import type { GeneratorMetadata } from '../../lib/generators.js';
export type AccessControl = 'internal' | 'public' | 'private' | 'package';
type SwiftGeneratorOptions = {
    accessControl: AccessControl;
};
export declare const apply: ({ schemas, tables, foreignTables, views, materializedViews, columns, types, accessControl, }: GeneratorMetadata & SwiftGeneratorOptions) => Promise<string>;
export {};
//# sourceMappingURL=swift.d.ts.map