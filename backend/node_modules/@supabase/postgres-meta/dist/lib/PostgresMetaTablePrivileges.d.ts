import { PostgresMetaResult, PostgresTablePrivileges, PostgresTablePrivilegesGrant, PostgresTablePrivilegesRevoke } from './types.js';
export default class PostgresMetaTablePrivileges {
    query: (sql: string) => Promise<PostgresMetaResult<any>>;
    constructor(query: (sql: string) => Promise<PostgresMetaResult<any>>);
    list({ includeSystemSchemas, includedSchemas, excludedSchemas, limit, offset, }?: {
        includeSystemSchemas?: boolean;
        includedSchemas?: string[];
        excludedSchemas?: string[];
        limit?: number;
        offset?: number;
    }): Promise<PostgresMetaResult<PostgresTablePrivileges[]>>;
    retrieve({ id }: {
        id: number;
    }): Promise<PostgresMetaResult<PostgresTablePrivileges>>;
    retrieve({ name, schema, }: {
        name: string;
        schema: string;
    }): Promise<PostgresMetaResult<PostgresTablePrivileges>>;
    grant(grants: PostgresTablePrivilegesGrant[]): Promise<PostgresMetaResult<PostgresTablePrivileges[]>>;
    revoke(revokes: PostgresTablePrivilegesRevoke[]): Promise<PostgresMetaResult<PostgresTablePrivileges[]>>;
}
//# sourceMappingURL=PostgresMetaTablePrivileges.d.ts.map