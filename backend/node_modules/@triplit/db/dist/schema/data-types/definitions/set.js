export function SetType(items, options) {
    // TODO: validate options
    return {
        type: 'set',
        items,
        config: {
            nullable: options?.nullable,
            default: options?.default,
        },
    };
}
//# sourceMappingURL=set.js.map