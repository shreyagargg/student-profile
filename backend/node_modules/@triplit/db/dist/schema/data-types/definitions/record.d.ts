import { DataType } from '../types/index.js';
import { BaseTypeOptions, OptionsInput, TypeInterface } from '../types/type-definitions.js';
export type RecordProps<Key extends string, DT extends TypeInterface> = {
    [K in Key]: DT;
};
export type DataTypeRecordProps<Properties = Record<string, DataType>> = {
    [K in keyof Properties]: Properties[K] extends DataType ? Properties[K] : never;
};
export type RecordTypeOptions = Omit<BaseTypeOptions, 'default'>;
export type RecordType<Properties extends RecordProps<any, any> = RecordProps<any, any>, Options extends RecordTypeOptions = RecordTypeOptions> = TypeInterface<'record', Options> & {
    properties: Properties;
};
export declare function RecordType<Properties extends DataTypeRecordProps<Properties>, Options extends RecordTypeOptions = {}>(properties: Properties): RecordType<Properties, Options>;
export declare function RecordType<Properties extends DataTypeRecordProps<Properties>, Options extends OptionsInput<RecordTypeOptions>>(properties: Properties, options: Options): RecordType<Properties, Options>;
