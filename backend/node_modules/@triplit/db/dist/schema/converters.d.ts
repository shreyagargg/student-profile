import { DBSchema } from '../db.js';
type EntityConverter = (entity: any) => any;
export type TypeConverters = Map<string, {
    fromDB: EntityConverter;
}>;
export declare function getTypeConvertersFromSchema(schema?: DBSchema): TypeConverters | undefined;
export {};
