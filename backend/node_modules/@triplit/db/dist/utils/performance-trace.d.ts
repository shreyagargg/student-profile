export declare function performanceTrace(name: string, { track, color, properties, }?: {
    track?: string;
    color?: string;
    properties?: Record<string, string | number>;
}): {
    end: () => void;
    [Symbol.dispose](): void;
};
