export function isQueryInclusionSubquery(inclusion) {
    return (!isQueryInclusionShorthand(inclusion) &&
        typeof inclusion === 'object' &&
        'subquery' in inclusion);
}
export function isQueryInclusionShorthand(inclusion) {
    return inclusion === true || inclusion === null;
}
export function isQueryInclusionReference(inclusion) {
    return (!isQueryInclusionShorthand(inclusion) &&
        typeof inclusion === 'object' &&
        '_extends' in inclusion);
}
export function isQueryResultCardinality(cardinality) {
    return cardinality === 'one' || cardinality === 'many';
}
//# sourceMappingURL=subquery.js.map