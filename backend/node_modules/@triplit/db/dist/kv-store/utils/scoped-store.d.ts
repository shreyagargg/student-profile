import { Tuple } from '../../codec.js';
import { CountOptions, KVStore, KVStoreTransaction, ScanOptions } from '../../types.js';
export declare class ScopedKVStore<T extends KVStore> implements KVStore {
    private store;
    private prefix;
    constructor(store: T, prefix: Tuple);
    scope(scope: Tuple): KVStore;
    get(key: Tuple): Promise<any>;
    set(key: Tuple, value: any): Promise<void>;
    delete(key: Tuple): Promise<void>;
    scan(options: ScanOptions): AsyncIterable<[Tuple, any]>;
    scanValues(options: ScanOptions): AsyncIterable<any>;
    clear(): Promise<void>;
    count(options: CountOptions): Promise<number>;
    transact(): KVStoreTransaction;
    applyEdits(sets: AsyncIterable<[Tuple, any]> | Iterable<[Tuple, any]>, deletes: AsyncIterable<Tuple> | Iterable<Tuple>): Promise<void>;
}
export declare class ScopedKVStoreTransaction<T extends KVStoreTransaction> implements KVStoreTransaction {
    private tx;
    private prefix;
    constructor(tx: T, prefix: Tuple);
    scope(prefix: Tuple): KVStoreTransaction;
    get(key: Tuple): Promise<any>;
    set(key: Tuple, value: any): Promise<void>;
    delete(key: Tuple): Promise<void>;
    scan(options: ScanOptions): AsyncIterable<[Tuple, any]>;
    scanValues(options: ScanOptions): AsyncIterable<any>;
    clear(): Promise<void>;
    count(options: CountOptions): Promise<number>;
    commit(): Promise<void>;
    cancel(): void;
    get status(): import("../../types.js").TxStatus;
}
